import { describe, it } from 'vitest';

describe('content script configuration', () => {
  it.todo('matches *://*.flatfox.ch/* URL pattern');

  it.todo('does NOT match homegate.ch URLs');

  it.todo('uses cssInjectionMode ui for Shadow DOM style isolation');
});
