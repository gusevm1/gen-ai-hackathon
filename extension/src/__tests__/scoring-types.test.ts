import { describe, it } from 'vitest';

describe('ScoreResponse types', () => {
  it.todo('TIER_COLORS has entries for all four match tiers');

  it.todo('TIER_COLORS uses modern palette (not traffic light green/yellow/red)');

  it.todo('ScoreResponse interface matches backend contract');
});
