import { describe, it } from 'vitest';

describe('loading state behavior', () => {
  it.todo('shows loading skeleton while score is being computed');

  it.todo('removes loading skeleton when score arrives');

  it.todo('FAB shows spinner while scoring is in progress');
});
