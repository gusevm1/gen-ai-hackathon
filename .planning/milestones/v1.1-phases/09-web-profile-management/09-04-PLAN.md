---
phase: 09-web-profile-management
plan: 04
type: execute
wave: 2
depends_on: [09-02]
files_modified:
  - web/src/components/analysis/ScoreHeader.tsx
  - web/src/components/analysis/CategoryBreakdown.tsx
  - web/src/components/analysis/BulletSummary.tsx
  - web/src/components/analysis/ChecklistSection.tsx
  - web/src/app/(dashboard)/analysis/[listingId]/page.tsx
  - web/src/app/(dashboard)/analyses/page.tsx
autonomous: true
requirements: [UI-04]

must_haves:
  truths:
    - "Analysis page has a professional layout with clear visual hierarchy suitable for demo presentations"
    - "Score header displays prominently with large score number, tier badge, and listing link"
    - "Category breakdown uses score bars with color coding and expandable reasoning"
    - "Checklist section clearly shows met/unmet/unknown items with appropriate icons"
    - "Key points (bullet summary) are visually prominent in a highlighted card"
    - "Analyses list page shows all scored listings for the user"
  artifacts:
    - path: "web/src/components/analysis/ScoreHeader.tsx"
      provides: "Redesigned score header with prominent score display and listing info"
      exports: ["ScoreHeader", "getTierColor"]
    - path: "web/src/components/analysis/CategoryBreakdown.tsx"
      provides: "Redesigned category cards with score bars, importance labels, and reasoning"
      exports: ["CategoryBreakdown", "getScoreColor"]
    - path: "web/src/components/analysis/BulletSummary.tsx"
      provides: "Redesigned key points card with better visual hierarchy"
      exports: ["BulletSummary"]
    - path: "web/src/components/analysis/ChecklistSection.tsx"
      provides: "Redesigned checklist with clear met/unmet status"
      exports: ["ChecklistSection", "getStatusIndicator"]
    - path: "web/src/app/(dashboard)/analysis/[listingId]/page.tsx"
      provides: "Redesigned analysis page layout with 2-column grid"
    - path: "web/src/app/(dashboard)/analyses/page.tsx"
      provides: "Analyses list page showing all scored listings"
  key_links:
    - from: "web/src/app/(dashboard)/analysis/[listingId]/page.tsx"
      to: "all 4 analysis components"
      via: "import and render in page layout"
      pattern: "import.*(ScoreHeader|CategoryBreakdown|BulletSummary|ChecklistSection)"
    - from: "web/src/app/(dashboard)/analyses/page.tsx"
      to: "supabase analyses table"
      via: "server-side query for user's analyses"
      pattern: "supabase\\.from\\('analyses'\\)"
---

<objective>
Redesign the analysis page with a professional layout suitable for demo presentations, and build the analyses list page. Improve visual hierarchy with better spacing, card layouts, color-coded score bars, and a 2-column layout for wider screens.

Purpose: The current analysis page is functional but basic. For demo presentations, it needs to look polished and professional. The analyses list page (currently a placeholder) needs to show actual scored listings.
Output: Redesigned analysis detail page and functional analyses list page.
</objective>

<execution_context>
@/Users/maximgusev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maximgusev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-web-profile-management/09-CONTEXT.md
@.planning/phases/09-web-profile-management/09-RESEARCH.md

@web/src/components/analysis/ScoreHeader.tsx
@web/src/components/analysis/CategoryBreakdown.tsx
@web/src/components/analysis/BulletSummary.tsx
@web/src/components/analysis/ChecklistSection.tsx
@web/src/app/(dashboard)/analysis/[listingId]/page.tsx
@web/src/app/(dashboard)/analyses/page.tsx

<interfaces>
<!-- Existing analysis data shape from Supabase -->
Analysis row from `analyses` table:
```typescript
interface AnalysisRow {
  id: string
  user_id: string
  listing_id: string
  profile_id: string | null
  score: number
  breakdown: {
    overall_score?: number
    match_tier?: string        // 'excellent' | 'good' | 'fair' | 'poor'
    summary_bullets?: string[]
    categories?: Array<{
      name: string             // e.g., 'location', 'price', 'size', 'features', 'condition'
      score: number            // 0-100
      weight: number           // 0-100 (percentage)
      reasoning: string[]      // bullet points
    }>
    checklist?: Array<{
      criterion: string
      met: boolean | null
      note: string
    }>
  }
  created_at: string
  updated_at: string
}
```

Tier color palette (keep consistent):
```typescript
const TIER_COLORS = {
  excellent: { bg: 'bg-emerald-500', text: 'text-white', ring: 'ring-emerald-500' },
  good: { bg: 'bg-blue-500', text: 'text-white', ring: 'ring-blue-500' },
  fair: { bg: 'bg-amber-500', text: 'text-gray-900', ring: 'ring-amber-500' },
  poor: { bg: 'bg-gray-500', text: 'text-white', ring: 'ring-gray-500' },
}
```

Available UI components: Badge, Button, Card (installed by Plan 02 Task 1), Separator, Skeleton
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign analysis components with professional visual hierarchy</name>
  <files>web/src/components/analysis/ScoreHeader.tsx, web/src/components/analysis/CategoryBreakdown.tsx, web/src/components/analysis/BulletSummary.tsx, web/src/components/analysis/ChecklistSection.tsx</files>
  <action>
Redesign all 4 analysis components for a professional, demo-ready appearance. Keep existing exports (getTierColor, getScoreColor, getStatusIndicator) for backward compatibility.

**1. ScoreHeader.tsx** -- redesign for visual impact:
- Larger score circle: 120x120px with ring/border effect (e.g., `ring-4` matching tier color)
- Score number in large bold text (text-4xl), "/100" in smaller muted text below
- Tier badge below the circle with capitalized label and subtle background
- Listing ID and Flatfox link in a separated row below
- Add profile name display if available (pass as optional prop `profileName?: string`)
- Centered layout with generous vertical spacing (py-10)
- Add subtle gradient or shadow to the score circle for depth
- Keep 'use client' directive

**2. CategoryBreakdown.tsx** -- redesign for clarity:
- Use shadcn Card component (installed by Plan 02) for each category instead of plain divs
- Category name as CardTitle with score badge (e.g., "85/100") right-aligned
- Score bar: taller (h-3), rounded, with smooth gradient transition
- Weight shown as a subtle "Weight: X%" label in muted text
- Reasoning bullets in an expandable/collapsible section (start expanded for top categories)
- Better spacing between categories (gap-4)
- Add importance level label if weight maps to a known level (20=low, 40=medium, 60=high, 80=critical -- approximate mapping for display)
- Responsive: single column on all screens (category cards stack vertically)

**3. BulletSummary.tsx** -- redesign as a highlight card:
- Use a visually distinct card: accent border-left (4px primary color) or subtle gradient background
- "Key Takeaways" as the heading (more impactful than "Key Points")
- Larger bullet icons or numbered list for visual weight
- Each bullet gets slightly more padding and a separator between items
- Subtle icon next to heading (e.g., Lightbulb from lucide-react)

**4. ChecklistSection.tsx** -- redesign for scannability:
- Group items by status: "Met" items first (green), then "Not Met" (red), then "Unknown" (gray)
- Use larger status icons with colored backgrounds (small colored circles behind the icons)
- Each item in its own card-like container with hover effect
- Count summary at the top: "5 met, 2 not met, 1 unknown" in a badge row
- Keep existing getStatusIndicator export

All components must maintain their existing prop interfaces for backward compatibility with the existing page. Add optional new props (like profileName) without breaking existing usage.
  </action>
  <verify>
    <automated>cd /Users/maximgusev/workspace/gen-ai-hackathon/web && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>All 4 analysis components redesigned with professional styling. ScoreHeader has prominent score display. CategoryBreakdown uses Card components. BulletSummary has accent styling. ChecklistSection groups by status. All existing exports preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign analysis page layout and build analyses list page</name>
  <files>web/src/app/(dashboard)/analysis/[listingId]/page.tsx, web/src/app/(dashboard)/analyses/page.tsx</files>
  <action>
**1. Redesign analysis/[listingId]/page.tsx:**
- Increase max width to `max-w-4xl` (from max-w-2xl) for more breathing room
- Layout: ScoreHeader full-width at top, then a 2-column grid on lg screens:
  - Left column (wider, ~60%): BulletSummary + CategoryBreakdown stacked
  - Right column (~40%): ChecklistSection as a sticky sidebar
  - On mobile: single column, natural stacking order
- Use `grid grid-cols-1 lg:grid-cols-[1fr_340px] gap-6` for the 2-column layout
- Back link: "Back to Analyses" pointing to /analyses (not /dashboard)
- If analysis has a profile_id, fetch profile name and pass to ScoreHeader
- Add timestamp display: "Scored on {formatted date}" below the ScoreHeader
- Add a subtle breadcrumb: Analyses > Listing {listingId}
- Maintain the "No analysis found" fallback with updated link to /analyses

**2. Build analyses/page.tsx** (server component):
- Auth guard: get user, redirect to / if not authenticated
- Fetch analyses: `supabase.from('analyses').select('id, listing_id, score, breakdown, profile_id, created_at').eq('user_id', user.id).order('created_at', { ascending: false })`
- Also fetch profiles for profile name lookup: `supabase.from('profiles').select('id, name')`
- Build a profile name map: `Map<string, string>` from profile id to name
- Display as a list/grid of analysis cards:
  - Each card shows: Listing ID, overall score with tier-colored badge, profile name (if available), date scored
  - Score displayed as colored badge matching tier (emerald/blue/amber/gray)
  - Card is clickable, navigating to `/analysis/{listing_id}`
- Responsive grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4`
- Empty state: "No analyses yet. Score listings on Flatfox using the browser extension to see results here."
- Page title: "Analyses" with subtitle "Your scored listings"
- Use shadcn Card component for each analysis entry
  </action>
  <verify>
    <automated>cd /Users/maximgusev/workspace/gen-ai-hackathon/web && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Analysis detail page has 2-column layout with better visual hierarchy. Analyses list page shows all scored listings as cards with score, tier, profile name, and date. Navigation between list and detail works. All TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
1. `cd web && npx tsc --noEmit` -- TypeScript compiles without errors
2. `cd web && pnpm build` -- Next.js production build succeeds
3. Analysis detail page renders with redesigned components and 2-column layout
4. Analyses list page renders cards for each scored listing
5. Navigation from list to detail and back works
6. Existing ScoreHeader, CategoryBreakdown, BulletSummary, ChecklistSection exports preserved
</verification>

<success_criteria>
- Analysis page has professional, demo-ready layout with clear visual hierarchy
- ScoreHeader displays large score circle with tier coloring and ring effect
- CategoryBreakdown uses Card components with score bars and expandable reasoning
- BulletSummary has accent styling and "Key Takeaways" heading
- ChecklistSection groups items by status with count summary
- 2-column layout on large screens, single column on mobile
- Analyses list page shows all scored listings with score badges
- Navigation between analyses list and individual analysis works
- No TypeScript errors, successful build
</success_criteria>

<output>
After completion, create `.planning/phases/09-web-profile-management/09-04-SUMMARY.md`
</output>
