---
phase: 09-web-profile-management
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/preferences/preferences-form.tsx
  - web/src/components/preferences/location-type-section.tsx
  - web/src/components/preferences/budget-section.tsx
  - web/src/components/preferences/size-rooms-section.tsx
  - web/src/components/preferences/features-section.tsx
  - web/src/components/preferences/soft-criteria-section.tsx
  - web/src/components/preferences/importance-section.tsx
autonomous: true
requirements: [PREF-11, PREF-12]

must_haves:
  truths:
    - "Preferences form has 6 accordion sections matching CONTEXT.md wireframe"
    - "Budget section has a dealbreaker checkbox labeled 'Hard limit -- score 0 if over'"
    - "Size & Rooms section has a dealbreaker checkbox for rooms and living space"
    - "Importance chips (Low/Medium/High/Critical) are button-style selectors, not sliders"
    - "Features section shows predefined feature chips from FEATURE_SUGGESTIONS"
    - "Floor preference dropdown/radio with Any/Ground/Not Ground options"
    - "Availability field is present in the Features & Availability section"
  artifacts:
    - path: "web/src/components/preferences/preferences-form.tsx"
      provides: "Restructured form with 6 accordion sections and profile-aware save"
      min_lines: 50
    - path: "web/src/components/preferences/location-type-section.tsx"
      provides: "Section 1: Location, Offer Type, Property Type"
      exports: ["LocationTypeSection"]
    - path: "web/src/components/preferences/budget-section.tsx"
      provides: "Section 2: Budget range with dealbreaker checkbox"
      exports: ["BudgetSection"]
    - path: "web/src/components/preferences/size-rooms-section.tsx"
      provides: "Section 3: Rooms, Living Space, Floor Preference with dealbreaker checkboxes"
      exports: ["SizeRoomsSection"]
    - path: "web/src/components/preferences/features-section.tsx"
      provides: "Section 4: Feature chips and Availability"
      exports: ["FeaturesSection"]
    - path: "web/src/components/preferences/soft-criteria-section.tsx"
      provides: "Section 5: Free text soft criteria tags"
      exports: ["SoftCriteriaSection"]
    - path: "web/src/components/preferences/importance-section.tsx"
      provides: "Section 6: Importance level chips per category"
      exports: ["ImportanceSection"]
  key_links:
    - from: "web/src/components/preferences/preferences-form.tsx"
      to: "all 6 section components"
      via: "import and render inside Accordion"
      pattern: "import.*Section.*from"
    - from: "web/src/components/preferences/budget-section.tsx"
      to: "budgetDealbreaker form field"
      via: "FormField with Checkbox control"
      pattern: "budgetDealbreaker"
    - from: "web/src/components/preferences/size-rooms-section.tsx"
      to: "roomsDealbreaker and livingSpaceDealbreaker form fields"
      via: "FormField with Checkbox control"
      pattern: "(roomsDealbreaker|livingSpaceDealbreaker)"
---

<objective>
Restructure the preferences form from 3 accordion sections into 6 sections matching the CONTEXT.md wireframe. Add dealbreaker checkboxes on budget and size sections, add floor preference and availability fields, and split the monolithic StandardFilters into focused section components.

Purpose: The existing form lacks dealbreaker toggles, floor preference, and availability fields. It lumps all standard filters into one section. This restructure aligns the form with the canonical schema and makes dealbreakers (PREF-11) and importance chips (PREF-12) first-class UI elements.
Output: 6 new section components and a restructured preferences-form.tsx.
</objective>

<execution_context>
@/Users/maximgusev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maximgusev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-web-profile-management/09-CONTEXT.md
@.planning/phases/09-web-profile-management/09-RESEARCH.md

@web/src/components/preferences/preferences-form.tsx
@web/src/components/preferences/standard-filters.tsx
@web/src/components/preferences/soft-criteria.tsx
@web/src/components/preferences/weight-sliders.tsx
@web/src/lib/schemas/preferences.ts
@web/src/lib/constants/features.ts
@web/src/components/ui/accordion.tsx

<interfaces>
<!-- Key types and contracts the executor needs -->

From web/src/lib/schemas/preferences.ts:
```typescript
export type Preferences = z.infer<typeof preferencesSchema>
// Fields: location, offerType, objectCategory, budgetMin, budgetMax, roomsMin, roomsMax,
// livingSpaceMin, livingSpaceMax, budgetDealbreaker, roomsDealbreaker, livingSpaceDealbreaker,
// floorPreference ('any'|'ground'|'not_ground'), availability, features (string[]),
// softCriteria (string[]), importance ({location, price, size, features, condition}), language
```

From web/src/lib/constants/features.ts:
```typescript
export const FEATURE_SUGGESTIONS = [
  { value: 'balconygarden', label: 'Balcony / Garden' },
  { value: 'parkingspace', label: 'Parking Space' },
  { value: 'garage', label: 'Garage' },
  { value: 'lift', label: 'Elevator' },
  { value: 'dishwasher', label: 'Dishwasher' },
  { value: 'washingmachine', label: 'Washing Machine' },
  { value: 'petsallowed', label: 'Pets Allowed' },
  { value: 'minergie', label: 'Minergie (Energy Efficient)' },
  { value: 'parquetflooring', label: 'Parquet Flooring' },
  { value: 'view', label: 'View' },
  { value: 'cable', label: 'Cable TV' },
  { value: 'accessiblewithwheelchair', label: 'Wheelchair Accessible' },
] as const
```

Existing section component pattern (form prop):
```typescript
interface SectionProps {
  form: UseFormReturn<Preferences>
}
```

shadcn components available: Accordion, AccordionItem, AccordionTrigger, AccordionContent, Badge, Button, Form (FormField, FormControl, FormItem, FormLabel, FormMessage), Input, RadioGroup, RadioGroupItem, Select, SelectContent, SelectItem, SelectTrigger, SelectValue

NOTE: shadcn Checkbox and Switch are NOT yet installed. Install them first:
```bash
cd web && pnpm dlx shadcn@latest add checkbox field
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn checkbox and field components</name>
  <files>web/src/components/ui/checkbox.tsx, web/src/components/ui/field.tsx</files>
  <action>
Install the shadcn checkbox and field components needed for dealbreaker toggles:

```bash
cd web && pnpm dlx shadcn@latest add checkbox field
```

Verify the files are created at `web/src/components/ui/checkbox.tsx` and `web/src/components/ui/field.tsx`.

Also install card, alert-dialog, and dialog components needed by Plan 03 (doing it now to avoid duplicate install steps):

```bash
cd web && pnpm dlx shadcn@latest add card alert-dialog dialog
```

This is a pure infrastructure step. No code modifications beyond what shadcn generates.
  </action>
  <verify>
    <automated>ls /Users/maximgusev/workspace/gen-ai-hackathon/web/src/components/ui/checkbox.tsx /Users/maximgusev/workspace/gen-ai-hackathon/web/src/components/ui/field.tsx /Users/maximgusev/workspace/gen-ai-hackathon/web/src/components/ui/card.tsx /Users/maximgusev/workspace/gen-ai-hackathon/web/src/components/ui/alert-dialog.tsx /Users/maximgusev/workspace/gen-ai-hackathon/web/src/components/ui/dialog.tsx</automated>
  </verify>
  <done>All 5 shadcn components installed: checkbox, field, card, alert-dialog, dialog.</done>
</task>

<task type="auto">
  <name>Task 2: Create 6 form section components and restructure preferences form</name>
  <files>web/src/components/preferences/location-type-section.tsx, web/src/components/preferences/budget-section.tsx, web/src/components/preferences/size-rooms-section.tsx, web/src/components/preferences/features-section.tsx, web/src/components/preferences/soft-criteria-section.tsx, web/src/components/preferences/importance-section.tsx, web/src/components/preferences/preferences-form.tsx</files>
  <action>
Create 6 new section components. Each is 'use client' and receives `{ form: UseFormReturn<Preferences> }` as props. The existing `standard-filters.tsx`, `soft-criteria.tsx`, and `weight-sliders.tsx` files remain for backward compatibility but are superseded by these new files.

**1. location-type-section.tsx** -- Section "Location & Type"
- Extract Location input, Offer Type radio group, and Property Type select from existing `standard-filters.tsx`
- Same field names: `location`, `offerType`, `objectCategory`
- Use existing FormField/FormControl/FormItem/FormLabel pattern

**2. budget-section.tsx** -- Section "Budget" with dealbreaker
- Extract Budget Min/Max inputs from `standard-filters.tsx`
- ADD dealbreaker checkbox: `<FormField name="budgetDealbreaker">` using shadcn Checkbox component
- Checkbox label: "Hard limit -- score 0 if over budget"
- Layout: budget min/max inputs in a flex row, checkbox below with label
- Use the Checkbox from `@/components/ui/checkbox` (just installed)
- For accessible labeling: wrap in a `<div className="flex items-center gap-2">` with an explicit `<label>` element using `onClick` to toggle, and add `aria-labelledby` on the Checkbox

**3. size-rooms-section.tsx** -- Section "Size & Rooms" with dealbreakers
- Extract Rooms Min/Max and Living Space Min/Max from `standard-filters.tsx`
- ADD `roomsDealbreaker` checkbox: "Hard limit -- score 0 if below minimum rooms"
- ADD `livingSpaceDealbreaker` checkbox: "Hard limit -- score 0 if below minimum space"
- ADD Floor Preference: radio group or select with values 'any', 'ground', 'not_ground' and labels "Any floor", "Ground floor only", "Not ground floor"
- Layout: rooms row, rooms dealbreaker, living space row, living space dealbreaker, floor preference

**4. features-section.tsx** -- Section "Features & Availability"
- Extract the feature chips (Badge toggle pattern) from existing `soft-criteria.tsx`
- ADD Availability field: a Select dropdown with options "Any time", "Immediately", "Within 1 month", "Within 3 months", "Specific date". Values: "any", "immediately", "1month", "3months", "specific"
- Import FEATURE_SUGGESTIONS from `@/lib/constants/features`
- Same Badge toggle pattern as existing

**5. soft-criteria-section.tsx** -- Section "Soft Criteria"
- Extract the free-text custom criteria from existing `soft-criteria.tsx`
- Same add/remove/update pattern with Input fields and X buttons
- Unchanged behavior, just moved to its own file

**6. importance-section.tsx** -- Section "What Matters Most"
- Copy content from existing `weight-sliders.tsx` (already implements importance chips correctly)
- Rename component to `ImportanceSection`
- Same IMPORTANCE_CATEGORIES and IMPORTANCE_LEVELS constants
- Same button-style chip selector pattern (already correct from Phase 8)

**Restructure preferences-form.tsx:**
- Replace existing 3-section Accordion with 6-section Accordion
- CRITICAL: Add explicit string `value` props to each AccordionItem to fix the Base UI defaultValue bug: `<AccordionItem value="location">`, `<AccordionItem value="budget">`, etc.
- Set `defaultValue={["location", "budget", "size", "features", "soft", "importance"]}` (all open by default)
- Import all 6 new section components
- Remove imports of StandardFilters, SoftCriteria, WeightSliders
- Update the component props to accept `profileId?: string` and `profileName?: string` for future profile-aware behavior (Plan 03 will use these)
- Keep the existing `onSave` prop pattern for now -- Plan 03 will refactor to use saveProfilePreferences server action
- Keep the existing save button and message display

Section order in the Accordion:
1. Location & Type
2. Budget
3. Size & Rooms
4. Features & Availability
5. Soft Criteria
6. What Matters Most
  </action>
  <verify>
    <automated>cd /Users/maximgusev/workspace/gen-ai-hackathon/web && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Preferences form renders 6 accordion sections. Budget and Size sections show dealbreaker checkboxes. Floor preference and availability fields are present. All 6 sections compile without TypeScript errors. Importance chips remain functional (Low/Medium/High/Critical button group).</done>
</task>

</tasks>

<verification>
1. `cd web && npx tsc --noEmit` -- TypeScript compiles without errors
2. `cd web && pnpm build` -- Next.js production build succeeds
3. All 6 section components exist and export their named components
4. preferences-form.tsx imports and renders all 6 sections
5. shadcn checkbox, field, card, alert-dialog, dialog components installed
</verification>

<success_criteria>
- 6 accordion sections rendered in the preferences form matching CONTEXT.md wireframe
- Dealbreaker checkboxes on Budget (budgetDealbreaker) and Size/Rooms (roomsDealbreaker, livingSpaceDealbreaker)
- Floor preference field with 3 options (any/ground/not_ground)
- Availability dropdown field in Features section
- Importance chips (Low/Medium/High/Critical) in dedicated section
- All shadcn components installed for this plan and Plan 03
- No TypeScript errors, successful build
</success_criteria>

<output>
After completion, create `.planning/phases/09-web-profile-management/09-02-SUMMARY.md`
</output>
