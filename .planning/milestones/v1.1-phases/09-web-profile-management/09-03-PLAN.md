---
phase: 09-web-profile-management
plan: 03
type: execute
wave: 2
depends_on: [09-01, 09-02]
files_modified:
  - web/src/app/(dashboard)/profiles/page.tsx
  - web/src/app/(dashboard)/profiles/[profileId]/page.tsx
  - web/src/components/profiles/profile-card.tsx
  - web/src/components/profiles/profile-list.tsx
  - web/src/components/profiles/create-profile-dialog.tsx
  - web/src/components/profiles/rename-profile-dialog.tsx
  - web/src/components/profiles/delete-profile-dialog.tsx
  - web/src/components/preferences/profile-summary.tsx
  - web/src/app/(dashboard)/dashboard/page.tsx
  - web/src/components/top-navbar.tsx
autonomous: true
requirements: [PROF-06, PREF-15]

must_haves:
  truths:
    - "Profile list page shows cards for all user profiles with name, key criteria summary, and active star badge"
    - "User can create a new profile via dialog on the profiles page"
    - "User can rename a profile via dialog from the card's dropdown menu"
    - "User can duplicate a profile from the card's dropdown menu"
    - "User can delete a profile with AlertDialog confirmation (blocked if last profile)"
    - "User can set a profile as active from its card"
    - "Clicking Edit on a profile card navigates to /profiles/[profileId] with the preferences form"
    - "Preferences form on edit page saves to the specific profile and shows live summary"
    - "/dashboard redirects to /profiles"
  artifacts:
    - path: "web/src/app/(dashboard)/profiles/page.tsx"
      provides: "Profile list page with server-side profile fetch"
      min_lines: 20
    - path: "web/src/app/(dashboard)/profiles/[profileId]/page.tsx"
      provides: "Edit profile page with preferences form loaded for specific profile"
      min_lines: 30
    - path: "web/src/components/profiles/profile-card.tsx"
      provides: "Single profile card with actions"
      exports: ["ProfileCard"]
    - path: "web/src/components/profiles/profile-list.tsx"
      provides: "Grid of profile cards with CRUD callbacks"
      exports: ["ProfileList"]
    - path: "web/src/components/profiles/create-profile-dialog.tsx"
      provides: "Dialog for creating a new profile with name input"
      exports: ["CreateProfileDialog"]
    - path: "web/src/components/profiles/rename-profile-dialog.tsx"
      provides: "Dialog for renaming a profile"
      exports: ["RenameProfileDialog"]
    - path: "web/src/components/profiles/delete-profile-dialog.tsx"
      provides: "AlertDialog for delete confirmation"
      exports: ["DeleteProfileDialog"]
    - path: "web/src/components/preferences/profile-summary.tsx"
      provides: "Live profile summary component using form.watch()"
      exports: ["ProfileSummary"]
  key_links:
    - from: "web/src/components/profiles/profile-list.tsx"
      to: "web/src/app/(dashboard)/profiles/actions.ts"
      via: "server action calls for CRUD operations"
      pattern: "(createProfile|renameProfile|deleteProfile|duplicateProfile|setActiveProfile)"
    - from: "web/src/app/(dashboard)/profiles/page.tsx"
      to: "supabase profiles table"
      via: "server-side query for all profiles"
      pattern: "supabase\\.from\\('profiles'\\)"
    - from: "web/src/app/(dashboard)/profiles/[profileId]/page.tsx"
      to: "web/src/components/preferences/preferences-form.tsx"
      via: "renders PreferencesForm with profileId and profile data"
      pattern: "PreferencesForm.*profileId"
    - from: "web/src/components/preferences/profile-summary.tsx"
      to: "web/src/lib/profile-summary.ts"
      via: "calls generateProfileSummary with watched form values"
      pattern: "generateProfileSummary"
---

<objective>
Build the profile list page with card CRUD UI and the profile edit page. Wire the preferences form to save per-profile using server actions from Plan 01, and add the live profile summary preview using the utility from Plan 01 and the restructured form from Plan 02.

Purpose: This is the primary user-facing deliverable of the phase -- the page where users manage all their search profiles. It consumes the server actions from Plan 01 and the restructured form from Plan 02.
Output: Working profile list page with cards, all CRUD dialogs, edit profile page with live summary.
</objective>

<execution_context>
@/Users/maximgusev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maximgusev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-web-profile-management/09-CONTEXT.md
@.planning/phases/09-web-profile-management/09-RESEARCH.md
@.planning/phases/09-web-profile-management/09-01-SUMMARY.md
@.planning/phases/09-web-profile-management/09-02-SUMMARY.md

@web/src/app/(dashboard)/profiles/actions.ts
@web/src/lib/profile-summary.ts
@web/src/components/preferences/preferences-form.tsx

<interfaces>
<!-- From Plan 01: Server actions -->
From web/src/app/(dashboard)/profiles/actions.ts:
```typescript
export async function createProfile(name: string): Promise<string>
export async function renameProfile(profileId: string, newName: string): Promise<void>
export async function deleteProfile(profileId: string): Promise<void>
export async function duplicateProfile(sourceProfileId: string): Promise<string>
export async function setActiveProfile(profileId: string): Promise<void>
export async function saveProfilePreferences(profileId: string, data: Preferences): Promise<void>
```

From web/src/lib/profile-summary.ts:
```typescript
export function generateProfileSummary(prefs: Partial<Preferences>): string
```

<!-- From Plan 02: Restructured form -->
From web/src/components/preferences/preferences-form.tsx:
```typescript
interface PreferencesFormProps {
  defaultValues: Preferences
  onSave: (data: Preferences) => Promise<void>
  profileId?: string
  profileName?: string
}
export function PreferencesForm({ defaultValues, onSave, profileId, profileName }: PreferencesFormProps)
```

From web/src/lib/schemas/preferences.ts:
```typescript
export const preferencesSchema: z.ZodObject<...>
export type Preferences = z.infer<typeof preferencesSchema>
```

<!-- shadcn components from Plan 02 Task 1 -->
Available: Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter, CardAction
Available: AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle
Available: Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle, DialogTrigger
Available: DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem, DropdownMenuSeparator
Available: Badge, Button, Input
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build profile card, dialog components, and profile list</name>
  <files>web/src/components/profiles/profile-card.tsx, web/src/components/profiles/profile-list.tsx, web/src/components/profiles/create-profile-dialog.tsx, web/src/components/profiles/rename-profile-dialog.tsx, web/src/components/profiles/delete-profile-dialog.tsx</files>
  <action>
Create the `web/src/components/profiles/` directory and build 5 components:

**1. profile-card.tsx** (client component):
- Props: `profile: { id: string; name: string; is_default: boolean; preferences: Record<string, unknown> }`, `isOnly: boolean`, callbacks: `onEdit`, `onSetActive`, `onRename`, `onDuplicate`, `onDelete`
- Use shadcn Card with CardHeader (profile name + star icon if is_default), CardDescription (summary line), CardContent (budget/rooms), CardFooter (Edit button + Active badge or "Set Active" button)
- CardAction slot: DropdownMenu with MoreHorizontal trigger containing Rename, Duplicate, separator, Delete (disabled if `isOnly`)
- Summary derived inline: offer type + location, budget range, rooms range
- Star icon: `<Star className="size-4 text-amber-500 fill-amber-500" />` from lucide-react
- Icons: Star, MoreHorizontal, Copy, Pencil, Trash2 from lucide-react

**2. create-profile-dialog.tsx** (client component):
- Props: `open: boolean`, `onOpenChange: (open: boolean) => void`, `onCreate: (name: string) => void`
- Dialog with title "Create New Profile"
- Input field for profile name with placeholder "e.g., Zurich Family Apartment"
- Create and Cancel buttons in DialogFooter
- On create: validate name is not empty, call `onCreate(name)`, close dialog, clear input
- Auto-focus the input when dialog opens

**3. rename-profile-dialog.tsx** (client component):
- Props: `open: boolean`, `onOpenChange: (open: boolean) => void`, `currentName: string`, `onRename: (newName: string) => void`
- Dialog with title "Rename Profile"
- Input pre-filled with currentName
- Save and Cancel buttons
- On save: validate name not empty, call `onRename(newName)`, close dialog

**4. delete-profile-dialog.tsx** (client component):
- Props: `open: boolean`, `onOpenChange: (open: boolean) => void`, `profileName: string`, `onConfirm: () => void`
- AlertDialog (NOT Dialog -- AlertDialog prevents accidental closure)
- Title: `Delete "{profileName}"?`
- Description: "This will permanently delete this profile and all its preferences. Analyses scored with this profile will remain."
- Cancel and "Delete Profile" buttons (delete button should have destructive styling)

**5. profile-list.tsx** (client component):
- Props: `profiles: Profile[]` (array of profile objects with id, name, is_default, preferences)
- Uses `useTransition` and `useRouter` from Next.js for server action calls
- State for open dialogs: `createOpen`, `renameOpen` (with target profile), `deleteOpen` (with target profile)
- Renders a responsive grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4`
- Maps profiles to ProfileCard components
- Has a "New Profile" button (use ShimmerButton from Plan 08 if desired, or standard Button with Plus icon) that opens CreateProfileDialog
- Handles all CRUD callbacks:
  - `handleCreate(name)`: call `createProfile(name)` server action
  - `handleRename(id, newName)`: call `renameProfile(id, newName)` server action
  - `handleDelete(id)`: call `deleteProfile(id)` server action
  - `handleDuplicate(id)`: call `duplicateProfile(id)` server action
  - `handleSetActive(id)`: call `setActiveProfile(id)` server action
  - `handleEdit(id)`: `router.push(/profiles/${id})`
- All server action calls wrapped in `startTransition` for pending state
- Show toast or error message if server action throws (catch block with console.error for MVP; consider adding error state)
- Empty state: if profiles.length === 0, show "Create your first profile" message with prominent CTA button
  </action>
  <verify>
    <automated>cd /Users/maximgusev/workspace/gen-ai-hackathon/web && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>ProfileCard renders profile data with star badge, summary, and action menu. All 3 dialog components open/close correctly. ProfileList renders a grid of cards and handles CRUD via server actions. Empty state shown when no profiles exist.</done>
</task>

<task type="auto">
  <name>Task 2: Build profiles page, edit page, live summary, and redirect /dashboard</name>
  <files>web/src/app/(dashboard)/profiles/page.tsx, web/src/app/(dashboard)/profiles/[profileId]/page.tsx, web/src/components/preferences/profile-summary.tsx, web/src/app/(dashboard)/dashboard/page.tsx, web/src/components/top-navbar.tsx</files>
  <action>
**1. Rewrite profiles/page.tsx** (server component):
- Remove the placeholder content and ShimmerButton
- Auth guard: get user, redirect to / if not authenticated
- Fetch all profiles: `supabase.from('profiles').select('id, name, is_default, preferences').order('created_at', { ascending: true })`
- Render page header: "Profiles" title and "Manage your search profiles" subtitle
- Render `<ProfileList profiles={profiles ?? []} />`

**2. Create profiles/[profileId]/page.tsx** (server component):
- Props: `{ params: Promise<{ profileId: string }> }` (Next.js 16 async params pattern)
- Auth guard: get user, redirect to / if not authenticated
- Fetch profile by ID: `supabase.from('profiles').select('id, name, preferences').eq('id', profileId).single()`
- If profile not found, redirect to /profiles
- Parse preferences: `preferencesSchema.parse(profile.preferences ?? {})`
- Import `saveProfilePreferences` from the profiles actions
- Create a bound save function: `async function handleSave(data: Preferences) { 'use server'; await saveProfilePreferences(profile.id, data) }`
  - IMPORTANT: Actually, since saveProfilePreferences is already a server action, just create a wrapper: `const handleSave = saveProfilePreferences.bind(null, profile.id)` -- but this won't work because saveProfilePreferences takes (profileId, data) and bind would partially apply only the first arg. Instead, pass both profileId and onSave as separate props to PreferencesForm.
  - UPDATE: The simpler approach: update PreferencesForm to accept an optional `profileId` prop. If profileId is set, the form's handleSubmit calls `saveProfilePreferences(profileId, data)` instead of `onSave(data)`. For backward compat, keep `onSave` as fallback.
- Render: Back link to /profiles, profile name as h1, `<PreferencesForm profileId={profile.id} profileName={profile.name} defaultValues={defaults} onSave={...} />`
- Below or beside the form, render `<ProfileSummary form={form} />` -- but since PreferencesForm is a client component and ProfileSummary needs form.watch(), the ProfileSummary must be rendered INSIDE PreferencesForm

**3. Update preferences-form.tsx** to integrate live summary:
- If `profileId` is provided, the save handler calls `saveProfilePreferences(profileId, data)` (imported from profiles/actions)
- If `profileId` is NOT provided (legacy /dashboard usage), fall back to `onSave` prop
- Render `<ProfileSummary />` component at the top of the form (above accordion) when profileId is set
- ProfileSummary watches form values and shows the live summary
- Add a "Back to Profiles" link when profileId is set

**4. Create profile-summary.tsx** (client component):
- Props: `{ form: UseFormReturn<Preferences> }`
- Uses `form.watch()` to reactively track all form values
- Calls `generateProfileSummary()` from `@/lib/profile-summary.ts` with watched values
- Renders in a styled card/box: subtle background, italic text, "Profile Summary" header
- Updates in real-time as user changes form fields
- Use `useMemo` to avoid recalculating on every render (only when watched values change -- though watch() returns new refs each time, so memoize by stringifying or just compute directly)

**5. Update dashboard/page.tsx:**
- Replace current content with a redirect to /profiles: `import { redirect } from 'next/navigation'; export default function DashboardPage() { redirect('/profiles') }`
- This ensures old bookmark/navigation to /dashboard lands on the profiles list

**6. Update top-navbar.tsx:**
- Change the "Preferences" nav item to point to `/profiles` instead of `/dashboard`
- Rename the label from "Preferences" to "Profiles" (since that's now the main entry point)
- Update the isActive check accordingly
  </action>
  <verify>
    <automated>cd /Users/maximgusev/workspace/gen-ai-hackathon/web && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Profiles page shows cards for all profiles with CRUD actions. Clicking Edit navigates to /profiles/[profileId] showing the restructured preferences form with live summary. /dashboard redirects to /profiles. Navbar "Profiles" link goes to /profiles. All TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
1. `cd web && npx tsc --noEmit` -- TypeScript compiles without errors
2. `cd web && pnpm build` -- Next.js production build succeeds
3. /profiles page renders profile cards from database
4. Create, rename, duplicate, delete operations work via dialogs
5. /profiles/[id] renders preferences form for that specific profile
6. Profile summary updates live as form fields change
7. /dashboard redirects to /profiles
</verification>

<success_criteria>
- Profile list page at /profiles shows cards with name, summary, active badge, and CRUD actions
- Create dialog creates new profile with name
- Rename dialog updates profile name
- Delete dialog confirms and blocks last-profile deletion
- Duplicate creates copy with "(Copy)" suffix
- Set Active calls RPC and refreshes navbar
- Edit navigates to /profiles/[id] with preferences form pre-filled
- Live profile summary below form header updates as user types
- /dashboard redirects to /profiles
- Navbar links updated to point to /profiles
</success_criteria>

<output>
After completion, create `.planning/phases/09-web-profile-management/09-03-SUMMARY.md`
</output>
