---
phase: 10-extension-profile-switcher
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - extension/src/components/popup/Dashboard.tsx
  - extension/src/components/popup/ProfileSection.tsx
  - extension/src/components/popup/ConnectionStatus.tsx
autonomous: true
requirements: [EXT-09, EXT-10, EXT-11]

must_haves:
  truths:
    - "Popup shows active profile name immediately on open"
    - "User can switch profiles from dropdown without leaving Flatfox"
    - "Popup shows a Connected/Disconnected indicator reflecting session health"
  artifacts:
    - path: "extension/src/components/popup/ProfileSection.tsx"
      provides: "Profile display + switcher dropdown"
      exports: ["ProfileSection"]
      min_lines: 40
    - path: "extension/src/components/popup/ConnectionStatus.tsx"
      provides: "Session health indicator dot + label"
      exports: ["ConnectionStatus"]
      min_lines: 10
    - path: "extension/src/components/popup/Dashboard.tsx"
      provides: "Popup dashboard with profile section and health indicator"
      contains: "ProfileSection"
  key_links:
    - from: "extension/src/components/popup/Dashboard.tsx"
      to: "browser.runtime.sendMessage"
      via: "getProfiles and healthCheck messages on mount"
      pattern: "action.*getProfiles|action.*healthCheck"
    - from: "extension/src/components/popup/ProfileSection.tsx"
      to: "browser.runtime.sendMessage"
      via: "switchProfile message on select change"
      pattern: "action.*switchProfile"
    - from: "extension/src/components/popup/ProfileSection.tsx"
      to: "extension/src/storage/active-profile.ts"
      via: "updates activeProfileStorage after successful switch"
      pattern: "activeProfileStorage\\.setValue"
---

<objective>
Build the popup UI for profile display, switching, and session health. The popup shows the active profile name, provides a dropdown to switch profiles, and displays a connection status indicator.

Purpose: Gives users profile awareness and control directly in the extension popup (EXT-09, EXT-10, EXT-11). Profile switching updates the WXT storage item that the content script watches (enabling EXT-12 in Plan 03).
Output: ProfileSection and ConnectionStatus components, refactored Dashboard.tsx.
</objective>

<execution_context>
@/Users/maximgusev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maximgusev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-extension-profile-switcher/10-RESEARCH.md
@.planning/phases/10-extension-profile-switcher/10-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that this plan consumes -->

From extension/src/storage/active-profile.ts (created in Plan 01):
```typescript
export interface ActiveProfileData {
  id: string;
  name: string;
}
export const activeProfileStorage = storage.defineItem<ActiveProfileData | null>(
  'local:activeProfile',
  { fallback: null },
);
```

From extension/src/entrypoints/background.ts (extended in Plan 01):
```typescript
export type ExtMessage =
  | { action: 'signIn'; credentials: { email: string; password: string } }
  | { action: 'signOut' }
  | { action: 'getSession' }
  | { action: 'getUser' }
  | { action: 'getProfiles' }
  | { action: 'switchProfile'; profileId: string }
  | { action: 'healthCheck' };
// getProfiles returns: { profiles: { id: string; name: string; is_default: boolean }[], error }
// switchProfile returns: { error }
// healthCheck returns: { connected: boolean, email: string | null }
```

From extension/src/components/popup/Dashboard.tsx (existing):
```typescript
// Current structure: loading state -> login form -> dashboard
// Dashboard has: header, user info card, action buttons (Edit Preferences, View Analyses), sign out
// Width: min-w-[320px] max-w-[380px]
```

From extension/src/components/ui/ (already installed Radix primitives):
```typescript
// Available: Button, Card, CardContent, Separator
// Also installed: @radix-ui/react-select (for dropdown)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ProfileSection and ConnectionStatus components</name>
  <files>extension/src/components/popup/ProfileSection.tsx, extension/src/components/popup/ConnectionStatus.tsx</files>
  <action>
1. Create `extension/src/components/popup/ConnectionStatus.tsx`:
   - Props: `{ isConnected: boolean }`
   - Render a small flex row: colored dot (w-2 h-2 rounded-full, `bg-emerald-500` when connected, `bg-red-400` when disconnected) + text label ("Connected" / "Disconnected") in `text-xs text-muted-foreground`
   - Keep it minimal -- just a status indicator

2. Create `extension/src/components/popup/ProfileSection.tsx`:
   - Props: `{ profiles: Profile[]; activeProfileId: string | null; onSwitch: (profileId: string) => void; isLoading: boolean }`
   - Define local `Profile` interface: `{ id: string; name: string; is_default: boolean }`
   - Render inside a `Card` with `CardContent className="px-4 py-3"`:
     - Label: `<p className="text-xs text-muted-foreground mb-1.5">Active Profile</p>`
     - If `isLoading`, show a small pulse skeleton (h-8 w-full rounded bg-muted animate-pulse)
     - If no profiles, show "No profiles found" text with a link to create one on the website
     - If profiles exist, render a native `<select>` element (not Radix Select -- simpler, works reliably in popup, avoids portal issues in popup context):
       - `value={activeProfileId ?? ''}`
       - `onChange={(e) => onSwitch(e.target.value)}`
       - `className="w-full rounded-lg border border-input bg-background px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-ring"`
       - Map each profile to `<option key={p.id} value={p.id}>{p.name}</option>`
   - Use native `<select>` instead of Radix Select because Radix portals can behave unexpectedly in extension popups (fixed-size iframe context). Native select is reliable and accessible.
  </action>
  <verify>
    <automated>cd /Users/maximgusev/workspace/gen-ai-hackathon/extension && npx vitest run -x</automated>
  </verify>
  <done>
    - ProfileSection.tsx renders a profile dropdown with native select
    - ConnectionStatus.tsx renders a colored dot with status text
    - Both components exported and importable
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire ProfileSection, ConnectionStatus, and profile logic into Dashboard</name>
  <files>extension/src/components/popup/Dashboard.tsx</files>
  <action>
Refactor `extension/src/components/popup/Dashboard.tsx` to add profile awareness and health check:

1. Add imports:
   - `import { ProfileSection } from './ProfileSection'`
   - `import { ConnectionStatus } from './ConnectionStatus'`
   - `import { activeProfileStorage } from '@/storage/active-profile'`

2. Add state:
   - `const [profiles, setProfiles] = useState<{ id: string; name: string; is_default: boolean }[]>([])`
   - `const [activeProfileId, setActiveProfileId] = useState<string | null>(null)`
   - `const [isConnected, setIsConnected] = useState(false)`
   - `const [profilesLoading, setProfilesLoading] = useState(true)`

3. Add `fetchProfiles` function:
   - Send `{ action: 'getProfiles' }` via `browser.runtime.sendMessage`
   - Set profiles from response
   - Find the active profile (`is_default === true`), set `activeProfileId`
   - Update `activeProfileStorage.setValue({ id: active.id, name: active.name })` so the content script has the initial active profile
   - Set `profilesLoading = false`

4. Add `handleHealthCheck` function:
   - Send `{ action: 'healthCheck' }` via `browser.runtime.sendMessage`
   - Set `isConnected` from response

5. Update `useEffect` to call `fetchProfiles()` and `handleHealthCheck()` after session is loaded (only when authenticated)

6. Add `handleProfileSwitch` function:
   - Send `{ action: 'switchProfile', profileId }` via `browser.runtime.sendMessage`
   - On success (no error): update local profiles state (toggle is_default flags), set `activeProfileId`, update `activeProfileStorage.setValue({ id, name })` -- this triggers the content script watcher
   - On error: could show brief error (console.error is fine for now)

7. Update the authenticated dashboard layout:
   - Keep header ("HomeMatch" title)
   - Add `<ConnectionStatus isConnected={isConnected} />` next to the header (in the flex justify-between row)
   - Replace the existing user info Card with `<ProfileSection profiles={profiles} activeProfileId={activeProfileId} onSwitch={handleProfileSwitch} isLoading={profilesLoading} />`
   - Below ProfileSection, add a small `<p className="text-xs text-muted-foreground">Signed in as {session.user.email}</p>` (move email display to a subtle line)
   - Keep the Separator and action buttons (Edit Preferences, View Analyses)
   - Update "Edit Preferences" to open `https://homematch-web.vercel.app/profiles` (not /dashboard)
   - Keep Sign Out button

8. Keep the entire popup within `min-w-[320px] max-w-[380px]` constraints. Test with long profile names by using `truncate` class on the select text.
  </action>
  <verify>
    <automated>cd /Users/maximgusev/workspace/gen-ai-hackathon/extension && npx vitest run -x</automated>
  </verify>
  <done>
    - Popup shows active profile name in a dropdown when authenticated
    - Switching profiles sends switchProfile message and updates activeProfileStorage
    - ConnectionStatus dot visible in header area
    - Session email shown as subtitle text
    - Edit Preferences links to /profiles
    - All existing tests pass
  </done>
</task>

</tasks>

<verification>
1. Build the extension: `cd /Users/maximgusev/workspace/gen-ai-hackathon/extension && npm run build`
2. Run tests: `cd /Users/maximgusev/workspace/gen-ai-hackathon/extension && npx vitest run`
3. Visual: Load unpacked extension, open popup, verify profile dropdown and connection dot appear
</verification>

<success_criteria>
- Popup shows active profile name immediately on open (EXT-09)
- Dropdown lets user change active profile, which calls switchProfile and updates activeProfileStorage (EXT-10)
- Connection status indicator visible in popup header (EXT-11)
- All tests pass, extension builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-extension-profile-switcher/10-02-SUMMARY.md`
</output>
