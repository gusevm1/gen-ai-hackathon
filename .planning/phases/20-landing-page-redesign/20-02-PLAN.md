---
phase: 20-landing-page-redesign
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - web/src/components/landing/ChapterHook.tsx
  - web/src/components/landing/ChapterSwitzerland.tsx
  - web/src/components/landing/ChapterProblem.tsx
  - web/src/components/landing/ChapterMechanism.tsx
  - web/src/components/landing/ChapterScore.tsx
  - web/src/components/landing/ChapterDream.tsx
  - web/src/components/landing/ChapterCTA.tsx
  - web/src/components/landing/IsometricHome.tsx
  - web/src/components/landing/SectionHero.tsx
  - web/src/components/landing/SectionGlobe.tsx
autonomous: true
requirements: [LP-01, LP-02, LP-05]

must_haves:
  truths:
    - "SectionHero renders: HomeMatch logotype/wordmark, headline, subtitle, single 'Get Started' CTA button linking to /auth"
    - "Hero content animates on mount (not whileInView) — logo fades in at 0.1s, headline at 0.3s, subtitle at 0.5s, CTA at 0.7s"
    - "SectionGlobe renders: SVG globe draws in via pathLength, Switzerland polygon transitions to teal fill, glow ring appears after draw-in"
    - "Both components use useReducedMotion() — when true, skip transitions and show final state immediately"
    - "All 8 old Chapter/IsometricHome files are deleted from the filesystem"
  artifacts:
    - path: "web/src/components/landing/SectionHero.tsx"
      provides: "Hero section with mount-time staggered animation"
      exports: ["SectionHero"]
    - path: "web/src/components/landing/SectionGlobe.tsx"
      provides: "SVG globe draw-in with Switzerland teal highlight"
      exports: ["SectionGlobe"]
  key_links:
    - from: "SectionHero.tsx"
      to: "/auth"
      via: "Button render={<Link href='/auth' />}"
      pattern: "href.*auth"
    - from: "SectionGlobe.tsx"
      to: "motion/react pathLength"
      via: "motion.circle / motion.ellipse variants with pathLength: 0 → 1"
      pattern: "pathLength"
---

<objective>
Delete all 8 old Chapter and IsometricHome files, then create the first two new section components: SectionHero and SectionGlobe.

Purpose: Clears the broken sticky-parallax architecture from disk and establishes the new scroll-triggered entry pattern with two concrete, working components. SectionHero demonstrates mount-time animation; SectionGlobe demonstrates the pathLength SVG draw-in technique.

Output: 8 deleted files, 2 new section components.
</objective>

<execution_context>
@/Users/singhs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/singhs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-landing-page-redesign/20-CONTEXT.md
@.planning/phases/20-landing-page-redesign/20-RESEARCH.md
@.planning/phases/20-landing-page-redesign/20-01-SUMMARY.md

<interfaces>
<!-- Animation infrastructure — use these, do not recreate -->

From web/src/lib/motion.ts (verified in RESEARCH.md):
```typescript
export const duration = { instant: 0, fast: 0.15, base: 0.2, moderate: 0.3, slow: 0.5, crawl: 0.8 }
export const ease = {
  enter: [0.22, 1, 0.36, 1],    // decelerate (elements arriving)
  exit:  [0.55, 0, 1, 0.45],    // accelerate (elements leaving)
  inOut: [0.65, 0, 0.35, 1],
  expressive: [0.34, 1.56, 0.64, 1],
  linear: 'linear',
}
export const fadeUpVariants  // hidden: opacity 0, y 16  →  visible: opacity 1, y 0
export const fadeInVariants  // hidden: opacity 0         →  visible: opacity 1
export const staggerContainerVariants
export const staggerItemVariants
```

From web/src/components/motion/FadeIn.tsx:
```typescript
// Wraps children in motion.div with whileInView="visible", viewport={{ once: true, amount: 0.2 }}
// Accepts: delay?, duration?, className?
export function FadeIn({ children, delay, duration, className }: FadeInProps)
```

From web/src/components/motion/StaggerGroup.tsx:
```typescript
export function StaggerGroup({ children, className }: StaggerGroupProps)
export function StaggerItem({ children, className }: StaggerItemProps)
// StaggerGroup: whileInView="visible", viewport={{ once: true }}, staggerChildren: 0.08
// StaggerItem: uses staggerItemVariants (inherits from parent)
```

From web/src/components/ui/button.tsx (Phase 19 pattern):
```typescript
// Use render prop for link-as-button:
<Button render={<Link href="/auth" />} size="lg" className="...">
  {t(lang, 'landing_hero_cta')}
</Button>
```

From web/src/components/ui/shimmer-button.tsx:
```typescript
// Primary CTA option — 21st.dev component already in project
// Use as alternative to Button for the hero CTA if it fits visually
```

Design tokens (CSS variables, use via Tailwind bg-hero-bg etc.):
  --color-hero-bg → bg-hero-bg (near-black: hsl(0 0% 4%))
  --color-hero-fg → text-hero-fg (off-white)
  --color-hero-teal → text-teal / bg-hero-teal (hsl(173 65% 52%))

Globe SVG constraints (from RESEARCH.md Pattern 3):
  - pathLength only works on stroked elements (circle, ellipse, path with stroke)
  - Switzerland polygon uses fill color transition — NOT pathLength
  - whileInView="visible" with viewport={{ once: true, amount: 0.3 }}
  - Draw timing: outer circle 0s, latitude lines 0.3s delay, Europe silhouette fade at 0.8s,
    Switzerland fill at 1.6s, glow ring at 1.6s

Translation key access:
  import { t } from '@/lib/translations'
  import type { Language } from '@/lib/translations'
  // All keys added in Plan 01 are now available
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete old Chapter files and IsometricHome</name>
  <files>
    web/src/components/landing/ChapterHook.tsx,
    web/src/components/landing/ChapterSwitzerland.tsx,
    web/src/components/landing/ChapterProblem.tsx,
    web/src/components/landing/ChapterMechanism.tsx,
    web/src/components/landing/ChapterScore.tsx,
    web/src/components/landing/ChapterDream.tsx,
    web/src/components/landing/ChapterCTA.tsx,
    web/src/components/landing/IsometricHome.tsx
  </files>
  <action>
Delete all 8 files using the Bash tool:

```bash
rm /Users/singhs/gen-ai-hackathon/web/src/components/landing/ChapterHook.tsx \
   /Users/singhs/gen-ai-hackathon/web/src/components/landing/ChapterSwitzerland.tsx \
   /Users/singhs/gen-ai-hackathon/web/src/components/landing/ChapterProblem.tsx \
   /Users/singhs/gen-ai-hackathon/web/src/components/landing/ChapterMechanism.tsx \
   /Users/singhs/gen-ai-hackathon/web/src/components/landing/ChapterScore.tsx \
   /Users/singhs/gen-ai-hackathon/web/src/components/landing/ChapterDream.tsx \
   /Users/singhs/gen-ai-hackathon/web/src/components/landing/ChapterCTA.tsx \
   /Users/singhs/gen-ai-hackathon/web/src/components/landing/IsometricHome.tsx
```

Note: LandingPageContent.tsx still imports these files at this point — it will be rewritten in Plan 03.
The TypeScript compiler will fail until Plan 03 fixes LandingPageContent. That is expected.
  </action>
  <verify>
    <automated>ls /Users/singhs/gen-ai-hackathon/web/src/components/landing/</automated>
  </verify>
  <done>
    Directory contains only: LandingPageContent.tsx, LandingNavbar.tsx, LandingFooter.tsx.
    No Chapter* or IsometricHome* files present.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create SectionHero and SectionGlobe</name>
  <files>
    web/src/components/landing/SectionHero.tsx,
    web/src/components/landing/SectionGlobe.tsx
  </files>
  <behavior>
    SectionHero:
    - Renders a section with the HomeMatch wordmark ("HomeMatch") as an h1 or styled div
    - Renders the landing_hero_overline text as an overline above the headline
    - Renders landing_hero_headline as the primary headline (large display text)
    - Renders landing_hero_subtitle as body text
    - Renders a Button (with render={<Link href="/auth" />}) containing landing_hero_cta text
    - When useReducedMotion() is false: logo/overline at delay 0.1s, headline at 0.3s, subtitle at 0.5s, CTA at 0.7s — all use animate (mount-time), NOT whileInView
    - When useReducedMotion() is true: all elements visible immediately (no animation)

    SectionGlobe:
    - Renders a motion.svg with viewBox="0 0 200 200" and aria-label
    - Has initial="hidden" whileInView="visible" viewport={{ once: true, amount: 0.3 }}
    - Ocean fill circle fades in at duration 0.4s
    - Outer globe circle (stroke) draws in via pathLength: 0 → 1 at duration: ease.enter 1.4s
    - At least 2 latitude ellipses (stroke) draw in with 0.3s and 0.6s delays
    - Europe silhouette path (fill, no pathLength) fades in at delay: 0.8s
    - Switzerland polygon transitions fill from hsl(220 15% 25%) to hsl(173 65% 52%) at delay: 1.6s
    - Glow ring circle (stroke) draws in with pathLength at delay: 1.6s
    - landing_globe_headline text fades in below globe via FadeIn with delay={2.0}
    - landing_globe_body text fades in with delay={2.2}
    - useReducedMotion() check: when true, all variants passed as {} (empty = no animation)
  </behavior>
  <action>
Create `web/src/components/landing/SectionHero.tsx`:

Layout: `min-h-screen` centered flex column, `bg-hero-bg` background, `px-6 py-24` padding.
Use `motion` from `motion/react`, `useReducedMotion` from `motion/react`.
Use `duration`, `ease` from `@/lib/motion`.
Use `Button` from `@/components/ui/button`.
Use `Link` from `next/link`.
Use `t` from `@/lib/translations`, `Language` type from `@/lib/translations`.

Animation pattern (mount-time, NOT whileInView — see RESEARCH.md Pattern 4):
```typescript
const prefersReduced = useReducedMotion()

const motionProps = (delaySeconds: number) => ({
  initial: prefersReduced ? { opacity: 1, y: 0 } : { opacity: 0, y: 20 },
  animate: { opacity: 1, y: 0 },
  transition: prefersReduced
    ? { duration: 0 }
    : { delay: delaySeconds, duration: duration.slow, ease: ease.enter },
})
```

Structure:
```tsx
<section className="min-h-screen flex flex-col items-center justify-center bg-hero-bg text-hero-fg text-center px-6 py-24">
  <motion.p className="overline text" style={{ color: 'var(--color-hero-teal)', ... }} {...motionProps(0.1)}>
    {t(lang, 'landing_hero_overline')}
  </motion.p>
  <motion.h1 className="display headline" {...motionProps(0.3)}>
    {t(lang, 'landing_hero_headline')}
  </motion.h1>
  <motion.p className="subtitle" {...motionProps(0.5)}>
    {t(lang, 'landing_hero_subtitle')}
  </motion.p>
  <motion.div {...motionProps(0.7)}>
    <Button render={<Link href="/auth" />} size="lg"
      className="bg-hero-teal text-hero-bg hover:opacity-90 px-8 py-3 text-base font-semibold rounded-xl h-auto mt-6">
      {t(lang, 'landing_hero_cta')}
    </Button>
  </motion.div>
</section>
```

Typography sizing: use CSS variables from the design system (--text-display-size for headline, --text-body-size for subtitle). Max width on subtitle: `max-w-xl`. Headline max width: `max-w-2xl`.

---

Create `web/src/components/landing/SectionGlobe.tsx`:

Use `motion` from `motion/react`, `useReducedMotion` from `motion/react`.
Use `FadeIn` from `@/components/motion/FadeIn`.
Use `ease` from `@/lib/motion`.

The full SVG implementation is specified in RESEARCH.md Pattern 3. Implement it exactly:
- motion.svg with whileInView/viewport
- motion.circle for ocean fill (fade variant)
- motion.circle for globe outline (drawVariant with pathLength)
- motion.ellipse x2 for latitude lines (drawVariant with delay offsets)
- motion.ellipse for equator (drawVariant with delay 0.6s)
- motion.path for Europe silhouette (fadeVariant with delay 0.8s)
- motion.polygon for Switzerland (switzerlandVariant: fill transition)
- motion.circle for glow ring (glowRingVariant: pathLength + delay 1.6s)
- FadeIn delay={2.0} wrapping landing_globe_headline paragraph
- FadeIn delay={2.2} wrapping landing_globe_body paragraph (smaller text)

Section wrapper: `min-h-screen flex flex-col items-center justify-center bg-hero-bg py-24 px-6 text-center`

CRITICAL: Switzerland polygon uses fill color transition — NOT pathLength (polygon has fill, no stroke). The glow ring IS a circle with stroke — it DOES use pathLength. See RESEARCH.md Pitfall 4.
  </action>
  <verify>
    <automated>cd /Users/singhs/gen-ai-hackathon/web && npx tsc --noEmit --project tsconfig.json 2>&1 | grep -E "SectionHero|SectionGlobe" | head -20</automated>
  </verify>
  <done>
    SectionHero.tsx and SectionGlobe.tsx exist with no TypeScript errors in those files.
    Both export named functions accepting `{ lang: Language }` prop.
    Hero uses mount-time animate (not whileInView).
    Globe uses whileInView with pathLength variants and fill transition on Switzerland.
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `ls /Users/singhs/gen-ai-hackathon/web/src/components/landing/` — shows only Navbar, Footer, PageContent, SectionHero, SectionGlobe
2. `npx tsc --noEmit` in web/ — SectionHero and SectionGlobe compile clean (LandingPageContent.tsx will still error — fixed in Plan 03)
</verification>

<success_criteria>
- 8 Chapter/IsometricHome files deleted (confirmed via ls)
- SectionHero.tsx: mount-time animation, CTA → /auth via Button render prop, all 4 hero keys used
- SectionGlobe.tsx: pathLength draw-in on stroked elements, fill transition on Switzerland polygon, useReducedMotion fallback, globe + text content
- Both components accept `{ lang: Language }` and use `t(lang, key)` for all strings
</success_criteria>

<output>
After completion, create `.planning/phases/20-landing-page-redesign/20-02-SUMMARY.md`
</output>
