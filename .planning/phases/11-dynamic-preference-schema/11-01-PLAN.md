---
phase: 11-dynamic-preference-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/schemas/preferences.ts
  - backend/app/models/preferences.py
  - backend/tests/conftest.py
  - backend/tests/test_preferences.py
  - web/src/__tests__/preferences-schema.test.ts
autonomous: true
requirements: [SCHM-01, SCHM-02, SCHM-03, SCHM-05]

must_haves:
  truths:
    - "DynamicField type exists in Zod with name (string, min 1), value (string, default ''), and importance (critical/high/medium/low)"
    - "DynamicField model exists in Pydantic with name, value, importance fields and camelCase alias support"
    - "Preferences with softCriteria but no dynamicFields are auto-migrated to dynamicFields on parse (both Pydantic and Zod-side)"
    - "Migrated dynamicFields have importance defaulting to 'medium'"
    - "Existing tests continue to pass (backward compat) and new dynamic field tests are green"
  artifacts:
    - path: "web/src/lib/schemas/preferences.ts"
      provides: "dynamicFieldSchema + dynamicFields field on preferencesSchema + migratePreferences function"
      contains: "dynamicFieldSchema"
    - path: "backend/app/models/preferences.py"
      provides: "DynamicField BaseModel + dynamic_fields field on UserPreferences + migration in model_validator"
      contains: "class DynamicField"
    - path: "backend/tests/conftest.py"
      provides: "SAMPLE_PREFERENCES_JSON updated with dynamicFields"
      contains: "dynamicFields"
    - path: "backend/tests/test_preferences.py"
      provides: "Tests for DynamicField parsing, validation, migration"
      contains: "test_dynamic"
    - path: "web/src/__tests__/preferences-schema.test.ts"
      provides: "Tests for dynamicFields schema validation, defaults, migration"
      contains: "dynamicFields"
  key_links:
    - from: "backend/app/models/preferences.py"
      to: "Supabase JSONB"
      via: "model_validator migrates softCriteria -> dynamicFields on parse"
      pattern: "softCriteria.*dynamicFields"
    - from: "web/src/lib/schemas/preferences.ts"
      to: "preferencesSchema.parse() call sites"
      via: "migratePreferences() called before parse at load points"
      pattern: "migratePreferences"
---

<objective>
Add the DynamicField type to both Zod (web) and Pydantic (backend) schemas, with backward-compatible migration from softCriteria, and comprehensive tests.

Purpose: This is the foundation for Phase 11 -- all subsequent work (scoring prompt, UI) depends on the DynamicField type existing in both schemas. Migration ensures existing profiles with softCriteria continue to work.

Output: DynamicField types in both schemas, migration logic, updated test fixtures, passing test suites.
</objective>

<execution_context>
@/Users/maximgusev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maximgusev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-dynamic-preference-schema/11-RESEARCH.md

Source files being modified:
@web/src/lib/schemas/preferences.ts
@backend/app/models/preferences.py
@backend/tests/conftest.py
@backend/tests/test_preferences.py
@web/src/__tests__/preferences-schema.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add DynamicField to Pydantic model + migration + backend tests</name>
  <files>backend/app/models/preferences.py, backend/tests/conftest.py, backend/tests/test_preferences.py</files>
  <behavior>
    - Test: DynamicField with name/value/importance parses correctly from camelCase JSON
    - Test: UserPreferences with dynamicFields array parses and round-trips
    - Test: Empty dict parse produces dynamic_fields=[] (default)
    - Test: JSONB with softCriteria but no dynamicFields auto-migrates: each soft criterion becomes DynamicField(name=criterion, value="", importance="medium")
    - Test: JSONB with both softCriteria and dynamicFields preserves dynamicFields (no double migration)
    - Test: JSONB with soft_criteria (snake_case) also migrates correctly
    - Test: DynamicField rejects empty name (min length validation)
    - Test: DynamicField rejects invalid importance level
    - Test: Existing SAMPLE_PREFERENCES_JSON and LEGACY_PREFERENCES_JSON tests still pass
  </behavior>
  <action>
    1. In `backend/app/models/preferences.py`:
       - Add `DynamicField(BaseModel)` class with `model_config = ConfigDict(alias_generator=to_camel, populate_by_name=True)`, fields: `name: str`, `value: str = ""`, `importance: ImportanceLevel = ImportanceLevel.MEDIUM`. Add `@field_validator("name")` that rejects empty strings.
       - Add `dynamic_fields: list[DynamicField] = Field(default_factory=list)` to `UserPreferences` (the `to_camel` alias_generator will auto-create `dynamicFields` alias)
       - Extend `migrate_legacy_format` model_validator to migrate softCriteria -> dynamicFields (only when dynamicFields key is absent). Handle both camelCase `softCriteria` and snake_case `soft_criteria` variants. Filter out empty strings.

    2. In `backend/tests/conftest.py`:
       - Add `dynamicFields` to `SAMPLE_PREFERENCES_JSON` with sample values: `[{"name": "near Bahnhof", "value": "", "importance": "high"}, {"name": "quiet neighborhood", "value": "no main road", "importance": "medium"}]`
       - Keep existing `softCriteria` in `SAMPLE_PREFERENCES_JSON` for now (backward compat -- the model keeps both)
       - Keep `LEGACY_PREFERENCES_JSON` unchanged (it has softCriteria only, testing migration path)

    3. In `backend/tests/test_preferences.py`:
       - Add `TestDynamicFields` class with tests from behavior block above
       - Add `TestSoftCriteriaMigration` class with migration-specific tests
       - Ensure existing tests still pass (they reference SAMPLE_PREFERENCES_JSON which now includes dynamicFields)
       - Import `DynamicField` from the models module

    Run RED tests first, then implement, then verify GREEN.
  </action>
  <verify>
    <automated>cd /Users/maximgusev/workspace/gen-ai-hackathon/backend && python -m pytest tests/test_preferences.py -x -v</automated>
  </verify>
  <done>
    - DynamicField model exists with name/value/importance fields and camelCase alias support
    - UserPreferences.dynamic_fields field declared, not silently dropped by extra="ignore"
    - model_validator migrates softCriteria -> dynamicFields for legacy JSONB
    - All new tests pass, all existing tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add DynamicField to Zod schema + migratePreferences + web tests</name>
  <files>web/src/lib/schemas/preferences.ts, web/src/__tests__/preferences-schema.test.ts</files>
  <behavior>
    - Test: Empty object parse produces dynamicFields=[] (default)
    - Test: dynamicFields with valid entries round-trips through parse
    - Test: dynamicFieldSchema rejects empty name (min 1 char)
    - Test: dynamicFieldSchema defaults value to '' and importance to 'medium'
    - Test: migratePreferences converts softCriteria strings to dynamicFields with importance='medium'
    - Test: migratePreferences skips migration when dynamicFields already present
    - Test: migratePreferences handles empty softCriteria array (no dynamicFields created)
    - Test: Existing test cases still pass
  </behavior>
  <action>
    1. In `web/src/lib/schemas/preferences.ts`:
       - Add `dynamicFieldSchema = z.object({ name: z.string().min(1), value: z.string().default(''), importance: importanceLevelSchema })` after the existing `importanceLevelSchema`
       - Export `DynamicField` type: `export type DynamicField = z.infer<typeof dynamicFieldSchema>`
       - Add `dynamicFields: z.array(dynamicFieldSchema).default([])` to `preferencesSchema` (keep `softCriteria` field for backward compat)
       - Export `migratePreferences(raw: Record<string, unknown>): Record<string, unknown>` function that converts softCriteria strings to dynamicFields objects with importance='medium', only when dynamicFields key is not present. Filter empty strings.

    2. In `web/src/__tests__/preferences-schema.test.ts`:
       - Import `migratePreferences` and `dynamicFieldSchema` from the schema module
       - Add `describe('dynamicFields')` block with tests from behavior block above
       - Add `describe('migratePreferences')` block with migration tests
       - Update existing empty-parse test to also check `result.dynamicFields` equals `[]`

    Run RED tests first, then implement, then verify GREEN.
  </action>
  <verify>
    <automated>cd /Users/maximgusev/workspace/gen-ai-hackathon/web && npx vitest run src/__tests__/preferences-schema.test.ts</automated>
  </verify>
  <done>
    - dynamicFieldSchema exported with name/value/importance validation
    - DynamicField type exported
    - preferencesSchema includes dynamicFields field with default []
    - migratePreferences function exported and handles softCriteria -> dynamicFields conversion
    - All new tests pass, all existing tests pass
  </done>
</task>

</tasks>

<verification>
Both test suites green:
- `cd backend && python -m pytest tests/test_preferences.py -x -v` -- all pass
- `cd web && npx vitest run src/__tests__/preferences-schema.test.ts` -- all pass

Full suites also green:
- `cd backend && python -m pytest tests/ -x`
- `cd web && npx vitest run`
</verification>

<success_criteria>
- DynamicField type exists in both Zod and Pydantic with name, value, importance
- softCriteria -> dynamicFields migration works in both schemas
- All backend and web tests pass (new + existing)
- No schema breakage for existing profiles
</success_criteria>

<output>
After completion, create `.planning/phases/11-dynamic-preference-schema/11-01-SUMMARY.md`
</output>
