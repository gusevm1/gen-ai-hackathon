---
phase: 26-proximity-enhancements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/proximity.py
  - backend/app/prompts/scoring.py
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
  - D-10

must_haves:
  truths:
    - "When no places are found within the requested radius, a second Apify search at 2x radius is performed"
    - "From the expanded search, only the single nearest result is returned, tagged with is_fallback=True"
    - "If expanded search also returns nothing, fallback is empty — no fake results"
    - "The LLM prompt instructs the scorer to output met='partial' when fallback data is present and importance is not critical"
    - "The LLM prompt instructs the scorer to output met=false when fallback data is present and importance is critical"
    - "Ratings already flow through to the LLM prompt (no changes needed for D-10)"
  artifacts:
    - path: "backend/app/services/proximity.py"
      provides: "Closest fallback logic in fetch_nearby_places"
      contains: "is_fallback"
    - path: "backend/app/prompts/scoring.py"
      provides: "Partial met scoring rule in LLM prompt"
      contains: "partial"
  key_links:
    - from: "backend/app/services/proximity.py"
      to: "backend/app/services/places.py"
      via: "search_nearby_places called with radius_km * 2"
      pattern: "search_nearby_places.*radius_km.*2"
    - from: "backend/app/prompts/scoring.py"
      to: "backend/app/services/proximity.py"
      via: "nearby_places dict carries is_fallback flag into prompt text"
      pattern: "is_fallback"
---

<objective>
Add closest-fallback logic to the proximity pipeline and update the LLM scoring prompt to output `met: "partial"` for near-miss proximity results based on importance level.

Purpose: When a user requests "Starbucks within 5km" and the nearest is at 5.2km, the system should surface that near-miss rather than silently reporting "not found." The LLM scorer must distinguish fallback results from in-range results and apply importance-based scoring rules.

Output: Updated proximity.py with fallback search + updated scoring.py prompt with partial-met rules.
</objective>

<execution_context>
@/Users/prakhar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prakhar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-proximity-enhancements-google-ratings-closest-fallback-and-grossmuenster-background-image/26-CONTEXT.md
@backend/app/services/proximity.py
@backend/app/services/places.py
@backend/app/prompts/scoring.py
@backend/app/routers/scoring.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add closest-fallback logic to fetch_nearby_places</name>
  <files>backend/app/services/proximity.py</files>
  <read_first>
    - backend/app/services/proximity.py (current implementation of fetch_nearby_places)
    - backend/app/services/places.py (search_nearby_places signature, haversine_km)
  </read_first>
  <action>
Modify `fetch_nearby_places` in `proximity.py` to implement the closest-fallback search:

1. After the existing Apify search at line ~153 (`raw_results = await search_nearby_places(...)`), add fallback logic:
   - If `raw_results` is empty (no places found within `requirement.radius_km`):
     a. Compute `fallback_radius = requirement.radius_km * 2` (D-05: no additional cap beyond the 2x multiplier, but still respect `_MAX_RADIUS_KM` if you want — actually per D-05 the 2x IS the extent, so cap at `min(requirement.radius_km * 2, _MAX_RADIUS_KM * 2)` is NOT needed. Just use `requirement.radius_km * 2` directly).
     b. Log: `logger.info("No results within %.2f km for query=%r — trying fallback at %.2f km", requirement.radius_km, requirement.query, fallback_radius)`
     c. Check cache for the expanded radius first (same cache check pattern as existing, but with `fallback_radius` as the radius_km parameter).
     d. If cache miss, call `search_nearby_places(query=requirement.query, latitude=lat, longitude=lon, radius_km=fallback_radius, max_results=5)`.
     e. Compute haversine distances for all fallback results (same loop as existing lines 162-182).
     f. Sort by `distance_km` (ascending, None values last).
     g. Take only the **single nearest** result (D-02).
     h. Add `"is_fallback": True` to that result dict (D-03).
     i. Cache the fallback results under the expanded radius key.
     j. Return the single-item list `[nearest_result]`.

2. If the expanded search also returns nothing (D-04), return `[]` (empty list — scorer handles this as fully not met).

3. For NON-fallback results (original search returned results), add `"is_fallback": False` to each result dict so the prompt builder can distinguish them.

The result dict shape becomes: `{name, address, rating, review_count, distance_km, is_fallback}`.
  </action>
  <verify>
    <automated>cd /Users/prakhar/Desktop/GenAI_hackathon/gen-ai-hackathon && python -c "
import ast, sys
with open('backend/app/services/proximity.py') as f:
    tree = ast.parse(f.read())
# Check fetch_nearby_places has 'is_fallback' string literal
src = open('backend/app/services/proximity.py').read()
assert 'is_fallback' in src, 'Missing is_fallback flag'
assert 'fallback_radius' in src or 'radius_km * 2' in src, 'Missing 2x radius logic'
assert 'search_nearby_places' in src, 'Missing search call'
print('OK: proximity.py has fallback logic')
"</automated>
  </verify>
  <acceptance_criteria>
    - `proximity.py` contains `is_fallback` string in at least 3 places (True for fallback, False for normal)
    - `proximity.py` contains `radius_km * 2` or equivalent fallback radius computation
    - `proximity.py` contains a sort by distance_km for fallback results
    - `proximity.py` contains a log line with "fallback" for the expanded search path
    - Non-fallback results also get `"is_fallback": False` added to their dict
  </acceptance_criteria>
  <done>fetch_nearby_places performs a 2x radius fallback search when original returns empty, returns single nearest result tagged with is_fallback=True, caches under expanded radius key</done>
</task>

<task type="auto">
  <name>Task 2: Update LLM scoring prompt with partial-met rules and fallback context</name>
  <files>backend/app/prompts/scoring.py</files>
  <read_first>
    - backend/app/prompts/scoring.py (current build_user_prompt, nearby places section builder)
    - .planning/phases/26-proximity-enhancements-google-ratings-closest-fallback-and-grossmuenster-background-image/26-CONTEXT.md (D-06 through D-09 rules)
  </read_first>
  <action>
Two changes to `scoring.py`:

**Change 1: Update the nearby places section in `build_user_prompt`** (lines 355-385)

In the loop that formats `nearby_places` (line 361: `for query, places in nearby_places.items()`), detect fallback results and annotate them clearly for the LLM:

- For each place in the inner loop (line 364), check `place.get("is_fallback", False)`.
- If `is_fallback` is True, append ` [FALLBACK — outside requested radius]` after the distance string.
- Example output line: `1. Starbucks — 5.20 km [FALLBACK — outside requested radius] | Rating: 4.1 (892 reviews)`

**Change 2: Add PARTIAL MET scoring rules to `build_system_prompt`** (after the PROXIMITY EVALUATION RULES block, around line 93)

Add a new rules block to the system prompt string:

```
PARTIAL MET RULES (for proximity criteria with fallback data):
- When the "## Nearby Places Data (Verified)" section contains a result marked [FALLBACK — outside requested radius]:
  - The nearest amenity was found OUTSIDE the user's requested distance but within 2x that distance.
  - Check the importance level of that proximity criterion from the user's Custom Criteria section.
  - If importance is CRITICAL: set met to false (boolean). Critical requirements must be strictly met.
  - If importance is HIGH, MEDIUM, or LOW: set met to "partial" (the string "partial", not a boolean). This signals a near-miss worth considering.
  - In the note field, include the actual distance, the requested radius, and the place name/rating.
- When results are NOT marked as fallback (found within requested radius): set met to true (boolean).
- When no results are found at all (even after fallback): set met to false (boolean).
- The met field in your JSON response for checklist items must be exactly one of: true, false, or "partial" (string).
```

This goes inside the `build_system_prompt` function's return string, right after the `PROXIMITY EVALUATION RULES:` block (before the closing triple-quote).
  </action>
  <verify>
    <automated>cd /Users/prakhar/Desktop/GenAI_hackathon/gen-ai-hackathon && python -c "
src = open('backend/app/prompts/scoring.py').read()
assert 'PARTIAL MET RULES' in src, 'Missing PARTIAL MET RULES block'
assert '\"partial\"' in src, 'Missing partial string literal in prompt'
assert 'FALLBACK' in src, 'Missing FALLBACK marker in nearby places formatter'
assert 'is_fallback' in src, 'Missing is_fallback check in formatter'
assert 'CRITICAL' in src and 'met to false' in src, 'Missing critical->false rule'
print('OK: scoring.py has partial-met rules and fallback annotation')
"</automated>
  </verify>
  <acceptance_criteria>
    - `scoring.py` system prompt contains `PARTIAL MET RULES` section
    - `scoring.py` system prompt contains `"partial"` as a valid met value
    - `scoring.py` system prompt contains rule: importance CRITICAL -> met=false
    - `scoring.py` system prompt contains rule: importance HIGH/MEDIUM/LOW -> met="partial"
    - `scoring.py` `build_user_prompt` checks `is_fallback` and appends `[FALLBACK — outside requested radius]` to the distance line
    - `scoring.py` contains `met to false` and `met to "partial"` phrasing in the prompt rules
  </acceptance_criteria>
  <done>LLM prompt includes PARTIAL MET RULES block instructing importance-based partial/false scoring for fallback results; nearby places formatter annotates fallback results with [FALLBACK] marker</done>
</task>

</tasks>

<verification>
1. `python -c "import ast; ast.parse(open('backend/app/services/proximity.py').read()); print('syntax ok')"` — no syntax errors
2. `python -c "import ast; ast.parse(open('backend/app/prompts/scoring.py').read()); print('syntax ok')"` — no syntax errors
3. `grep -c 'is_fallback' backend/app/services/proximity.py` — should be >= 3
4. `grep -c 'PARTIAL MET' backend/app/prompts/scoring.py` — should be >= 1
5. `grep -c 'FALLBACK' backend/app/prompts/scoring.py` — should be >= 1
</verification>

<success_criteria>
- proximity.py implements 2x radius fallback when original search returns empty, returns single nearest tagged with is_fallback=True
- scoring.py system prompt has PARTIAL MET RULES block with importance-based met/partial/false logic
- scoring.py user prompt annotates fallback results with [FALLBACK] marker
- Both files have valid Python syntax
</success_criteria>

<output>
After completion, create `.planning/phases/26-proximity-enhancements-google-ratings-closest-fallback-and-grossmuenster-background-image/26-01-SUMMARY.md`
</output>
