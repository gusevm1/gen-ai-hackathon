---
phase: 37-design-system-propagation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/__tests__/analysis-page.test.ts
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "Full test suite shows no phase-37-caused failures"
    - "analysis-page.test.ts assertions match the DS-03 tier palette (teal/green/amber/red)"
  artifacts:
    - path: "web/src/__tests__/analysis-page.test.ts"
      provides: "Tier color assertions matching the updated ScoreHeader TIER_COLORS map"
      contains: "bg-teal-500"
  key_links:
    - from: "web/src/__tests__/analysis-page.test.ts"
      to: "web/src/components/analysis/ScoreHeader.tsx"
      via: "getTierColor() import"
      pattern: "getTierColor"
---

<objective>
Fix 4 stale tier-color assertions in analysis-page.test.ts that still reference the pre-DS-03 palette (emerald/blue/gray) after ScoreHeader TIER_COLORS was updated to teal/green/red.

Purpose: These 4 test failures are the only blocker preventing the full test suite from going green after Phase 37's implementation work.
Output: analysis-page.test.ts with correct assertions; `npm test -- --run src/__tests__/analysis-page.test.ts` reports 0 failures.
</objective>

<execution_context>
@/Users/singhs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/singhs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-design-system-propagation/37-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update stale tier-color assertions in analysis-page.test.ts</name>
  <files>web/src/__tests__/analysis-page.test.ts</files>
  <action>
Update 4 assertions in the `getTierColor` describe block (lines 7-36) to match the current ScoreHeader TIER_COLORS map:

1. Line 7 — test name: change "returns emerald colors for excellent tier" → "returns teal colors for excellent tier"
   Line 9 — assertion: `expect(result.bg).toBe('bg-emerald-500')` → `expect(result.bg).toBe('bg-teal-500')`

2. Line 13 — test name: change "returns blue colors for good tier" → "returns green colors for good tier"
   Line 15 — assertion: `expect(result.bg).toBe('bg-blue-500')` → `expect(result.bg).toBe('bg-green-500')`

3. Line 25 — test name: change "returns gray colors for poor tier" → "returns red colors for poor tier"
   Line 27 — assertion: `expect(result.bg).toBe('bg-gray-500')` → `expect(result.bg).toBe('bg-red-500')`

4. Line 31 — test name: change "defaults to poor colors for unknown tier" — keep name as-is (semantically correct)
   Line 33 — assertion: `expect(result.bg).toBe('bg-gray-500')` → `expect(result.bg).toBe('bg-red-500')`
   (Unknown tier falls back to TIER_COLORS.poor via `?? TIER_COLORS.poor` in getTierColor — poor is now red-500)

Do NOT change any other assertions in the file. The `text-white` and `text-gray-900` assertions are unchanged. The `fair` tier was always amber and is unaffected.
  </action>
  <verify>
    <automated>cd /Users/singhs/gen-ai-hackathon/web && npm test -- --run src/__tests__/analysis-page.test.ts</automated>
  </verify>
  <done>All tests in analysis-page.test.ts pass. The 4 previously-failing tier-color assertions now match the DS-03 palette (bg-teal-500, bg-green-500, bg-red-500). No other tests in the file are broken.</done>
</task>

</tasks>

<verification>
Run the targeted test file to confirm all tests pass:

```bash
cd /Users/singhs/gen-ai-hackathon/web && npm test -- --run src/__tests__/analysis-page.test.ts
```

Expected: All tests pass (previously 4 failing, now 0 failing in this file).

Also confirm no regression in tier-colors tests:
```bash
cd /Users/singhs/gen-ai-hackathon/web && npm test -- --run src/__tests__/tier-colors.test.tsx
```

Expected: 5/5 still pass.
</verification>

<success_criteria>
- analysis-page.test.ts reports 0 failures (was 4 before this gap closure)
- The 4 updated assertions use: bg-teal-500 (excellent), bg-green-500 (good), bg-red-500 (poor), bg-red-500 (unknown fallback)
- tier-colors.test.tsx continues to pass 5/5 (no regression)
- No production code changes — test file only
</success_criteria>

<output>
After completion, create `.planning/phases/37-design-system-propagation/37-05-SUMMARY.md`
</output>
