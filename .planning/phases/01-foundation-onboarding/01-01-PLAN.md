---
phase: 01-foundation-onboarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/wxt.config.ts
  - extension/package.json
  - extension/tsconfig.json
  - extension/tailwind.config.js
  - extension/postcss.config.js
  - extension/components.json
  - extension/src/entrypoints/popup/index.html
  - extension/src/entrypoints/popup/App.tsx
  - extension/src/entrypoints/popup/main.tsx
  - extension/src/entrypoints/popup/style.css
  - extension/src/entrypoints/background.ts
  - extension/src/entrypoints/content.ts
  - extension/src/lib/supabase.ts
  - extension/src/lib/utils.ts
  - extension/src/assets/styles/globals.css
  - web/src/app/layout.tsx
  - web/src/app/page.tsx
  - web/src/lib/supabase/client.ts
  - web/src/lib/supabase/server.ts
  - web/src/lib/supabase/middleware.ts
  - web/middleware.ts
  - web/.env.local
  - backend/app/main.py
  - backend/app/__init__.py
  - backend/requirements.txt
  - backend/Dockerfile
  - supabase/config.toml
  - supabase/migrations/001_initial_schema.sql
  - supabase/functions/score-proxy/index.ts
  - .planning/archive/extension-homegate/
autonomous: true
requirements:
  - INFRA-03
user_setup:
  - service: supabase
    why: "Auth + database + edge functions hub for all three codebases"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard -> Project Settings -> API -> Project URL"
      - name: NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> anon/public key"
      - name: VITE_SUPABASE_URL
        source: "Same as NEXT_PUBLIC_SUPABASE_URL"
      - name: VITE_SUPABASE_ANON_KEY
        source: "Same as NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY"
    dashboard_config:
      - task: "Create a new Supabase project (free tier)"
        location: "https://supabase.com/dashboard -> New Project"
      - task: "Disable email confirmation for dev (Auth -> Providers -> Email -> toggle off 'Confirm email')"
        location: "Supabase Dashboard -> Authentication -> Providers -> Email"
      - task: "Install Supabase CLI: brew install supabase/tap/supabase"
        location: "Terminal"

must_haves:
  truths:
    - "Fresh WXT extension scaffold exists with popup, background, and content script entrypoints"
    - "Next.js App Router project exists with Supabase SSR client configured"
    - "FastAPI project exists with health check endpoint and Dockerfile"
    - "Supabase project has migrations for user_preferences and analyses tables with RLS"
    - "Old Homegate extension code is archived, not deleted"
  artifacts:
    - path: "extension/wxt.config.ts"
      provides: "WXT config with React module, Flatfox host permissions, storage permission"
      contains: "flatfox"
    - path: "extension/src/lib/supabase.ts"
      provides: "Supabase client with chrome.storage.local adapter"
      contains: "chromeStorageAdapter"
    - path: "web/src/lib/supabase/server.ts"
      provides: "Server-side Supabase client with cookie handling"
      contains: "createServerClient"
    - path: "web/src/lib/supabase/client.ts"
      provides: "Browser-side Supabase client"
      contains: "createBrowserClient"
    - path: "web/middleware.ts"
      provides: "Root middleware for session refresh"
      contains: "updateSession"
    - path: "backend/app/main.py"
      provides: "FastAPI app with health check and CORS"
      contains: "health"
    - path: "backend/Dockerfile"
      provides: "Docker config for FastAPI deployment"
      contains: "uvicorn"
    - path: "supabase/migrations/001_initial_schema.sql"
      provides: "Database tables with RLS policies"
      contains: "user_preferences"
    - path: "supabase/functions/score-proxy/index.ts"
      provides: "Edge function scaffold for proxying to EC2"
      contains: "score-proxy"
  key_links:
    - from: "extension/src/lib/supabase.ts"
      to: "chrome.storage.local"
      via: "Custom storage adapter"
      pattern: "chrome\\.storage\\.local"
    - from: "web/middleware.ts"
      to: "web/src/lib/supabase/middleware.ts"
      via: "updateSession import"
      pattern: "import.*updateSession"
    - from: "supabase/migrations/001_initial_schema.sql"
      to: "auth.users"
      via: "Foreign key reference"
      pattern: "REFERENCES auth\\.users"
---

<objective>
Scaffold all four codebases (WXT extension, Next.js web app, FastAPI backend, Supabase project) from scratch, archive the old Homegate extension code, and create the Supabase database schema with RLS policies.

Purpose: Establish the project foundation that all subsequent plans build on. Every codebase needs to exist with its core libraries installed before auth wiring or deployment can happen.
Output: Four working project directories (extension/, web/, backend/, supabase/) with base configurations, plus archived old code.
</objective>

<execution_context>
@/Users/maximgusev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maximgusev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-onboarding/01-CONTEXT.md
@.planning/phases/01-foundation-onboarding/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Archive old extension and scaffold fresh WXT extension with Supabase adapter</name>
  <files>
    .planning/archive/extension-homegate/
    extension/wxt.config.ts
    extension/package.json
    extension/tsconfig.json
    extension/tailwind.config.js
    extension/postcss.config.js
    extension/components.json
    extension/vitest.config.ts
    extension/src/entrypoints/popup/index.html
    extension/src/entrypoints/popup/App.tsx
    extension/src/entrypoints/popup/main.tsx
    extension/src/entrypoints/popup/style.css
    extension/src/entrypoints/background.ts
    extension/src/entrypoints/content.ts
    extension/src/components/ui/
    extension/src/lib/supabase.ts
    extension/src/lib/utils.ts
    extension/src/assets/styles/globals.css
    extension/.env
  </files>
  <action>
    1. Archive old extension code: `cp -r extension/ .planning/archive/extension-homegate/` then `rm -rf extension/` (keep node_modules out of archive if large -- use rsync --exclude)

    2. Scaffold fresh WXT extension:
       - Run `pnpm dlx wxt@latest init extension --template react`
       - `cd extension && pnpm add @supabase/supabase-js`
       - Configure shadcn/ui: reuse the old archive's components.json and tailwind.config.js patterns. Install dependencies: `pnpm add tailwindcss@3 postcss autoprefixer class-variance-authority clsx tailwind-merge lucide-react`
       - Set up `pnpm dlx shadcn@latest init` with default config pointing to src/components/ui/
       - Add shadcn button and input components: `pnpm dlx shadcn@latest add button input card label`

    3. Configure WXT (wxt.config.ts):
       ```typescript
       import { defineConfig } from 'wxt'
       export default defineConfig({
         srcDir: 'src',
         extensionApi: 'chrome',
         modules: ['@wxt-dev/module-react'],
         manifest: {
           name: 'HomeMatch',
           description: 'AI-powered property match scoring for Flatfox.ch',
           version: '0.1.0',
           permissions: ['storage'],
           host_permissions: ['*://*.flatfox.ch/*'],
         },
       })
       ```

    4. Create entrypoints:
       - `src/entrypoints/popup/`: index.html + App.tsx (simple React root rendering "HomeMatch" placeholder) + main.tsx + style.css importing globals.css
       - `src/entrypoints/background.ts`: Use defineBackground() with console.log('HomeMatch background loaded'). NOTE: main() CANNOT be async per WXT docs.
       - `src/entrypoints/content.ts`: Use defineContentScript with matches: ['*://*.flatfox.ch/*'], main() logs 'HomeMatch content script loaded on Flatfox'. Phase 1 scaffold only -- no visible output.

    5. Create Supabase client with chrome.storage adapter (`src/lib/supabase.ts`):
       - Custom chromeStorageAdapter object with getItem/setItem/removeItem using chrome.storage.local
       - Export `supabase` client created with createClient() using VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY from import.meta.env
       - Configure: storage: chromeStorageAdapter, autoRefreshToken: true, persistSession: true, detectSessionInUrl: false

    6. Create `src/lib/utils.ts` with the cn() utility (clsx + tailwind-merge).

    7. Create `src/assets/styles/globals.css` with Tailwind directives (@tailwind base/components/utilities) and shadcn CSS variables.

    8. Create `.env` file with placeholder values:
       ```
       VITE_SUPABASE_URL=your-supabase-url
       VITE_SUPABASE_ANON_KEY=your-supabase-anon-key
       ```

    9. Verify the extension builds: `cd extension && pnpm run build`
  </action>
  <verify>
    <automated>cd /Users/maximgusev/workspace/gen-ai-hackathon && test -d .planning/archive/extension-homegate && cd extension && pnpm run build 2>&1 | tail -5 && echo "BUILD OK"</automated>
  </verify>
  <done>
    - Old extension archived to .planning/archive/extension-homegate/
    - Fresh WXT extension builds successfully with React, Tailwind, shadcn/ui
    - All 3 entrypoints (popup, background, content) defined
    - Supabase client with chrome.storage adapter exists
    - .env has placeholder Supabase credentials
  </done>
</task>

<task type="auto">
  <name>Task 2: Scaffold Next.js web app with Supabase SSR auth infrastructure</name>
  <files>
    web/src/app/layout.tsx
    web/src/app/page.tsx
    web/src/lib/supabase/client.ts
    web/src/lib/supabase/server.ts
    web/src/lib/supabase/middleware.ts
    web/middleware.ts
    web/.env.local
    web/package.json
  </files>
  <action>
    1. Scaffold Next.js app:
       ```bash
       pnpm create next-app@latest web --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --use-pnpm
       ```

    2. Install Supabase SSR dependencies:
       ```bash
       cd web && pnpm add @supabase/supabase-js @supabase/ssr
       ```

    3. Set up shadcn/ui in web/:
       ```bash
       cd web && pnpm dlx shadcn@latest init
       pnpm dlx shadcn@latest add button input card label
       ```

    4. Create Supabase client files (follow RESEARCH.md Pattern 1 exactly):
       - `src/lib/supabase/client.ts`: Browser client using createBrowserClient with NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY
       - `src/lib/supabase/server.ts`: Server client using createServerClient with cookies() from next/headers. Wrap setAll in try/catch for Server Component compatibility.
       - `src/lib/supabase/middleware.ts`: updateSession helper that creates a server client from request cookies, calls getUser() to refresh session, and returns response with updated cookies.

    5. Create root middleware (web/middleware.ts):
       - Import updateSession from @/lib/supabase/middleware
       - Export middleware function that calls updateSession(request)
       - Export config.matcher excluding static assets

    6. Replace default page.tsx with a minimal centered "HomeMatch" heading (landing page placeholder -- auth UI comes in Plan 02).

    7. Create .env.local with placeholder values:
       ```
       NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
       NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-supabase-anon-key
       ```

    8. Verify the Next.js app builds: `cd web && pnpm run build`
  </action>
  <verify>
    <automated>cd /Users/maximgusev/workspace/gen-ai-hackathon/web && pnpm run build 2>&1 | tail -5 && echo "BUILD OK"</automated>
  </verify>
  <done>
    - Next.js app scaffolded with App Router, TypeScript, Tailwind, shadcn/ui
    - Supabase SSR client files created (client.ts, server.ts, middleware.ts)
    - Root middleware wired for session refresh
    - .env.local has placeholder Supabase credentials
    - App builds successfully
  </done>
</task>

<task type="auto">
  <name>Task 3: Scaffold FastAPI backend + Supabase project with database migrations and edge function</name>
  <files>
    backend/app/main.py
    backend/app/__init__.py
    backend/requirements.txt
    backend/Dockerfile
    backend/.env
    backend/.dockerignore
    supabase/config.toml
    supabase/migrations/001_initial_schema.sql
    supabase/functions/score-proxy/index.ts
  </files>
  <action>
    1. Create FastAPI backend (backend/ directory):
       - `backend/app/__init__.py`: empty
       - `backend/app/main.py`: FastAPI app with title "HomeMatch API", version "0.1.0". Add CORSMiddleware with allow_origins=["*"], allow_credentials=True, allow_methods=["*"], allow_headers=["*"]. Add GET /health endpoint returning {"status": "healthy", "service": "homematch-api"}.
       - `backend/requirements.txt`: fastapi, uvicorn[standard]
       - `backend/Dockerfile`: Python 3.12-slim base, copy requirements.txt first (layer caching), pip install, copy app/, expose 8000, HEALTHCHECK with curl to /health, CMD uvicorn app.main:app --host 0.0.0.0 --port 8000
       - `backend/.dockerignore`: __pycache__, .env, .git, *.pyc
       - `backend/.env`: placeholder (not committed)

    2. Verify FastAPI locally:
       ```bash
       cd backend && pip install -r requirements.txt && python -c "from app.main import app; print('FastAPI import OK')"
       ```

    3. Initialize Supabase project:
       ```bash
       cd /Users/maximgusev/workspace/gen-ai-hackathon && supabase init
       ```
       This creates the supabase/ directory with config.toml.

    4. Create SQL migration (supabase/migrations/001_initial_schema.sql):
       - user_preferences table: id UUID PK, user_id UUID FK to auth.users UNIQUE NOT NULL, preferences JSONB DEFAULT '{}' NOT NULL, created_at TIMESTAMPTZ DEFAULT now(), updated_at TIMESTAMPTZ DEFAULT now()
       - Enable RLS on user_preferences with SELECT/INSERT/UPDATE policies using auth.uid() = user_id
       - analyses table: id UUID PK, user_id UUID FK to auth.users NOT NULL, listing_id TEXT NOT NULL, score INTEGER NOT NULL CHECK 0-100, breakdown JSONB DEFAULT '{}', created_at TIMESTAMPTZ DEFAULT now(), UNIQUE(user_id, listing_id)
       - Enable RLS on analyses with SELECT/INSERT/UPDATE/DELETE policies using auth.uid() = user_id
       - Use exact SQL from RESEARCH.md code examples section.

    5. Create edge function scaffold (supabase/functions/score-proxy/index.ts):
       - Deno.serve handler
       - CORS preflight handling for OPTIONS
       - Auth verification: extract Authorization header, create Supabase client, call getUser()
       - Proxy to EC2_BACKEND_URL from Deno.env (placeholder -- actual URL set after EC2 deploy)
       - Return proxied response with CORS headers
       - Follow Pattern 3 from RESEARCH.md exactly.

    6. Verify Docker builds:
       ```bash
       cd backend && docker build -t homematch-api . && docker run --rm -d -p 8000:8000 --name hm-test homematch-api && sleep 3 && curl -f http://localhost:8000/health && docker stop hm-test
       ```
  </action>
  <verify>
    <automated>cd /Users/maximgusev/workspace/gen-ai-hackathon && python -c "from backend.app.main import app; print('FastAPI OK')" && test -f supabase/migrations/001_initial_schema.sql && test -f supabase/functions/score-proxy/index.ts && echo "ALL SCAFFOLDS OK"</automated>
  </verify>
  <done>
    - FastAPI backend with health check endpoint and Docker config
    - Supabase project initialized with config.toml
    - SQL migration creates user_preferences and analyses tables with RLS
    - Edge function scaffold for score-proxy exists
    - Docker image builds and health check responds
  </done>
</task>

</tasks>

<verification>
All four codebases exist and build/import successfully:
- `cd extension && pnpm run build` succeeds
- `cd web && pnpm run build` succeeds
- `python -c "from backend.app.main import app"` succeeds
- `supabase/migrations/001_initial_schema.sql` exists with CREATE TABLE statements
- `.planning/archive/extension-homegate/` contains old code
</verification>

<success_criteria>
- Four project directories exist: extension/, web/, backend/, supabase/
- Extension builds with WXT + React + Tailwind + shadcn/ui + Supabase client
- Next.js app builds with Supabase SSR auth infrastructure
- FastAPI Docker image builds and health check works
- Database migration file has both tables with RLS policies
- Edge function scaffold exists
- Old extension code archived (not deleted)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-onboarding/01-01-SUMMARY.md`
</output>
