---
phase: 01-foundation-onboarding
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - extension/src/entrypoints/popup/App.tsx
  - extension/src/components/popup/LoginForm.tsx
  - extension/src/components/popup/Dashboard.tsx
  - extension/src/entrypoints/popup/style.css
  - extension/.env
  - backend/.env
  - supabase/functions/score-proxy/index.ts
  - supabase/.env
autonomous: false
requirements:
  - AUTH-02
  - AUTH-03
  - INFRA-02

must_haves:
  truths:
    - "User can log in via the Chrome extension popup with email/password"
    - "Logged-in extension popup shows user email, logout button, and link to website"
    - "Logging out in extension returns to login form"
    - "FastAPI backend is running on EC2 and /health returns 200"
    - "Supabase edge function proxies requests to EC2 backend with JWT auth validation"
    - "Edge function rejects unauthenticated requests with 401"
  artifacts:
    - path: "extension/src/components/popup/LoginForm.tsx"
      provides: "Email/password login form for extension popup"
      contains: "signInWithPassword"
    - path: "extension/src/components/popup/Dashboard.tsx"
      provides: "Logged-in state showing email, logout, website link"
      contains: "signOut"
    - path: "extension/src/entrypoints/popup/App.tsx"
      provides: "Root component switching between LoginForm and Dashboard based on auth state"
      contains: "onAuthStateChange"
  key_links:
    - from: "extension/src/components/popup/LoginForm.tsx"
      to: "extension/src/lib/supabase.ts"
      via: "Supabase client import for signInWithPassword"
      pattern: "import.*supabase"
    - from: "extension/src/entrypoints/popup/App.tsx"
      to: "extension/src/lib/supabase.ts"
      via: "Auth state listener"
      pattern: "onAuthStateChange"
    - from: "supabase/functions/score-proxy/index.ts"
      to: "backend/app/main.py"
      via: "HTTP fetch to EC2_BACKEND_URL"
      pattern: "EC2_BACKEND_URL"
---

<objective>
Wire Supabase auth into the Chrome extension popup (login/logout), deploy FastAPI to EC2, and deploy the Supabase edge function that proxies authenticated requests to the backend.

Purpose: Completes the auth circuit (AUTH-02) and the backend infrastructure (INFRA-02, AUTH-03). After this plan, a user who signed up on the website can log into the extension, and the edge function can reach the EC2 backend.
Output: Working extension popup auth + deployed EC2 backend + deployed edge function.
</objective>

<execution_context>
@/Users/maximgusev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maximgusev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-onboarding/01-CONTEXT.md
@.planning/phases/01-foundation-onboarding/01-RESEARCH.md
@.planning/phases/01-foundation-onboarding/01-01-SUMMARY.md
</context>

<interfaces>
<!-- From Plan 01 - Extension Supabase client the executor needs -->

From extension/src/lib/supabase.ts:
```typescript
import { createClient } from '@supabase/supabase-js'

// Custom chrome.storage.local adapter
const chromeStorageAdapter = {
  getItem: async (key: string): Promise<string | null> => { ... },
  setItem: async (key: string, value: string): Promise<void> => { ... },
  removeItem: async (key: string): Promise<void> => { ... },
}

export const supabase = createClient(SUPABASE_URL, SUPABASE_ANON_KEY, {
  auth: { storage: chromeStorageAdapter, autoRefreshToken: true, persistSession: true, detectSessionInUrl: false }
})
```

From backend/app/main.py:
```python
from fastapi import FastAPI
app = FastAPI(title="HomeMatch API", version="0.1.0")

@app.get("/health")
async def health_check():
    return {"status": "healthy", "service": "homematch-api"}
```

From supabase/functions/score-proxy/index.ts:
```typescript
// Edge function scaffold -- already has CORS, auth verification, EC2 proxy structure
// EC2_BACKEND_URL from Deno.env
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Implement extension popup auth (login form and logged-in dashboard)</name>
  <files>
    extension/src/entrypoints/popup/App.tsx
    extension/src/entrypoints/popup/style.css
    extension/src/components/popup/LoginForm.tsx
    extension/src/components/popup/Dashboard.tsx
  </files>
  <action>
    1. Create LoginForm component (extension/src/components/popup/LoginForm.tsx):
       - React component with email/password state, error state, loading state.
       - On submit: call supabase.auth.signInWithPassword({ email, password }).
       - Display error message if login fails.
       - Use shadcn/ui Button, Input, Label components.
       - Inline form directly in popup (NO redirect to website, per user decision).
       - Compact layout suitable for popup dimensions (~400px wide, ~500px tall).
       - Include a note "Don't have an account? Sign up at [website URL]" (signup happens on website, not in extension popup -- extension popup is login only).

    2. Create Dashboard component (extension/src/components/popup/Dashboard.tsx):
       - Shows logged-in user's email.
       - Logout button: calls supabase.auth.signOut().
       - "Open HomeMatch" link that opens the Next.js website in a new tab (use chrome.tabs.create or window.open).
       - Minimal layout: user email + logout + link. Nothing else in Phase 1.

    3. Update App.tsx (extension/src/entrypoints/popup/App.tsx):
       - Use useState for session/user state.
       - On mount: call supabase.auth.getSession() to check initial state.
       - Subscribe to supabase.auth.onAuthStateChange() to react to login/logout.
       - Render LoginForm when no session, Dashboard when session exists.
       - Show a brief loading state while checking initial session.
       - Clean up subscription on unmount.
       - IMPORTANT: Use getUser() for actual auth verification if making any server-side decisions, but for popup UI switching, getSession() is acceptable (client-side only).

    4. Update popup style.css to import globals.css and set popup body dimensions:
       ```css
       @import '../assets/styles/globals.css';
       body { width: 400px; min-height: 300px; }
       ```

    5. Update extension/.env with real Supabase credentials (same project as web app).

    6. Verify extension builds and popup works:
       ```bash
       cd extension && pnpm run build
       ```
       Then load unpacked extension in Chrome from extension/.output/chrome-mv3 and test popup.
  </action>
  <verify>
    <automated>cd /Users/maximgusev/workspace/gen-ai-hackathon/extension && pnpm run build 2>&1 | tail -5 && echo "BUILD OK"</automated>
  </verify>
  <done>
    - Extension popup shows login form when logged out
    - After successful login, popup shows user email + logout + "Open HomeMatch" link
    - Logout returns to login form
    - Extension builds without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy FastAPI to EC2, deploy edge function, and verify full infrastructure</name>
  <files>
    backend/.env
    supabase/.env
  </files>
  <action>
    Before presenting this checkpoint, the executor will:
    1. Deploy FastAPI to EC2:
       - Build Docker image: `cd backend && docker build -t homematch-api .`
       - Transfer to EC2 via `docker save homematch-api | gzip | ssh [ec2-host] "gunzip | docker load"` or build directly on EC2
       - Run container: `docker run -d --restart=unless-stopped -p 8000:8000 --name homematch-api homematch-api`
       - Verify health: `curl http://[EC2-IP]:8000/health`
       - Configure EC2 security group to allow inbound port 8000 via AWS CLI

    2. Link Supabase project and deploy:
       - `supabase link --project-ref [project-ref]`
       - Push database migrations: `supabase db push`
       - Set edge function secret: `supabase secrets set EC2_BACKEND_URL=http://[EC2-IP]:8000`
       - Deploy edge function: `supabase functions deploy score-proxy`

    3. Build extension for manual testing:
       - `cd extension && pnpm run build`
       - Output at extension/.output/chrome-mv3/
  </action>
  <verify>
    <automated>echo "Manual verification required -- see how-to-verify steps below"</automated>
  </verify>
  <done>
    - EC2 health check returns 200
    - Edge function returns 401 for unauthenticated requests
    - Extension popup login works with website credentials
    - Human has verified all infrastructure end-to-end
  </done>
  <what-built>
    Complete infrastructure: FastAPI on EC2, Supabase edge function proxying to EC2, extension popup auth.
  </what-built>
  <how-to-verify>
    1. **EC2 Backend**: Run `curl http://[EC2-IP]:8000/health` -- should return {"status": "healthy", "service": "homematch-api"}
    2. **Edge Function Auth Rejection**: Run the curl command shown by executor to call the edge function without auth -- should return 401
    3. **Extension Popup Auth**:
       a. Open Chrome, go to chrome://extensions, enable Developer mode
       b. Click "Load unpacked" and select the extension/.output/chrome-mv3 directory
       c. Click the extension icon in toolbar to open popup
       d. Enter the same email/password you used on the website (from Plan 02 testing)
       e. Should see your email + logout button + "Open HomeMatch" link
       f. Click "Open HomeMatch" -- should open the Next.js website
       g. Click "Logout" -- should return to login form
    4. **Cross-auth verification**: Log in on website, then log in on extension with same credentials -- both should work independently
  </how-to-verify>
  <resume-signal>Type "approved" if all infrastructure works, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- Extension popup login works with Supabase credentials
- FastAPI health check responds on EC2
- Edge function returns 401 for unauthenticated requests
- Edge function proxies authenticated requests to EC2 backend
- Same credentials work on both website and extension
</verification>

<success_criteria>
- AUTH-02: User can log in via the Chrome extension popup using the same Supabase credentials
- AUTH-03: Supabase edge functions proxy scoring requests to EC2 backend with auth validation
- INFRA-02: FastAPI backend deployed on EC2 via Docker
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-onboarding/01-03-SUMMARY.md`
</output>
