---
phase: 39-critical-handoffs
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - web/src/__tests__/critical-handoffs.test.tsx
autonomous: true
requirements:
  - HND-01
  - HND-02
  - HND-03
  - HND-04

must_haves:
  truths:
    - "Test file exists with failing RED tests for all 4 HND requirements"
  artifacts:
    - path: "web/src/__tests__/critical-handoffs.test.tsx"
      provides: "Test scaffold for HND-01 through HND-04"
      contains: "Save & Open in Flatfox"
  key_links: []
---

<objective>
Create the Wave 0 test scaffold for Phase 39 Critical Handoffs requirements.

Purpose: Nyquist compliance -- all 4 HND requirements need failing RED tests before implementation begins in Plans 01 and 02.
Output: `web/src/__tests__/critical-handoffs.test.tsx` with tests for HND-01 through HND-04 in deliberate RED state.
</objective>

<execution_context>
@/Users/singhs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/singhs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-critical-handoffs/39-CONTEXT.md
@.planning/phases/39-critical-handoffs/39-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From web/src/components/preferences/preferences-form.tsx:
```typescript
interface PreferencesFormProps {
  defaultValues: Preferences
  onSave: (data: Preferences) => Promise<void>
  profileId?: string
  profileName?: string
  form?: UseFormReturn<Preferences>
}
export function PreferencesForm({ defaultValues, onSave, profileId, profileName, form: externalForm }: PreferencesFormProps)
```

From web/src/components/analyses/analyses-filter-bar.tsx:
```typescript
interface Props {
  profiles: { id: string; name: string }[]
  currentProfile: string
  currentSort: string
  lang: Language
}
export function AnalysesFilterBar({ profiles, currentProfile, currentSort, lang }: Props)
```

From web/src/components/ui/button.tsx:
```typescript
export { Button, buttonVariants }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create critical-handoffs test scaffold</name>
  <files>web/src/__tests__/critical-handoffs.test.tsx</files>
  <action>
Create `web/src/__tests__/critical-handoffs.test.tsx` with vitest + @testing-library/react tests in deliberate RED state. Follow existing test patterns in the web app (see `web/src/__tests__/` for mock patterns).

**HND-01 tests (PreferencesForm sticky bar):**
- Test: renders "Save Preferences" button initially (before any save)
- Test: after successful save, button text changes to contain "Save & Open in Flatfox"
- Test: sticky bar container has `sticky bottom-0` positioning classes

**HND-02 tests (progress indicator):**
- Test: renders section progress indicator with section count (e.g. matches `/\d+ of 6/` or `/Step \d/`)
- Test: progress indicator contains section name text

**HND-03 tests (analyses empty state):**
- Test: when 0 analyses, renders "Open Flatfox" link with `href` containing `flatfox.ch`
- Test: when 0 analyses, renders "Download" link with `href="/download"`
- Test: when analyses exist, does NOT render empty state CTAs

**HND-04 tests (filter bar conditional):**
- Test: AnalysesFilterBar returns null when `analysisCount` is 0
- Test: AnalysesFilterBar renders when `analysisCount` > 0

Mock patterns to follow:
- Mock `@/lib/translations` with `vi.mock` returning key names (established pattern from existing tests)
- Mock `@/lib/language-context` with `useLanguage` returning `{ language: 'en' }`
- Mock `next/navigation` with `useRouter` returning `{ push: vi.fn() }`
- Mock `@/components/onboarding/OnboardingProvider` with `useOnboardingContext` returning default state
- Mock `react-hook-form` or pass a real form instance for PreferencesForm tests
- Use `render()` + `screen.getByText()` / `screen.queryByText()` pattern

All tests should FAIL (RED) because the production code does not yet have:
- Sticky bar markup
- Progress indicator
- Empty state CTAs
- `analysisCount` prop on AnalysesFilterBar

NOTE: Follow Phase 38 precedent -- hardcode English strings in test assertions (not translation keys).
  </action>
  <verify>
    <automated>cd /Users/singhs/gen-ai-hackathon/web && npx vitest run src/__tests__/critical-handoffs.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Test file exists with 9-11 test cases, all in RED (failing) state. Tests cover HND-01 through HND-04 requirements.</done>
</task>

</tasks>

<verification>
- `cd web && npx vitest run src/__tests__/critical-handoffs.test.tsx --reporter=verbose` shows all tests failing (RED state)
- No tests from other files are broken by the new test file
</verification>

<success_criteria>
- Test scaffold file exists at `web/src/__tests__/critical-handoffs.test.tsx`
- Tests cover all 4 HND requirements
- All tests are in RED (failing) state -- production code changes come in Plans 01 and 02
</success_criteria>

<output>
After completion, create `.planning/phases/39-critical-handoffs/39-00-SUMMARY.md`
</output>
