---
phase: 23-hackathon-credits-section
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/eth-zurich-white.svg
  - public/zurich-bg.jpg
  - src/lib/translations.ts
  - src/components/landing/SectionCredits.tsx
  - src/components/landing/SectionHero.tsx
  - src/components/landing/LandingPageContent.tsx
  - src/app/auth/page.tsx
autonomous: false
requirements:
  - CRED-01
  - CRED-02

must_haves:
  truths:
    - "Landing page has a credits section above the footer showing ETH Zürich logo and GenAI Zürich Hackathon 2026 badge"
    - "Credits section displays 'A project from' label with ETH logo | divider | hackathon badge layout"
    - "Hero section has a dimmed Zurich cityscape photo that fades to invisible as user scrolls"
    - "Auth page shows the Zurich cityscape as a full-screen background with login card floating over it"
    - "Auth page has 'A project from' credits strip at the bottom"
    - "TypeScript build passes with no errors — translation key parity maintained"
  artifacts:
    - path: "public/eth-zurich-white.svg"
      provides: "ETH Zürich white wordmark SVG"
    - path: "public/zurich-bg.jpg"
      provides: "Zurich aerial dusk cityscape photo"
    - path: "src/components/landing/SectionCredits.tsx"
      provides: "Credits section component"
      exports: ["SectionCredits"]
    - path: "src/lib/translations.ts"
      provides: "Translation keys for credits label"
      contains: "landing_credits_label"
  key_links:
    - from: "src/components/landing/LandingPageContent.tsx"
      to: "src/components/landing/SectionCredits.tsx"
      via: "<SectionCredits lang={lang} /> between SectionCTA and LandingFooter"
      pattern: "SectionCredits"
    - from: "src/components/landing/SectionHero.tsx"
      to: "public/zurich-bg.jpg"
      via: "next/image fill + motion.div with useScroll+useTransform opacity"
      pattern: "hero-photo"
    - from: "src/app/auth/page.tsx"
      to: "public/zurich-bg.jpg"
      via: "absolute fill Image on outer div with relative positioning"
      pattern: "zurich-bg"
---

<objective>
Add Zurich cityscape photo and hackathon credits to three surfaces: hero section (dimmed scroll-fade background), new SectionCredits component (photo fill with ETH + hackathon logo overlay above footer), and auth page (full-screen photo with floating login card and credits strip at bottom).

Purpose: Connect HomeMatch visually to its ETH Zurich and Gen-AI Hackathon origins — a brand identity bookend from the landing page hero to the auth sign-up page.
Output: SectionCredits.tsx (new), modified SectionHero + LandingPageContent + auth/page.tsx, two public assets, translation keys.
</objective>

<execution_context>
@/Users/singhs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/singhs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/components/landing/LandingPageContent.tsx
@src/components/landing/SectionHero.tsx
@src/components/landing/SectionCTA.tsx
@src/app/auth/page.tsx
@src/lib/translations.ts
</context>

<interfaces>
<!-- Key types and patterns the executor needs. Extracted from codebase. -->

From src/lib/translations.ts:
```typescript
export type Language = 'en' | 'de'
export function t(lang: Language, key: keyof typeof translations.en): string

// Translation key parity enforcement:
type _DeHasAllEnKeys = keyof typeof translations.en extends keyof typeof translations.de ? true : false
const _deKeyCheck: _DeHasAllEnKeys = true
// WARNING: Any new key added to `en` MUST also be added to `de` or the build fails.
```

From src/components/landing/SectionCTA.tsx (established pattern for sections with props):
```typescript
export function SectionCTA({ lang }: { lang: Language }) { ... }
// SectionCredits should follow same signature: ({ lang }: { lang: Language })
```

From src/components/landing/LandingPageContent.tsx (insertion point):
```typescript
// Current: <SectionCTA lang={lang} /> → <LandingFooter lang={lang} />
// Target:  <SectionCTA lang={lang} /> → <SectionCredits lang={lang} /> → <LandingFooter lang={lang} />
```

From src/components/landing/SectionHero.tsx (existing structure to amend):
```typescript
// Section element: className="relative min-h-screen flex flex-col items-center justify-center text-center px-6 py-24 overflow-hidden"
// First child: dot grid div (aria-hidden, absolute inset-0 z-0)
// Photo layer goes FIRST inside section, before dot grid (z-[-1] so it sits behind everything)
// Pattern: 'use client' already present; useScroll + useTransform are not yet imported
```

From src/app/auth/page.tsx (outer wrapper to modify):
```typescript
// Current outer div: <div className="flex min-h-screen items-center justify-center bg-background px-4">
// Target:  relative, photo fills it (absolute inset-0), remove bg-background, keep flex min-h-screen items-center justify-center px-4
// Credits strip: absolute bottom-8 left-0 right-0, centered
```

Framer Motion scroll-fade pattern (from LandingNavbar.tsx — confirmed in codebase):
```typescript
import { useScroll, useTransform } from 'motion/react'
const { scrollY } = useScroll()
// Hero photo: starts at opacity 0.25 at scroll=0, fades to 0 at scroll=600
const photoOpacity = useTransform(scrollY, [0, 600], [0.25, 0])
// Wrap Image in: <motion.div style={{ opacity: photoOpacity }} className="absolute inset-0 ..." aria-hidden>
```

next/image fill pattern (anti-patterns documented in RESEARCH.md):
```typescript
import Image from 'next/image'
// Parent must have position:relative (or absolute) with explicit size
<Image src="/zurich-bg.jpg" alt="" fill className="object-cover" priority />
// Only hero gets priority=true; credits section and auth page omit priority (lazy load)
// Wrap in motion.div to animate opacity — do NOT animate Image directly
```

GenAI Zürich Hackathon Badge (recreated in code — no file asset):
```typescript
// Brand colors: green #50e75f on black #000000
// Structure: inline-flex rounded-full pill with green dot + text
// Text: "GenAI Zürich Hackathon 2026"
// Year is 2026 — NOT 2025 as shown in reference screenshot
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Acquire assets, add translation keys, create SectionCredits component</name>
  <files>
    public/eth-zurich-white.svg,
    public/zurich-bg.jpg,
    src/lib/translations.ts,
    src/components/landing/SectionCredits.tsx
  </files>
  <action>
**Step 1 — Download ETH Zürich white SVG:**

```bash
curl -L "https://upload.wikimedia.org/wikipedia/commons/a/a7/ETH_Z%C3%BCrich_Logo_white.svg" \
  -o /Users/singhs/gen-ai-hackathon/web/public/eth-zurich-white.svg
```

After download, inspect the SVG. If any fill colors are not white (not `#fff`, `#ffffff`, or `white`), add `style="fill: white"` to the root SVG element. The file is 3KB — it is the official white wordmark variant so fill should already be white.

**Step 2 — Acquire Zurich aerial dusk photo:**

Use the Unsplash source API to download a high-res Zurich aerial dusk image. Try this direct Unsplash source URL (dusk cityscape, Lake Zurich visible):

```bash
curl -L "https://images.unsplash.com/photo-1515488764276-beab7607c1e6?w=2400&q=80" \
  -o /Users/singhs/gen-ai-hackathon/web/public/zurich-bg.jpg
```

If that URL fails or returns non-JPEG content, use an alternative:
```bash
curl -L "https://images.unsplash.com/photo-1543716091-a840c05249ec?w=2400&q=80" \
  -o /Users/singhs/gen-ai-hackathon/web/public/zurich-bg.jpg
```

Verify the file is a valid JPEG (not a 404 HTML page) by checking size: it should be > 100KB. If both fail, download any Zurich aerial sunset JPEG from Unsplash manually and place at `public/zurich-bg.jpg`.

**Step 3 — Add translation keys to src/lib/translations.ts:**

In the `en` object, add a new key after the `landing_cta_button` entry (before the closing `},`):

```typescript
// Section Credits
landing_credits_label: 'A project from',
```

In the `de` object, add the same key in the same position:

```typescript
// Section Credits
landing_credits_label: 'A project from',
```

Both `en` and `de` must receive the key simultaneously to satisfy the `_deKeyCheck` TypeScript enforcement. The value is English in both locales — "A project from" is a proper brand phrase, no translation needed.

**Step 4 — Create src/components/landing/SectionCredits.tsx:**

Create a new file at `src/components/landing/SectionCredits.tsx`. It is a `'use client'` component (needs useRef + useInView).

Structure:
- Outer `<section>` with `className="relative overflow-hidden"` and `style={{ minHeight: '50vh' }}` — gives the photo room to show without competing with SectionCTA
- Photo layer: `<div className="absolute inset-0" aria-hidden>` containing:
  - `<Image src="/zurich-bg.jpg" alt="" fill className="object-cover" />` (no `priority` — lazy load)
  - Dark gradient overlay: `<div className="absolute inset-0" style={{ background: 'linear-gradient(to bottom, hsl(0 0% 0% / 0.65) 0%, hsl(0 0% 0% / 0.75) 100%)' }} />`
- Main content: `<div ref={ref} className="relative z-10 flex flex-col items-center justify-center min-h-[50vh] py-16 px-6 text-center">`
  - Fade-in via useInView (same pattern as SectionProblem/SectionCTA — `useInView(ref, { once: false, amount: 0.3 })` on a `motion.div`, no `initial` prop, `animate={{ opacity: isInView ? 1 : 0, y: isInView ? 0 : 20 }}`)
  - Label: `<p className="text-xs uppercase tracking-widest font-semibold mb-8" style={{ color: 'hsl(0 0% 55%)' }}>` with `t(lang, 'landing_credits_label')`
  - Logo row: `<div className="flex items-center gap-6">` containing:
    1. ETH logo: `<img src="/eth-zurich-white.svg" alt="ETH Zürich" className="h-8 w-auto opacity-90" />`
    2. Vertical divider: `<div className="h-8 w-px bg-white/20" aria-hidden />`
    3. HackathonBadge component (inline in same file, not exported): black pill with green dot + "GenAI Zürich Hackathon 2026"

HackathonBadge implementation (define as a local function above SectionCredits):
```typescript
function HackathonBadge() {
  return (
    <div
      className="inline-flex items-center gap-1.5 rounded-full px-3 py-1.5 text-sm font-semibold"
      style={{
        backgroundColor: '#000000',
        color: '#50e75f',
        border: '1px solid #50e75f',
      }}
    >
      <span
        className="h-2 w-2 rounded-full flex-shrink-0"
        style={{ backgroundColor: '#50e75f' }}
      />
      GenAI Zürich Hackathon 2026
    </div>
  )
}
```

Imports needed: `'use client'`, `useRef` from react, `motion, useInView` from `'motion/react'`, `Image` from `'next/image'`, `t` and `Language` from `'@/lib/translations'`.

Do NOT add `initial` prop to the motion.div — per STATE.md: "x slide-in uses no initial prop — Framer Motion derives start state from animate value at mount when isInView=false". Same rule applies here.
  </action>
  <verify>
    <automated>cd /Users/singhs/gen-ai-hackathon/web && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `public/eth-zurich-white.svg` exists and is a valid SVG file (> 0 bytes)
    - `public/zurich-bg.jpg` exists and is a valid JPEG (> 100KB)
    - `src/lib/translations.ts` has `landing_credits_label` in both `en` and `de` objects
    - `src/components/landing/SectionCredits.tsx` exports `SectionCredits` component
    - `npx tsc --noEmit` passes with no errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Hero photo scroll-fade layer, LandingPageContent wiring, auth page photo background</name>
  <files>
    src/components/landing/SectionHero.tsx,
    src/components/landing/LandingPageContent.tsx,
    src/app/auth/page.tsx
  </files>
  <action>
**Modification 1 — src/components/landing/SectionHero.tsx:**

Add `useScroll`, `useTransform` to the existing `motion/react` import:
```typescript
import { motion, useReducedMotion, useScroll, useTransform } from 'motion/react'
```

Add `Image` import from `'next/image'`.

Inside `SectionHero` function body, before the return, add:
```typescript
const { scrollY } = useScroll()
const photoOpacity = useTransform(scrollY, [0, 600], [0.25, 0])
```

Inside the `<section>` element, add the photo layer as the VERY FIRST child (before the dot grid div). The photo layer must be `z-[-1]` so it sits behind the dot grid and all other elements:

```tsx
{/* Zurich cityscape photo — scroll-fades from 25% to 0% */}
<motion.div
  aria-hidden
  data-testid="hero-photo"
  className="absolute inset-0 overflow-hidden pointer-events-none"
  style={{ opacity: photoOpacity, zIndex: -1 }}
>
  <Image
    src="/zurich-bg.jpg"
    alt=""
    fill
    className="object-cover"
    priority
  />
  {/* Dark overlay for contrast */}
  <div
    className="absolute inset-0"
    style={{ background: 'linear-gradient(to bottom, hsl(0 0% 0% / 0.55), hsl(0 0% 0% / 0.65))' }}
  />
</motion.div>
```

No other changes to SectionHero.tsx. Existing dot grid, glow, orbs, chips, and content are untouched.

**Modification 2 — src/components/landing/LandingPageContent.tsx:**

Add import: `import { SectionCredits } from './SectionCredits'`

Insert `<SectionCredits lang={lang} />` between `<SectionCTA lang={lang} />` and `<LandingFooter lang={lang} />`:

```tsx
<SectionCTA lang={lang} />
<SectionCredits lang={lang} />
<LandingFooter lang={lang} />
```

No other changes.

**Modification 3 — src/app/auth/page.tsx:**

This is a `'use client'` component. Add imports at the top:
```typescript
import Image from 'next/image'
```

Modify the outer `<div>` that currently reads `className="flex min-h-screen items-center justify-center bg-background px-4"`:
- Remove `bg-background`
- Add `relative` (to anchor absolute children)
- Result: `className="relative flex min-h-screen items-center justify-center px-4"`

Inside that outer div, add the photo layer as the FIRST child (before the existing logo/card content):

```tsx
{/* Zurich cityscape photo background */}
<div className="absolute inset-0 overflow-hidden" aria-hidden>
  <Image
    src="/zurich-bg.jpg"
    alt=""
    fill
    className="object-cover"
  />
  {/* Dark overlay */}
  <div
    className="absolute inset-0"
    style={{ background: 'hsl(0 0% 0% / 0.55)' }}
  />
</div>
```

Note: No `priority` on auth page image — it is not above-the-fold critical path.

Also add a credits strip at the bottom of the page. After the existing `<div className="w-full max-w-sm space-y-6">` block (the logo + card wrapper), add:

```tsx
{/* Credits strip at bottom */}
<div className="absolute bottom-8 left-0 right-0 flex flex-col items-center gap-3 z-10">
  <p className="text-xs uppercase tracking-widest font-semibold" style={{ color: 'hsl(0 0% 55%)' }}>
    A project from
  </p>
  <div className="flex items-center gap-5">
    <img src="/eth-zurich-white.svg" alt="ETH Zürich" className="h-7 w-auto opacity-90" />
    <div className="h-7 w-px bg-white/20" aria-hidden />
    <div
      className="inline-flex items-center gap-1.5 rounded-full px-3 py-1.5 text-sm font-semibold"
      style={{ backgroundColor: '#000000', color: '#50e75f', border: '1px solid #50e75f' }}
    >
      <span className="h-2 w-2 rounded-full flex-shrink-0" style={{ backgroundColor: '#50e75f' }} />
      GenAI Zürich Hackathon 2026
    </div>
  </div>
</div>
```

The existing content (logo, card, sign-in/sign-up form) is completely unchanged. Only the outer div's bg class, the photo layer, and the credits strip are added.
  </action>
  <verify>
    <automated>cd /Users/singhs/gen-ai-hackathon/web && npm run build 2>&1 | tail -30</automated>
  </verify>
  <done>
    - `SectionHero.tsx` has `data-testid="hero-photo"` motion.div as first child of section, using useScroll+useTransform opacity
    - `LandingPageContent.tsx` renders SectionCredits between SectionCTA and LandingFooter
    - `src/app/auth/page.tsx` outer div has `relative` class, Zurich photo as absolute background, credits strip at bottom
    - `npm run build` exits 0 with no TypeScript or compilation errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of all three Zurich photo surfaces</name>
  <what-built>
    Three-surface Zurich photo and credits integration:
    1. Landing hero — dimmed Zurich cityscape fades as user scrolls (25% opacity → 0%)
    2. Landing credits section (above footer) — full-bleed Zurich photo with "A project from" + ETH logo + GenAI Zürich Hackathon 2026 badge overlaid
    3. Auth page — full-screen Zurich photo, login card floats over it, credits strip at bottom of viewport
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000
    2. Landing page hero: scroll slowly — Zurich cityscape should be faintly visible at top, fade away as you scroll past hero
    3. Scroll to bottom of landing page: credits section appears just above footer with Zurich photo clearly visible, "A project from" label, ETH Zürich white logo, divider, and "GenAI Zürich Hackathon 2026" green badge
    4. Navigate to http://localhost:3000/auth: full-screen Zurich photo fills viewport, login card floats centered, credits strip visible at bottom (ETH logo + hackathon badge)
    5. Confirm GenAI badge text reads "GenAI Zürich Hackathon 2026" (2026, not 2025)
    6. Confirm ETH logo is white and legible on dark background
    7. Confirm existing login/sign-up form functionality is unchanged (try typing in email field)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual issues</resume-signal>
  <action>Human visual verification of hero photo scroll-fade, credits section layout, and auth page photo background.</action>
  <verify><automated>echo "human checkpoint — no automated verify"</automated></verify>
  <done>User types "approved" confirming all three surfaces render correctly.</done>
</task>

</tasks>

<verification>
After both auto tasks complete:
- `npx tsc --noEmit` passes with zero errors (translation key parity maintained)
- `npm run build` exits 0 (no runtime import errors, no missing assets)
- `public/eth-zurich-white.svg` and `public/zurich-bg.jpg` exist in working directory
- SectionCredits is exported and imported in LandingPageContent
- SectionHero has `data-testid="hero-photo"` present in rendered output
</verification>

<success_criteria>
1. New SectionCredits component renders ETH Zürich logo + "GenAI Zürich Hackathon 2026" badge with "A project from" label, above LandingFooter
2. SectionCredits has Zurich photo as full-section background with dark overlay — logos legible
3. Hero section has Zurich photo dimmed to ~25% opacity, fades to 0 as user scrolls down
4. Auth page shows Zurich photo full-screen, login card floats centered, credits strip at bottom
5. TypeScript build passes — `landing_credits_label` added to both `en` and `de` objects
6. No existing section's functionality or appearance is changed (hero chips, problem cards, solution steps, CTA all unchanged)
</success_criteria>

<output>
After completion, create `.planning/phases/23-hackathon-credits-section/23-01-SUMMARY.md` following the summary template at `@/Users/singhs/.claude/get-shit-done/templates/summary.md`.
</output>
```
