---
phase: 28-deterministic-scorer
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - backend/tests/test_deterministic_scorer.py
autonomous: true
requirements:
  - DS-01
  - DS-02
  - DS-03
  - DS-04
  - DS-05
  - DS-06

must_haves:
  truths:
    - "test_deterministic_scorer.py exists with test classes for all 6 criterion types"
    - "Running pytest on the file fails (RED) — implementation file does not yet exist"
    - "Each test class has at least 2-3 concrete test methods with specific assertions"
  artifacts:
    - path: "backend/tests/test_deterministic_scorer.py"
      provides: "Failing test scaffold covering DS-01 through DS-06"
      contains: "TestPriceScorer, TestDistanceScorer, TestSizeScorer, TestBinaryFeatureScorer, TestProximityQualityScorer, TestBuiltinSynthesizer"
  key_links:
    - from: "backend/tests/test_deterministic_scorer.py"
      to: "backend/app/services/deterministic_scorer.py"
      via: "from app.services.deterministic_scorer import ..."
      pattern: "from app\\.services\\.deterministic_scorer import"
---

<objective>
Write a comprehensive pytest test file covering all six deterministic scorer requirements (DS-01 through DS-06). Tests must fail (RED state) because the implementation module does not yet exist. This is Wave 0 — define the contract before writing the code.

Purpose: TDD red phase. Tests define the exact API surface and behavioral expectations that Plan 02 must satisfy.
Output: backend/tests/test_deterministic_scorer.py with 6 test classes, ~20 test methods total, all failing with ImportError.
</objective>

<execution_context>
@/Users/singhs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/singhs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-deterministic-scorer/28-CONTEXT.md
@.planning/phases/28-deterministic-scorer/28-RESEARCH.md
</context>

<feature>
  <name>Deterministic Scorer Test Scaffold</name>
  <files>backend/tests/test_deterministic_scorer.py</files>
  <behavior>
    DS-01 (price):
    - score_price(field_with_budget_2000, listing_price_1800) == 1.0  [at or under budget]
    - score_price(field_with_budget_2000, listing_price_2500) == approx(exp(-2.5 * 500/2000), abs=0.001)
    - score_price(field_with_budget_2000, listing_price_None) is None  [missing price → skip]
    - score_price(field_with_invalid_value, listing) is None  [unparseable budget → skip]

    DS-02 (distance):
    - score_distance(field_with_target_500m, actual_distance_300m) == 1.0
    - score_distance(field_with_target_500m, actual_distance_800m) == approx(exp(-1.0 * 300/500), abs=0.001)
    - score_distance(field_no_data, listing_no_proximity) is None

    DS-03 (size):
    - score_size(field_min_60, listing_surface_45) == approx((45/60)**1.5, abs=0.001)  [below min]
    - score_size(field_min_60_max_100, listing_surface_80) == 1.0  [in range]
    - score_size(field_min_60_max_100, listing_surface_45) < 1.0  [below min]
    - score_size(field_min_60_max_100, listing_surface_130) < 1.0  [above max, soft decay]
    - score_size(field_min_60_max_None, listing_surface_200) == 1.0  [above min, no upper bound]
    - score_size(field_min_60, listing_surface_None) is None  [missing data → skip]

    DS-04 (binary feature):
    - score_binary_feature(field_name_balcony, listing_with_balconygarden) == 1.0  [alias match]
    - score_binary_feature(field_name_balkon, listing_with_balconygarden) == 1.0  [German alias]
    - score_binary_feature(field_name_lift, listing_without_lift) == 0.0  [absent → 0]
    - score_binary_feature(field_name_unknown_xyz, listing) is None  [unrecognized → skip]
    - score_binary_feature(field_name_parking, listing_with_parkingspace) == 1.0  [compound slug]

    DS-05 (proximity quality):
    - score_proximity_quality(field, listing, proximity_data_with_match) returns float in [0, 1]
    - score_proximity_quality with is_fallback=True uses fallback distance_km in formula
    - score_proximity_quality with no match in proximity_data returns None

    DS-06 (built-in synthesizer):
    - synthesize_builtin_results(prefs_with_budget_2000_dealbreaker, listing) returns entry with importance=CRITICAL
    - synthesize_builtin_results(prefs_with_budget_no_dealbreaker, listing) returns entry with importance=MEDIUM
    - synthesize_builtin_results(prefs_with_budget_None, listing) returns list WITHOUT budget entry
    - synthesize_builtin_results(prefs_complete, listing) returns up to 3 entries (budget, rooms, living_space)
    - synthesize_builtin_results(prefs_no_rooms_min, listing) omits rooms entry
  </behavior>
  <implementation>
    Write ONLY the test file. Do NOT create deterministic_scorer.py — it must not exist so tests fail.

    Structure:
    1. Imports from app.services.deterministic_scorer (these will fail on import)
    2. Helper fixtures: make_field(name, value, importance), make_listing(price, surface, rooms, attributes), make_prefs(...)
    3. Six test classes: TestPriceScorer, TestDistanceScorer, TestSizeScorer, TestBinaryFeatureScorer, TestProximityQualityScorer, TestBuiltinSynthesizer
    4. Each class has 3-5 test methods matching the behavior spec above

    Use pytest.approx() for float comparisons. Use math.exp() directly in expected value calculations to avoid magic numbers.

    Run pytest after writing — confirm it fails with ModuleNotFoundError or ImportError (not with assertion errors, which would indicate deterministic_scorer.py already exists).
  </implementation>
</feature>

<verification>
```bash
cd /Users/singhs/gen-ai-hackathon/backend && python3.11 -m pytest tests/test_deterministic_scorer.py -x 2>&1 | head -20
```
Expected: ModuleNotFoundError for `app.services.deterministic_scorer` — confirms RED state.
</verification>

<success_criteria>
- test_deterministic_scorer.py exists in backend/tests/
- File has 6 test classes and at least 18 test methods total
- `pytest tests/test_deterministic_scorer.py` exits with error due to ImportError (RED state confirmed)
- No deterministic_scorer.py exists in backend/app/services/
</success_criteria>

<output>
After completion, create `.planning/phases/28-deterministic-scorer/28-01-SUMMARY.md`
</output>
