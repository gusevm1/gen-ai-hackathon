---
phase: 14-chat-ui-navigation
plan: 03
type: execute
wave: 2
depends_on: [14-01, 14-02]
files_modified: []
autonomous: false
requirements: [CHAT-06, CHAT-07]

must_haves:
  truths:
    - "Full chat flow works end-to-end: navigate to AI-Powered Search, type description, click Start Creating Profile, enter name, see conversation with mock AI"
    - "AI-Powered Search nav item is visually distinct with pinkish-red text"
    - "Messages have clear visual distinction -- AI on left with avatar, user on right"
    - "Follow-up messages can be sent and mock AI responds"
  artifacts: []
  key_links: []
---

<objective>
Visual and functional verification of the complete Phase 14 implementation. User confirms nav item styling, chat flow, message thread layout, and typing indicator behavior.

Purpose: Catches visual/interaction issues that automated tests cannot verify (colors, layout transitions, scroll behavior, animation timing).
Output: User approval or issue list for fixes.
</objective>

<execution_context>
@/Users/prakhar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prakhar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-chat-ui-navigation/14-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and verify no regressions</name>
  <files></files>
  <read_first>
    - web/src/components/chat/chat-page.tsx (verify it exists and exports ChatPage)
    - web/src/components/top-navbar.tsx (verify AI-Powered Search is first item)
  </read_first>
  <action>
Run the full Vitest suite to confirm all existing tests still pass and all new Phase 14 tests pass:
```bash
cd web && npx vitest run --reporter=verbose
```

Also verify CHAT-08 (ephemeral) manually:
```bash
grep -rn "supabase\|createClient" web/src/components/chat/ web/src/app/\(dashboard\)/ai-search/ || echo "PASS: No Supabase imports in chat components"
```

If any tests fail, fix the issues before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>cd /Users/prakhar/Desktop/GenAI_hackathon/gen-ai-hackathon/web && npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Vitest output shows all tests passing (0 failures)
    - grep for supabase/createClient in chat components returns no matches
  </acceptance_criteria>
  <done>Full test suite passes with zero failures. No Supabase imports in chat components confirmed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification of complete chat flow</name>
  <files></files>
  <read_first>
    - .planning/phases/14-chat-ui-navigation/14-CONTEXT.md (reference for expected visual behavior)
  </read_first>
  <action>
Present the user with verification steps for the complete Phase 14 implementation. The user must manually test the full flow in the browser.
  </action>
  <what-built>Complete Phase 14: AI-Powered Search nav item + full chat page with three-phase conversation flow (idle, naming, chatting), mock AI responses, typing indicator, and ephemeral session.</what-built>
  <how-to-verify>
    1. Open the web app (localhost:3000 or Vercel preview)
    2. Nav item check: Verify "AI-Powered Search" appears FIRST in the navbar (before Profiles), with pinkish-red text. Click it.
    3. Idle state: You should see a vertically centered textarea with placeholder text about describing your ideal property, and a large "Start Creating Profile" button below it.
    4. Type a description: Enter something like "I am looking for a 3-room apartment in Zurich near a train station, budget around 2500 CHF, must have a balcony"
    5. Click "Start Creating Profile": An inline card should appear asking "What should we call this profile?" with a text input.
    6. Enter a name (e.g., "Zurich Dream") and click "Start Conversation".
    7. Conversation starts: Your original description should appear as the first message (right-aligned, no avatar). After about 1.5 seconds, an AI response should appear (left-aligned, with a circular house-icon avatar in brand colors). Typing indicator (three animated dots) should show during the wait.
    8. Follow-up: Type a follow-up message and send it (Enter key or send button). Verify it appears and mock AI responds again.
    9. Refresh the page: Verify the conversation is gone and you are back to the clean idle state.
    10. Dark mode: Toggle dark mode and verify the chat page looks correct (contrast, backgrounds, avatar colors).
  </how-to-verify>
  <verify>
    <automated>echo "Checkpoint: awaiting human verification"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms AI-Powered Search nav item is first with pinkish-red text
    - User confirms idle state shows centered textarea with placeholder and Start Creating Profile button
    - User confirms profile name prompt appears inline after clicking Start
    - User confirms conversation flow works with mock AI responses and typing indicator
    - User confirms page refresh resets to idle state
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
  <done>User approves the complete Phase 14 visual and functional experience.</done>
</task>

</tasks>

<verification>
- Full test suite green
- User visual approval of complete chat flow
</verification>

<success_criteria>
- All automated tests pass
- User confirms the visual and functional experience matches expectations
- No regressions in existing app functionality
</success_criteria>

<output>
After completion, create `.planning/phases/14-chat-ui-navigation/14-03-SUMMARY.md`
</output>
