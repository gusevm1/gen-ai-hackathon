---
phase: 33-dashboard-home-nav-polish-profile-creation-flow-and-analyses-titles-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/(dashboard)/dashboard/page.tsx
  - web/src/components/profile-creation-chooser.tsx
  - web/src/components/top-navbar.tsx
  - web/src/lib/translations.ts
autonomous: true
requirements: [HOME-01, HOME-02, NAV-01]

must_haves:
  truths:
    - "User sees 'Welcome to HomeMatch' heading and 'Let's create your profile' subheading on /dashboard"
    - "User sees two side-by-side cards: Manual profile creation and AI-guided profile creation"
    - "Clicking Manual card navigates to CreateProfileDialog (name prompt then /profiles/[id])"
    - "Clicking AI-guided card navigates to /ai-search"
    - "TopNavbar shows Home as the first nav item linking to /dashboard"
  artifacts:
    - path: "web/src/app/(dashboard)/dashboard/page.tsx"
      provides: "Dashboard home page with welcome text and ProfileCreationChooser"
      min_lines: 15
    - path: "web/src/components/profile-creation-chooser.tsx"
      provides: "Reusable two-card chooser component for Manual vs AI profile creation"
      min_lines: 40
    - path: "web/src/components/top-navbar.tsx"
      provides: "TopNavbar with Home nav item added as first entry"
      contains: "nav_home"
    - path: "web/src/lib/translations.ts"
      provides: "Translation keys for Home nav, welcome text, card descriptions"
      contains: "nav_home"
  key_links:
    - from: "web/src/app/(dashboard)/dashboard/page.tsx"
      to: "web/src/components/profile-creation-chooser.tsx"
      via: "import and render ProfileCreationChooser"
      pattern: "ProfileCreationChooser"
    - from: "web/src/components/top-navbar.tsx"
      to: "/dashboard"
      via: "Home nav item URL"
      pattern: "url.*dashboard"
---

<objective>
Create the dashboard home page with welcome text and two profile-creation cards, add Home nav item to TopNavbar, and build the reusable ProfileCreationChooser component.

Purpose: Users currently land on a redirect to /profiles after login. This plan creates a proper welcome page with clear entry points for manual and AI-guided profile creation, and adds a Home nav item so users can always return to it.

Output: New dashboard home page, reusable ProfileCreationChooser component, updated TopNavbar with Home item, new translation keys.
</objective>

<execution_context>
@/Users/prakhar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prakhar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-dashboard-home-nav-polish-profile-creation-flow-and-analyses-titles-fix/33-CONTEXT.md
@.planning/phases/33-dashboard-home-nav-polish-profile-creation-flow-and-analyses-titles-fix/33-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From web/src/components/top-navbar.tsx:
```typescript
// Current navItems array (Home must be prepended):
const navItems = [
  { titleKey: "nav_ai_search" as const, url: "/ai-search", icon: Sparkles, accent: true },
  { titleKey: "nav_profiles" as const, url: "/profiles", icon: User },
  { titleKey: "nav_analyses" as const, url: "/analyses", icon: BarChart3 },
  { titleKey: "nav_download" as const, url: "/download", icon: Download },
  { titleKey: "nav_settings" as const, url: "/settings", icon: Settings },
]
```

From web/src/lib/translations.ts:
```typescript
export type Language = 'en' | 'de'
// Translation keys are typed via the translations object shape.
// Both 'en' and 'de' objects must have identical keys.
```

From web/src/app/(dashboard)/layout.tsx:
```typescript
// Dashboard layout provides: Logo, TopNavbar, ProfileSwitcher, ThemeToggle, NavUser
// Auth guard: redirects to "/" if no user
// Children render inside <main className="flex-1 p-4">
```

From web/src/components/profiles/create-profile-dialog.tsx:
```typescript
interface CreateProfileDialogProps {
  open: boolean
  onOpenChange: (open: boolean) => void
  onCreate: (name: string) => void
}
```

From web/src/components/profiles/profile-list.tsx:
```typescript
// Uses createProfile server action from '@/app/(dashboard)/profiles/actions'
// handleCreate(name: string) calls createProfile(name) then router.refresh()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add translation keys and build ProfileCreationChooser component</name>
  <files>web/src/lib/translations.ts, web/src/components/profile-creation-chooser.tsx</files>
  <action>
1. In `web/src/lib/translations.ts`, add these keys to BOTH `en` and `de` objects:
   - `nav_home`: EN "Home" / DE "Startseite"
   - `dashboard_welcome`: EN "Welcome to HomeMatch" / DE "Willkommen bei HomeMatch"
   - `dashboard_subtitle`: EN "Let's create your profile" / DE "Erstellen Sie Ihr Profil"
   - `dashboard_manual_title`: EN "Manual profile creation" / DE "Manuelle Profilerstellung"
   - `dashboard_manual_desc`: EN "Prefer full control? Fill out a straightforward form and create your profile manually." / DE "Volle Kontrolle gewuenscht? Fuellen Sie ein einfaches Formular aus und erstellen Sie Ihr Profil manuell."
   - `dashboard_ai_title`: EN "AI-guided profile creation" / DE "KI-gesteuerte Profilerstellung"
   - `dashboard_ai_desc`: EN "Tell us exactly what you're looking for and let our AI build your profile automatically." / DE "Sagen Sie uns genau, wonach Sie suchen, und lassen Sie unsere KI Ihr Profil automatisch erstellen."

2. Create `web/src/components/profile-creation-chooser.tsx` as a "use client" component:
   - Props: `{ onManualClick: () => void, onAiClick: () => void }`
   - Renders two side-by-side cards using shadcn `Card` component
   - Left card: Manual — icon `ClipboardList` from lucide-react, title from `dashboard_manual_title`, description from `dashboard_manual_desc`, calls `onManualClick`
   - Right card: AI-guided — icon `Sparkles` from lucide-react, title from `dashboard_ai_title`, description from `dashboard_ai_desc`, calls `onAiClick`
   - Layout: `grid grid-cols-1 md:grid-cols-2 gap-6 max-w-3xl mx-auto`
   - Each card: clickable (cursor-pointer), hover state (hover:border-primary/50 hover:shadow-lg transition-all), min height for visual balance
   - Use `useLanguage()` + `t()` for all text
   - Import Card, CardHeader, CardTitle, CardDescription, CardContent from @/components/ui/card
  </action>
  <verify>cd /Users/prakhar/Desktop/GenAI_hackathon/gen-ai-hackathon/web && npx tsc --noEmit 2>&1 | head -30</verify>
  <done>Translation keys exist in both en/de, ProfileCreationChooser component renders two styled cards with click handlers, TypeScript compiles clean</done>
</task>

<task type="auto">
  <name>Task 2: Replace dashboard redirect with welcome page and add Home nav item</name>
  <files>web/src/app/(dashboard)/dashboard/page.tsx, web/src/components/top-navbar.tsx</files>
  <action>
1. Replace `web/src/app/(dashboard)/dashboard/page.tsx` entirely. Currently it just does `redirect('/profiles')`. New implementation:
   - Make it a "use client" component (needs click handlers for navigation)
   - Import `useRouter` from next/navigation, `ProfileCreationChooser` from @/components/profile-creation-chooser, `CreateProfileDialog` from @/components/profiles/create-profile-dialog, `createProfile` from @/app/(dashboard)/profiles/actions
   - Import `useLanguage`, `t` for translations
   - Render:
     - `<div className="flex flex-col items-center justify-center py-16 px-4">`
     - `<h1>` with `dashboard_welcome` translation key — use `text-3xl font-bold mb-2`
     - `<p>` with `dashboard_subtitle` translation key — use `text-lg text-muted-foreground mb-10`
     - `<ProfileCreationChooser onManualClick={openCreateDialog} onAiClick={goToAiSearch} />`
   - `onManualClick` opens a `CreateProfileDialog` (same as profiles page uses). After name is entered: call `createProfile(name)` server action — it always returns the new profile ID as a string. Capture it: `const id = await createProfile(name)`, then `router.push('/profiles/' + id)`. No conditional needed; `createProfile` always returns the ID.
   - `onAiClick` calls `router.push('/ai-search')`
   - Include `<CreateProfileDialog open={createOpen} onOpenChange={setCreateOpen} onCreate={handleCreate} />` in the JSX

2. In `web/src/components/top-navbar.tsx`:
   - Import `Home` icon from lucide-react (add to existing import)
   - Add Home as the FIRST item in the navItems array:
     `{ titleKey: "nav_home" as const, url: "/dashboard", icon: Home }`
   - The active-state check MUST use exact match for the Home item to avoid lighting it up on all `/dashboard/*` sub-routes. In the active-state logic, change the check from `pathname.startsWith(item.url)` to `item.url === "/dashboard" ? pathname === item.url : pathname.startsWith(item.url)`. This ensures Home is only active when exactly on `/dashboard`.
  </action>
  <verify>cd /Users/prakhar/Desktop/GenAI_hackathon/gen-ai-hackathon/web && npx tsc --noEmit 2>&1 | head -30</verify>
  <done>Dashboard page shows welcome text + two profile-creation cards instead of redirecting, TopNavbar has Home as first item linking to /dashboard, TypeScript compiles clean</done>
</task>

</tasks>

<verification>
1. `cd web && npx tsc --noEmit` — TypeScript compiles with no errors
2. `cd web && npm run build` — Next.js build succeeds
3. Translation key parity: both `en` and `de` objects have identical keys
</verification>

<success_criteria>
1. Visiting /dashboard shows "Welcome to HomeMatch" heading and "Let's create your profile" subheading
2. Two cards visible: Manual (with ClipboardList icon) and AI-guided (with Sparkles icon)
3. Manual card click opens name dialog, then navigates to profile edit page
4. AI-guided card click navigates to /ai-search
5. TopNavbar shows Home icon+label as first item, links to /dashboard
6. TypeScript build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/33-dashboard-home-nav-polish-profile-creation-flow-and-analyses-titles-fix/33-01-SUMMARY.md`
</output>
