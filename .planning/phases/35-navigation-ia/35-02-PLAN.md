---
phase: 35-navigation-ia
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/(dashboard)/settings/page.tsx
autonomous: true
requirements: [NAV-04]

must_haves:
  truths:
    - "User can navigate to the Download Extension page directly from the Settings page"
    - "Settings page has a visible 'Download Extension' section with a button/link to /download"
  artifacts:
    - path: "web/src/app/(dashboard)/settings/page.tsx"
      provides: "Download Extension section with a Link/Button below the language toggle"
      contains: "href=\"/download\""
  key_links:
    - from: "web/src/app/(dashboard)/settings/page.tsx"
      to: "/download"
      via: "Link or Button component with href /download"
      pattern: "href.*download"
---

<objective>
Add a "Download Extension" section to the Settings page so users can reach the /download page without it being in the primary nav.

Purpose: After removing Download from the primary nav (NAV-02), users still need a discoverable path to the extension download page. Settings is the canonical home for this.
Output: Updated settings/page.tsx with a Download Extension card/section.
</objective>

<execution_context>
@/Users/singhs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/singhs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/singhs/gen-ai-hackathon/.planning/PROJECT.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From web/src/app/(dashboard)/settings/page.tsx (current full file):
```tsx
"use client"

import { useLanguage } from "@/lib/language-context"
import { t, type Language } from "@/lib/translations"
import { Label } from "@/components/ui/label"
import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
  SelectValue,
} from "@/components/ui/select"
import { updateProfilesLanguage } from "@/app/(dashboard)/profiles/actions"

export default function SettingsPage() {
  const { language, setLanguage } = useLanguage()

  async function handleLanguageChange(newLang: Language) {
    setLanguage(newLang)
    await updateProfilesLanguage(newLang)
  }

  return (
    <div className="container max-w-4xl mx-auto py-8 px-4">
      <h1 className="text-2xl font-bold mb-2">{t(language, "settings_title")}</h1>
      <p className="text-muted-foreground mb-8">{t(language, "settings_subtitle")}</p>

      <div className="max-w-sm space-y-4">
        <div className="space-y-2">
          <Label>{t(language, "settings_language")}</Label>
          <Select value={language} onValueChange={(v) => handleLanguageChange(v as Language)}>
            <SelectTrigger className="w-48 cursor-pointer">
              <SelectValue />
            </SelectTrigger>
            <SelectContent>
              <SelectItem value="en">{t(language, "settings_language_en")}</SelectItem>
              <SelectItem value="de">{t(language, "settings_language_de")}</SelectItem>
            </SelectContent>
          </Select>
        </div>
      </div>
    </div>
  )
}
```

Shadcn Button is available: `import { Button } from "@/components/ui/button"`
Next.js Link: `import Link from "next/link"`
Lucide Download icon: `import { Download } from "lucide-react"`

Translation keys already in translations.ts for download content:
- `download_title: 'Download Extension'` / `'Erweiterung herunterladen'`
- `download_btn: 'Download HomeMatch Extension'` / `'HomeMatch-Erweiterung herunterladen'`

No new translation keys are needed — reuse existing download_title and download_btn.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Download Extension section to Settings page</name>
  <files>
    web/src/app/(dashboard)/settings/page.tsx
  </files>
  <action>
Rewrite web/src/app/(dashboard)/settings/page.tsx to add a "Download Extension" section below the language toggle.

Add these imports at the top:
- `import Link from "next/link"`
- `import { Button } from "@/components/ui/button"`
- `import { Download } from "lucide-react"`

After the closing `</div>` of the language section (`max-w-sm space-y-4` div), add a new section below it:

```tsx
{/* Download Extension section */}
<div className="mt-10">
  <h2 className="text-lg font-semibold mb-1">{t(language, "download_title")}</h2>
  <p className="text-sm text-muted-foreground mb-4">
    {t(language, "download_subtitle")}
  </p>
  <Button asChild>
    <Link href="/download">
      <Download className="size-4 mr-2" />
      {t(language, "download_btn")}
    </Link>
  </Button>
</div>
```

The section sits inside the outer `container` div, after the `max-w-sm` language section. Keep all existing imports and code intact — this is an additive change only.
  </action>
  <verify>
    <automated>cd /Users/singhs/gen-ai-hackathon/web && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - Settings page renders a "Download Extension" heading, subtitle, and a button linking to /download
    - Button displays the Download icon and the localized download_btn label
    - Language section still works correctly above it
    - TypeScript compiles with no errors
  </done>
</task>

</tasks>

<verification>
1. `cd /Users/singhs/gen-ai-hackathon/web && npx tsc --noEmit` — must pass
2. `cd /Users/singhs/gen-ai-hackathon/web && npm run build` — must pass
3. Visit /settings — verify "Download Extension" section appears with a button
4. Click the button — navigates to /download
</verification>

<success_criteria>
- Settings page has a "Download Extension" section below the language toggle
- Section contains a button linking to /download using next/link and Shadcn Button
- Uses existing translation keys: download_title, download_subtitle, download_btn
- TypeScript and build pass cleanly
</success_criteria>

<output>
After completion, create `/Users/singhs/gen-ai-hackathon/.planning/phases/35-navigation-ia/35-02-SUMMARY.md`
</output>
