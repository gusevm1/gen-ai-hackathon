---
phase: 12-chat-based-preference-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/package.json
  - web/src/lib/chat/system-prompt.ts
  - web/src/lib/chat/extraction-schema.ts
  - web/src/app/api/chat/route.ts
  - web/src/app/(dashboard)/profiles/[profileId]/actions.ts
  - web/src/lib/chat/merge-fields.ts
  - web/src/__tests__/extraction-schema.test.ts
  - web/src/__tests__/chat-merge.test.ts
autonomous: true
requirements: [CHAT-02, CHAT-03, CHAT-05]
user_setup:
  - service: anthropic
    why: "Claude API for chat and extraction (server-side only)"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console -> API Keys -> Create Key (https://console.anthropic.com/settings/keys)"
    dashboard_config:
      - task: "Add ANTHROPIC_API_KEY to Vercel"
        location: "Vercel Dashboard -> Project Settings -> Environment Variables -> Add (Production + Preview)"

must_haves:
  truths:
    - "Chat API route streams Claude responses to authenticated users"
    - "Extraction action returns structured DynamicField[] from conversation text"
    - "Merge utility appends extracted fields to existing dynamicFields without overwriting standard form fields"
    - "Unauthenticated requests to /api/chat return 401"
  artifacts:
    - path: "web/src/lib/chat/system-prompt.ts"
      provides: "System prompt for Swiss property preference discovery chat"
      exports: ["CHAT_SYSTEM_PROMPT"]
    - path: "web/src/lib/chat/extraction-schema.ts"
      provides: "Zod schema for structured extraction output"
      exports: ["extractedFieldSchema", "extractionResultSchema", "ExtractionResult"]
    - path: "web/src/app/api/chat/route.ts"
      provides: "POST handler streaming Claude responses via AI SDK"
      exports: ["POST", "maxDuration"]
    - path: "web/src/app/(dashboard)/profiles/[profileId]/actions.ts"
      provides: "extractPreferencesFromChat server action"
      exports: ["extractPreferencesFromChat"]
    - path: "web/src/lib/chat/merge-fields.ts"
      provides: "Merge utility for combining extracted fields with existing dynamicFields"
      exports: ["mergeExtractedFields"]
  key_links:
    - from: "web/src/app/api/chat/route.ts"
      to: "web/src/lib/chat/system-prompt.ts"
      via: "import CHAT_SYSTEM_PROMPT"
      pattern: "import.*CHAT_SYSTEM_PROMPT.*from.*system-prompt"
    - from: "web/src/app/(dashboard)/profiles/[profileId]/actions.ts"
      to: "web/src/lib/chat/extraction-schema.ts"
      via: "import extractionResultSchema"
      pattern: "import.*extractionResultSchema.*from.*extraction-schema"
---

<objective>
Install AI SDK packages and build the entire server-side layer for chat-based preference discovery: system prompt, extraction schema, streaming chat API route, extraction server action, and field merge utility.

Purpose: Provide the complete server-side foundation that chat UI components (Plan 02) and extraction review (Plan 03) build on top of.
Output: AI SDK installed, lib/chat/ directory with system prompt + extraction schema + merge utility, /api/chat route handler, extractPreferencesFromChat server action, and unit tests for schema + merge.
</objective>

<execution_context>
@/Users/maximgusev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maximgusev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-chat-based-preference-discovery/12-RESEARCH.md

@web/package.json
@web/src/lib/schemas/preferences.ts
@web/src/lib/supabase/server.ts
@web/src/app/(dashboard)/profiles/actions.ts
@web/src/app/(dashboard)/profiles/[profileId]/page.tsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From web/src/lib/schemas/preferences.ts:
```typescript
export const dynamicFieldSchema = z.object({
  name: z.string().min(1),
  value: z.string().default(''),
  importance: importanceLevelSchema, // z.enum(['critical', 'high', 'medium', 'low']).default('medium')
})
export type DynamicField = z.infer<typeof dynamicFieldSchema>
export type Preferences = z.infer<typeof preferencesSchema>
```

From web/src/lib/supabase/server.ts:
```typescript
export async function createClient(): Promise<SupabaseClient>
```

From web/src/app/(dashboard)/profiles/actions.ts:
```typescript
export async function saveProfilePreferences(profileId: string, data: Preferences): Promise<void>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install AI SDK + create lib/chat layer + extraction schema tests</name>
  <files>web/package.json, web/src/lib/chat/system-prompt.ts, web/src/lib/chat/extraction-schema.ts, web/src/__tests__/extraction-schema.test.ts</files>
  <behavior>
    - Test: extractedFieldSchema parses valid {name, value, importance} objects
    - Test: extractedFieldSchema rejects empty name strings (min 1 char)
    - Test: extractedFieldSchema defaults importance to 'medium' when omitted
    - Test: extractionResultSchema parses {fields: [...]} with multiple entries
    - Test: extractionResultSchema handles empty fields array
    - Test: importance rejects invalid enum values
  </behavior>
  <action>
    1. Install AI SDK packages: `cd web && pnpm add ai @ai-sdk/react @ai-sdk/anthropic`

    2. Create `web/src/lib/chat/system-prompt.ts`:
       - Export `CHAT_SYSTEM_PROMPT` string constant
       - System prompt for a friendly Swiss property search assistant
       - Focus on lifestyle preferences, must-haves, deal-breakers, nice-to-haves
       - Explicitly exclude budget/rooms/location (handled by standard form)
       - Respond in the user's language (de/en/fr/it)
       - Keep responses concise (2-4 sentences + question)
       - After 3-4 exchanges, summarize and ask if anything is missing
       - Topics: neighborhood, commute, light, outdoor space, storage, noise, building condition, pets, views, amenities
       - Use the exact system prompt from 12-RESEARCH.md Pattern 1

    3. Create `web/src/lib/chat/extraction-schema.ts`:
       - Import z from 'zod'
       - Export `extractedFieldSchema`: z.object with name (string, described), value (string, described), importance (enum critical/high/medium/low, described)
       - Export `extractionResultSchema`: z.object with fields (array of extractedFieldSchema, described)
       - Export type `ExtractionResult = z.infer<typeof extractionResultSchema>`
       - Use .describe() on fields for LLM structured output guidance
       - Match the schema from 12-RESEARCH.md "Extraction Schema" code example

    4. Write tests in `web/src/__tests__/extraction-schema.test.ts` FIRST (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>cd /Users/maximgusev/workspace/gen-ai-hackathon/web && npx vitest run src/__tests__/extraction-schema.test.ts</automated>
  </verify>
  <done>AI SDK packages installed in web/package.json. system-prompt.ts exports CHAT_SYSTEM_PROMPT. extraction-schema.ts exports extractedFieldSchema, extractionResultSchema, ExtractionResult. All 6+ extraction schema tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Chat API route + extraction action + merge utility + merge tests</name>
  <files>web/src/app/api/chat/route.ts, web/src/app/(dashboard)/profiles/[profileId]/actions.ts, web/src/lib/chat/merge-fields.ts, web/src/__tests__/chat-merge.test.ts</files>
  <behavior>
    - Test: mergeExtractedFields appends new fields to empty existing array
    - Test: mergeExtractedFields appends new fields to non-empty existing array
    - Test: mergeExtractedFields does not modify standard fields (location, budget, etc.)
    - Test: mergeExtractedFields handles duplicate field names (allows duplicates -- simple append)
    - Test: mergeExtractedFields with empty extracted array returns existing unchanged
  </behavior>
  <action>
    1. Create `web/src/lib/chat/merge-fields.ts`:
       - Import DynamicField type from '@/lib/schemas/preferences'
       - Export `mergeExtractedFields(existing: DynamicField[], extracted: DynamicField[]): DynamicField[]`
       - Simple append: return [...existing, ...extracted]
       - Per research recommendation, re-extraction replaces (handled at call site), merge itself is pure append
       - This is intentionally simple -- the complexity lives in the call site deciding whether to replace or append

    2. Write merge tests in `web/src/__tests__/chat-merge.test.ts` FIRST (RED), then implement (GREEN).

    3. Create `web/src/app/api/chat/route.ts`:
       - Import { streamText, UIMessage, convertToModelMessages } from 'ai'
       - Import { anthropic } from '@ai-sdk/anthropic'
       - Import { createClient } from '@/lib/supabase/server'
       - Import { CHAT_SYSTEM_PROMPT } from '@/lib/chat/system-prompt'
       - Export `const maxDuration = 60` (Vercel Fluid Compute timeout)
       - Export async function POST(req: Request):
         - Create Supabase client, call getUser()
         - If no user, return Response with 401 + JSON { error: 'Unauthorized' }
         - Parse request body: { messages }: { messages: UIMessage[] }
         - Call streamText with anthropic('claude-haiku-4-5-20251001'), system: CHAT_SYSTEM_PROMPT, messages: await convertToModelMessages(messages)
         - Return result.toUIMessageStreamResponse()

    4. Add `extractPreferencesFromChat` server action to the EXISTING `web/src/app/(dashboard)/profiles/[profileId]/actions.ts` file (do NOT create a new actions file -- add to the existing one that already has saveProfilePreferences):
       - Add 'use server' (already present)
       - Import { generateText, Output } from 'ai'
       - Import { anthropic } from '@ai-sdk/anthropic'
       - Import { extractionResultSchema } from '@/lib/chat/extraction-schema'
       - Import { createClient } from '@/lib/supabase/server'
       - Export async function extractPreferencesFromChat(conversationText: string)
       - Auth check: create Supabase client, getUser(), throw if no user
       - Call generateText with anthropic('claude-haiku-4-5-20251001'), output: Output.object({ schema: extractionResultSchema }), prompt with extraction instructions (from 12-RESEARCH.md Pattern 3)
       - Return output?.fields ?? []
       - IMPORTANT: This file already exports createProfile, renameProfile, deleteProfile, duplicateProfile, setActiveProfile, saveProfilePreferences. Preserve ALL existing exports. Only ADD the new extractPreferencesFromChat function.

    5. Add ANTHROPIC_API_KEY to web/.env.local (check if it exists, add placeholder if not):
       - `ANTHROPIC_API_KEY=` (empty -- user must fill in)
       - Do NOT prefix with NEXT_PUBLIC_ (must stay server-side only)
  </action>
  <verify>
    <automated>cd /Users/maximgusev/workspace/gen-ai-hackathon/web && npx vitest run src/__tests__/chat-merge.test.ts</automated>
  </verify>
  <done>Chat API route at /api/chat streams Claude responses with auth guard. extractPreferencesFromChat action added to existing actions.ts. mergeExtractedFields utility works correctly. All 5+ merge tests pass. ANTHROPIC_API_KEY placeholder in .env.local.</done>
</task>

</tasks>

<verification>
- `cd web && npx vitest run src/__tests__/extraction-schema.test.ts src/__tests__/chat-merge.test.ts` -- all tests pass
- `cd web && npx vitest run` -- full suite still green (no regressions)
- `web/src/app/api/chat/route.ts` exists with POST export and auth guard
- `web/src/lib/chat/system-prompt.ts` exports CHAT_SYSTEM_PROMPT
- `web/src/lib/chat/extraction-schema.ts` exports extractedFieldSchema, extractionResultSchema
- `web/src/lib/chat/merge-fields.ts` exports mergeExtractedFields
- `web/src/app/(dashboard)/profiles/[profileId]/actions.ts` exports extractPreferencesFromChat alongside all existing exports
- `grep -q "ANTHROPIC_API_KEY" web/.env.local` succeeds
</verification>

<success_criteria>
AI SDK packages installed. All server-side chat infrastructure in place. Extraction schema validates correctly. Merge utility tested. Chat API route and extraction action ready for UI consumption.
</success_criteria>

<output>
After completion, create `.planning/phases/12-chat-based-preference-discovery/12-01-SUMMARY.md`
</output>
