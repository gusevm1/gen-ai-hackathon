---
phase: 14-chat-ui-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/top-navbar.tsx
  - web/src/__tests__/top-navbar.test.tsx
  - web/src/__tests__/chat-page.test.tsx
  - web/src/__tests__/ai-avatar.test.tsx
autonomous: true
requirements: [NAV-01, NAV-02]

must_haves:
  truths:
    - "AI-Powered Search appears as the first item in the top navbar"
    - "AI-Powered Search nav item always shows pinkish-red text (text-primary) regardless of active state"
    - "Active state for AI-Powered Search additionally shows bg-primary/10 background"
    - "Nav order is: AI-Powered Search | Profiles | Analyses | Settings"
  artifacts:
    - path: "web/src/components/top-navbar.tsx"
      provides: "Updated navbar with AI-Powered Search as first item"
      contains: "AI-Powered Search"
    - path: "web/src/__tests__/top-navbar.test.tsx"
      provides: "Tests for nav item presence and ordering"
    - path: "web/src/__tests__/chat-page.test.tsx"
      provides: "Test scaffolds for chat page (Wave 0 for plans 02-03)"
    - path: "web/src/__tests__/ai-avatar.test.tsx"
      provides: "Test scaffolds for AI avatar (Wave 0 for plan 03)"
  key_links:
    - from: "web/src/components/top-navbar.tsx"
      to: "/ai-search"
      via: "Link href"
      pattern: 'url: "/ai-search"'
---

<objective>
Add "AI-Powered Search" as the first nav item in the top navbar with always-on pinkish-red accent styling, and scaffold all test files needed by subsequent plans.

Purpose: Establishes the entry point for the AI chat feature and creates Wave 0 test infrastructure so plans 02-03 have failing tests to implement against.
Output: Updated navbar component, three new/updated test files.
</objective>

<execution_context>
@/Users/prakhar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prakhar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-chat-ui-navigation/14-CONTEXT.md
@.planning/phases/14-chat-ui-navigation/14-RESEARCH.md
@.planning/phases/14-chat-ui-navigation/14-VALIDATION.md

<interfaces>
<!-- Current top-navbar.tsx structure (will be modified) -->
From web/src/components/top-navbar.tsx:
```typescript
"use client"
import Link from "next/link"
import { usePathname } from "next/navigation"
import { User, BarChart3, Settings } from "lucide-react"
import { cn } from "@/lib/utils"

const navItems = [
  { title: "Profiles", url: "/profiles", icon: User },
  { title: "Analyses", url: "/analyses", icon: BarChart3 },
  { title: "Settings", url: "/settings", icon: Settings },
]
```

From web/src/__tests__/navbar.test.tsx (existing test pattern):
```typescript
import { describe, it, expect, vi } from "vitest"
import { render, screen } from "@testing-library/react"
// Mock next/navigation
vi.mock("next/navigation", () => ({
  useRouter: () => ({ push: vi.fn(), replace: vi.fn(), prefetch: vi.fn() }),
}))
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Wave 0 test files for chat page and AI avatar</name>
  <files>web/src/__tests__/top-navbar.test.tsx, web/src/__tests__/chat-page.test.tsx, web/src/__tests__/ai-avatar.test.tsx</files>
  <read_first>
    - web/src/__tests__/navbar.test.tsx (existing test pattern for mocking next/navigation)
    - web/vitest.config.mts (test config)
    - web/src/components/top-navbar.tsx (current navbar to understand what tests should assert)
  </read_first>
  <action>
Create three test files:

**1. `web/src/__tests__/top-navbar.test.tsx`** -- Tests for NAV-01 and NAV-02:
- Mock `next/navigation` with `usePathname` returning `/ai-search` (same pattern as navbar.test.tsx but mock `usePathname`)
- Test "renders AI-Powered Search as first nav item": render `TopNavbar`, get all links, assert first link text contains "AI-Powered Search"
- Test "AI-Powered Search uses text-primary class": render `TopNavbar`, find the AI-Powered Search link, assert it has class `text-primary`
- Test "nav items render in correct order": render `TopNavbar`, get all link texts, assert order is ["AI-Powered Search", "Profiles", "Analyses", "Settings"]
- Test "AI-Powered Search uses Sparkles icon": render `TopNavbar`, assert the AI-Powered Search link contains an SVG element
- Import `TopNavbar` from `@/components/top-navbar`

**2. `web/src/__tests__/chat-page.test.tsx`** -- Scaffolded failing tests for plans 02-03:
- Mock `next/navigation` with `usePathname` returning `/ai-search`
- Import `ChatPage` from `@/components/chat/chat-page` (does not exist yet -- tests will fail)
- Test "renders centered layout in idle state": render `ChatPage`, expect element with text matching placeholder substring "Describe your ideal property"
- Test "shows Start Creating Profile button in idle state": render `ChatPage`, expect button with text "Start Creating Profile"
- Test "shows profile name prompt after clicking Start Creating Profile": render `ChatPage`, click "Start Creating Profile" button, expect text "What should we call this profile?"
- Test "sends first message after naming profile": render `ChatPage`, click "Start Creating Profile", type "My Dream Flat" into name input, click "Start Conversation", expect user message to appear in thread
- Test "shows AI avatar with house icon on assistant messages": render `ChatPage` with a mock conversation state, expect element with `bg-primary` class containing an SVG
- Test "user can send follow-up messages": after starting conversation, type in textarea, press Enter or click send, expect new user message in thread
- Use `@testing-library/react` `render`, `screen`, `fireEvent`, `waitFor`

**3. `web/src/__tests__/ai-avatar.test.tsx`** -- Tests for CHAT-09:
- Import `AIAvatar` from `@/components/chat/ai-avatar` (does not exist yet -- tests will fail)
- Test "renders circular container with bg-primary": render `AIAvatar`, assert container div has classes `rounded-full` and `bg-primary`
- Test "renders house SVG icon": render `AIAvatar`, assert an SVG element exists within the component
- Test "container is 32px (size-8)": render `AIAvatar`, assert container has class `size-8`

All test files must use `describe`/`it`/`expect` from `vitest` and `render`/`screen` from `@testing-library/react`.
  </action>
  <verify>
    <automated>cd /Users/prakhar/Desktop/GenAI_hackathon/gen-ai-hackathon/web && npx vitest run src/__tests__/top-navbar.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `web/src/__tests__/top-navbar.test.tsx` exists and contains `describe("TopNavbar"`
    - File `web/src/__tests__/top-navbar.test.tsx` contains `"AI-Powered Search"`
    - File `web/src/__tests__/chat-page.test.tsx` exists and contains `import { ChatPage }` and `describe("ChatPage"`
    - File `web/src/__tests__/chat-page.test.tsx` contains `"Start Creating Profile"`
    - File `web/src/__tests__/ai-avatar.test.tsx` exists and contains `import { AIAvatar }` and `"bg-primary"`
    - top-navbar tests FAIL because nav item not yet added (expected at this point)
    - chat-page and ai-avatar tests FAIL because components don't exist yet (expected -- Wave 0)
  </acceptance_criteria>
  <done>Three test files exist with failing tests that cover NAV-01, NAV-02, CHAT-01-09 requirements. top-navbar tests will pass after Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Add AI-Powered Search nav item to top-navbar.tsx</name>
  <files>web/src/components/top-navbar.tsx</files>
  <read_first>
    - web/src/components/top-navbar.tsx (current state before modification)
    - web/src/__tests__/top-navbar.test.tsx (tests to satisfy from Task 1)
  </read_first>
  <action>
Modify `web/src/components/top-navbar.tsx`:

1. Add `Sparkles` to the lucide-react import: `import { User, BarChart3, Settings, Sparkles } from "lucide-react"`

2. Add `accent?: boolean` to the nav item type by updating the `navItems` array to include a typed entry. Prepend the AI-Powered Search item:
```typescript
const navItems = [
  { title: "AI-Powered Search", url: "/ai-search", icon: Sparkles, accent: true },
  { title: "Profiles", url: "/profiles", icon: User },
  { title: "Analyses", url: "/analyses", icon: BarChart3 },
  { title: "Settings", url: "/settings", icon: Settings },
]
```

3. Update the className logic in the Link element to handle the `accent` property:
```typescript
className={cn(
  "flex items-center gap-1.5 rounded-md px-3 py-1.5 text-sm font-medium transition-colors",
  item.accent
    ? cn("text-primary", isActive && "bg-primary/10")
    : isActive
      ? "bg-accent text-accent-foreground"
      : "text-muted-foreground hover:bg-accent/50 hover:text-accent-foreground"
)}
```

This ensures AI-Powered Search always has `text-primary` (pinkish-red) text, and when active gets a subtle `bg-primary/10` background. Other nav items keep their existing behavior.
  </action>
  <verify>
    <automated>cd /Users/prakhar/Desktop/GenAI_hackathon/gen-ai-hackathon/web && npx vitest run src/__tests__/top-navbar.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/components/top-navbar.tsx` contains `import { User, BarChart3, Settings, Sparkles } from "lucide-react"`
    - `web/src/components/top-navbar.tsx` contains `{ title: "AI-Powered Search", url: "/ai-search", icon: Sparkles, accent: true }`
    - `web/src/components/top-navbar.tsx` contains `item.accent` in the className logic
    - `web/src/components/top-navbar.tsx` contains `"text-primary"` and `"bg-primary/10"` for accent items
    - AI-Powered Search is the FIRST entry in the navItems array (index 0)
    - All top-navbar tests pass
  </acceptance_criteria>
  <done>AI-Powered Search appears as the first nav item with always-on pinkish-red accent text. Nav order is: AI-Powered Search | Profiles | Analyses | Settings. All NAV-01 and NAV-02 tests pass.</done>
</task>

</tasks>

<verification>
- `cd /Users/prakhar/Desktop/GenAI_hackathon/gen-ai-hackathon/web && npx vitest run src/__tests__/top-navbar.test.tsx --reporter=verbose` -- all tests pass
- `grep -n "AI-Powered Search" web/src/components/top-navbar.tsx` -- found as first navItem
- `grep -n "Sparkles" web/src/components/top-navbar.tsx` -- imported and used
- `grep -n "text-primary" web/src/components/top-navbar.tsx` -- accent styling present
</verification>

<success_criteria>
- AI-Powered Search is the first nav item in the navbar
- It always renders with pinkish-red text (text-primary class)
- When active, it additionally shows bg-primary/10 background
- Nav order is: AI-Powered Search, Profiles, Analyses, Settings
- All top-navbar tests pass
- Wave 0 test scaffolds exist for chat-page and ai-avatar (failing is expected)
</success_criteria>

<output>
After completion, create `.planning/phases/14-chat-ui-navigation/14-01-SUMMARY.md`
</output>
