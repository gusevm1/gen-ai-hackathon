---
phase: 01.1-build-custom-homegate-scraper-actor-on-apify
plan: 02
type: execute
wave: 2
depends_on: [01.1-01]
files_modified:
  - actors/homegate-scraper/.actor/actor.json
  - src/scrapers/homegate/adapter.ts
autonomous: false
requirements: [SITE-01]
user_setup:
  - service: apify
    why: "Deploy custom actor to Apify platform"
    env_vars:
      - name: APIFY_TOKEN
        source: "Apify Console -> Settings -> Integrations -> API tokens (should already exist from Phase 1)"
    dashboard_config:
      - task: "Authenticate Apify CLI"
        location: "Run `npx apify login` in terminal with your API token"

must_haves:
  truths:
    - "Custom actor is deployed to Apify and callable via ApifyClient"
    - "HomegateAdapter calls the custom actor instead of the paid third-party actor"
    - "Running the actor on Apify produces listing data in the Dataset"
  artifacts:
    - path: "src/scrapers/homegate/adapter.ts"
      provides: "Updated adapter pointing to custom actor ID"
      contains: "homegate-scraper"
  key_links:
    - from: "src/scrapers/homegate/adapter.ts"
      to: "Apify platform custom actor"
      via: "client.actor(customActorId).call(input)"
      pattern: "actor\\("
---

<objective>
Deploy the custom Homegate scraper actor to Apify and update the main project's HomegateAdapter to use it.

Purpose: Complete the replacement of the paid third-party actor by deploying our custom actor and wiring it into the existing scraping pipeline. After this plan, the full pipeline (adapter -> actor -> normalize -> validate -> write) can be tested end-to-end.

Output: A deployed Apify actor accessible via ApifyClient, and an updated HomegateAdapter that calls the custom actor.
</objective>

<execution_context>
@/Users/maximgusev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maximgusev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-build-custom-homegate-scraper-actor-on-apify/01.1-CONTEXT.md
@.planning/phases/01.1-build-custom-homegate-scraper-actor-on-apify/01.1-01-SUMMARY.md

<interfaces>
<!-- HomegateAdapter interface from main project -->

From src/scrapers/homegate/adapter.ts:
```typescript
export class HomegateAdapter implements ScraperAdapter {
  readonly name = 'homegate';
  constructor(private readonly client: ApifyClient, private readonly logger: Logger) {}
  async scrape(options: ScrapeOptions): Promise<RawRecord[]> {
    // Currently calls: client.actor('ecomscrape/homegate-property-search-scraper').call(input)
    // Needs to call: client.actor('{username}/homegate-scraper').call(input)
  }
}
```

From src/scrapers/types.ts:
```typescript
export interface ScraperAdapter {
  readonly name: string;
  scrape(options: ScrapeOptions): Promise<RawRecord[]>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy actor to Apify and update adapter</name>
  <files>
    actors/homegate-scraper/.actor/actor.json,
    src/scrapers/homegate/adapter.ts
  </files>
  <action>
    **Step 1: Deploy actor to Apify**
    - `cd actors/homegate-scraper`
    - Run `npx apify login --token $APIFY_TOKEN` (use token from .env or environment)
    - Run `npx apify push` to deploy the actor to Apify platform
    - Note the full actor ID from the output (format: `{username}/homegate-scraper`)
    - If apify CLI is not authenticated, this will fail -- create a checkpoint for the user to authenticate

    **Step 2: Update HomegateAdapter**
    - In `src/scrapers/homegate/adapter.ts`:
      - Replace `'ecomscrape/homegate-property-search-scraper'` with the custom actor ID
      - Make the actor ID configurable: read from `process.env.HOMEGATE_ACTOR_ID` with the custom actor as default
      - Update the input format to match the custom actor's input_schema.json:
        - Old: `{ urls: [{url: "..."}], maxItems?: number }`
        - New: `{ listingType: 'both', maxItems?: number }`
      - Update log messages to reference the custom actor
    - Keep the same ApifyClient pattern (actor.call -> dataset.listItems)

    **Step 3: Verify adapter compiles**
    - Run `npx tsc --noEmit` from the main project root to ensure adapter changes compile
  </action>
  <verify>
    <automated>cd /Users/maximgusev/workspace/gen-ai-hackathon && npx tsc --noEmit</automated>
  </verify>
  <done>
    - Actor deployed to Apify platform (visible in Apify Console)
    - HomegateAdapter updated to call custom actor with new input format
    - Actor ID is configurable via HOMEGATE_ACTOR_ID env var
    - Main project compiles without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify actor runs on Apify and produces data</name>
  <files>actors/homegate-scraper/.actor/actor.json</files>
  <action>
    Human verification checkpoint. The custom Homegate scraper actor has been deployed to Apify and the HomegateAdapter has been updated to use it. The user must verify the actor runs correctly on the Apify platform.
  </action>
  <what-built>
    Custom Homegate scraper actor deployed to Apify, HomegateAdapter updated to use it.
  </what-built>
  <how-to-verify>
    1. Open Apify Console (https://console.apify.com) and navigate to your Actors
    2. Find "homegate-scraper" in the list -- confirm it exists and shows "latest" build
    3. Click "Start" with input: `{"listingType": "rent", "maxItems": 5}` (minimal test run)
    4. Wait for run to complete (should take < 2 minutes with only 5 items)
    5. Check the Dataset tab -- confirm it contains listing objects with fields: url, title, price, rooms, address, listingType
    6. If the run fails with anti-bot errors (403, empty __INITIAL_STATE__): report back and we'll add proxy configuration or switch to PlaywrightCrawler
    7. If successful, note the actor ID shown in the URL bar (e.g., `username/homegate-scraper`)
  </how-to-verify>
  <verify>
    <automated>echo "Checkpoint: human verification required -- run actor on Apify Console with maxItems=5"</automated>
  </verify>
  <done>
    - Actor test run completed successfully on Apify platform
    - Dataset contains listing objects with required fields (url, title, price, rooms, address, listingType)
    - No anti-bot blocking or empty data issues
  </done>
  <resume-signal>Type "approved" with the actor ID, or describe any issues (errors, empty data, blocked requests)</resume-signal>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes in main project root (adapter compiles)
2. Actor is visible in Apify Console under user's account
3. Test run with `{"listingType": "rent", "maxItems": 5}` produces listing data in Dataset
4. Dataset items contain required fields: url, title, price, rooms, address, listingType
</verification>

<success_criteria>
- Custom actor deployed to Apify and accessible via ApifyClient
- HomegateAdapter calls custom actor instead of paid third-party actor
- Test run produces real Homegate listing data
- Actor ID is configurable via environment variable for flexibility
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-build-custom-homegate-scraper-actor-on-apify/01.1-02-SUMMARY.md`
</output>
