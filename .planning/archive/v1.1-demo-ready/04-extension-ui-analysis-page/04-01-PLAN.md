---
phase: 04-extension-ui-analysis-page
plan: 01
type: execute
wave: 1
depends_on: ["04-00"]
files_modified:
  - extension/src/types/scoring.ts
  - extension/src/lib/supabase.ts
  - extension/src/lib/api.ts
  - extension/src/lib/flatfox.ts
  - extension/src/entrypoints/background.ts
  - extension/wxt.config.ts
  - extension/package.json
  - extension/src/entrypoints/popup/App.tsx
  - extension/src/components/popup/Dashboard.tsx
  - extension/src/components/popup/LoginForm.tsx
  - extension/src/__tests__/scoring-types.test.ts
  - extension/src/__tests__/auth-flow.test.ts
autonomous: true
requirements: [EXT-07]
must_haves:
  truths:
    - "Extension popup shows login form when user is not authenticated"
    - "Extension popup shows profile summary with link to preferences website when authenticated"
    - "Edit Preferences button opens the Next.js website (not onboarding wizard)"
    - "Background script manages Supabase auth session via chrome.storage.local adapter"
    - "Extension manifest targets flatfox.ch (not homegate.ch)"
  artifacts:
    - path: "extension/src/types/scoring.ts"
      provides: "TypeScript interfaces mirroring backend ScoreResponse, CategoryScore, ChecklistItem"
      exports: ["ScoreResponse", "CategoryScore", "ChecklistItem", "TIER_COLORS"]
    - path: "extension/src/lib/supabase.ts"
      provides: "Supabase client with chrome.storage.local adapter for MV3 service worker"
      exports: ["supabase"]
    - path: "extension/src/lib/api.ts"
      provides: "Edge function API client with JWT auth"
      exports: ["scoreListings", "scoreListing"]
    - path: "extension/src/lib/flatfox.ts"
      provides: "DOM parser for extracting listing PKs from Flatfox search pages"
      exports: ["extractVisibleListingPKs"]
    - path: "extension/src/entrypoints/background.ts"
      provides: "Background script with Supabase auth message handling"
    - path: "extension/src/components/popup/LoginForm.tsx"
      provides: "Email/password login form component"
    - path: "extension/src/components/popup/Dashboard.tsx"
      provides: "Rewritten popup dashboard with Supabase auth state"
  key_links:
    - from: "extension/src/entrypoints/background.ts"
      to: "extension/src/lib/supabase.ts"
      via: "import supabase client"
      pattern: "import.*supabase"
    - from: "extension/src/components/popup/Dashboard.tsx"
      to: "extension/src/entrypoints/background.ts"
      via: "browser.runtime.sendMessage for auth state"
      pattern: "sendMessage.*getSession"
    - from: "extension/src/lib/api.ts"
      to: "supabase edge function score-proxy"
      via: "fetch with JWT Bearer token"
      pattern: "score-proxy"
---

<objective>
Extension infrastructure, auth plumbing, and popup rewrite -- the foundation all other extension plans depend on.

Purpose: Establishes TypeScript types (mirroring backend ScoreResponse), Supabase auth in the extension (background script + chrome.storage adapter), API client for edge function calls, Flatfox DOM parser utility, and rewrites the popup from local-storage Homegate flow to Supabase auth with link to preferences website. This plan creates every shared module the content script (Plan 03) will import. Also fills Wave 0 test stubs for scoring-types and auth-flow.

Output: Working extension popup with Supabase login/logout, profile state display, and "Edit Preferences" linking to Next.js site. All shared libs ready for content script consumption. Tests for scoring types and auth flow passing.
</objective>

<execution_context>
@/Users/maximgusev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maximgusev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-extension-ui-analysis-page/04-CONTEXT.md
@.planning/phases/04-extension-ui-analysis-page/04-RESEARCH.md

@extension/wxt.config.ts
@extension/package.json
@extension/src/entrypoints/background.ts
@extension/src/entrypoints/popup/App.tsx
@extension/src/components/popup/Dashboard.tsx
@extension/src/lib/theme.ts
@extension/src/hooks/useProfile.ts

<interfaces>
<!-- Backend ScoreResponse contract (from backend/app/models/scoring.py) -->
```python
class CategoryScore(BaseModel):
    name: str       # location, price, size, features, condition
    score: int      # 0-100
    weight: int     # 0-100
    reasoning: list[str]  # 1-5 bullets

class ChecklistItem(BaseModel):
    criterion: str
    met: bool | None  # True/False/None(unknown)
    note: str

class ScoreResponse(BaseModel):
    overall_score: int          # 0-100
    match_tier: Literal["excellent", "good", "fair", "poor"]
    summary_bullets: list[str]  # 3-5 bullets
    categories: list[CategoryScore]
    checklist: list[ChecklistItem]
    language: str               # de/fr/it/en
```

<!-- Edge function URL -->
```
POST https://mlhtozdtiorkemamzjjc.supabase.co/functions/v1/score-proxy
Headers: Authorization: Bearer {JWT}, Content-Type: application/json
Body: { listing_id: number }
Response: ScoreResponse JSON
```

<!-- Supabase project -->
```
SUPABASE_URL: https://mlhtozdtiorkemamzjjc.supabase.co
SUPABASE_ANON_KEY: (from .env or hardcode for extension)
```

<!-- Website URL for preferences link -->
```
Next.js site: https://homematch-web.vercel.app (or localhost:3000 in dev)
Dashboard: /dashboard
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared types, lib modules, and install Supabase dependency</name>
  <files>
    extension/package.json,
    extension/src/types/scoring.ts,
    extension/src/lib/supabase.ts,
    extension/src/lib/api.ts,
    extension/src/lib/flatfox.ts,
    extension/wxt.config.ts,
    extension/src/__tests__/scoring-types.test.ts
  </files>
  <action>
1. Install @supabase/supabase-js in extension: `cd extension && pnpm add @supabase/supabase-js`

2. Create `extension/src/types/scoring.ts` -- TypeScript interfaces mirroring the backend Pydantic models exactly:
   - `CategoryScore { name: string; score: number; weight: number; reasoning: string[] }`
   - `ChecklistItem { criterion: string; met: boolean | null; note: string }`
   - `ScoreResponse { overall_score: number; match_tier: 'excellent'|'good'|'fair'|'poor'; summary_bullets: string[]; categories: CategoryScore[]; checklist: ChecklistItem[]; language: string }`
   - `TIER_COLORS` constant with modern palette (NOT traffic light):
     - excellent: emerald (#10b981 bg, white text)
     - good: blue (#3b82f6 bg, white text)
     - fair: amber (#f59e0b bg, dark text)
     - poor: gray (#6b7280 bg, white text)

3. Create `extension/src/lib/supabase.ts`:
   - Custom `chromeStorageAdapter` using `chrome.storage.local.get/set/remove` (required because MV3 background is a service worker without localStorage)
   - Export `supabase` client created with `createClient(SUPABASE_URL, SUPABASE_ANON_KEY, { auth: { storage: chromeStorageAdapter, autoRefreshToken: true, persistSession: true, detectSessionInUrl: false } })`
   - Hardcode the Supabase URL and anon key as constants (anon key is public -- safe for extension). Use `https://mlhtozdtiorkemamzjjc.supabase.co` and read the anon key from the web/.env.local file (NEXT_PUBLIC_SUPABASE_ANON_KEY).

4. Create `extension/src/lib/api.ts`:
   - `EDGE_FUNCTION_URL = 'https://mlhtozdtiorkemamzjjc.supabase.co/functions/v1/score-proxy'`
   - `async function scoreListing(listingId: number, jwt: string): Promise<ScoreResponse>` -- POST to edge function with Bearer JWT and `{ listing_id: listingId }` body. Parse response as ScoreResponse. Throw on non-200.
   - `async function scoreListings(listingIds: number[], jwt: string, onResult?: (id: number, result: ScoreResponse) => void): Promise<Map<number, ScoreResponse>>` -- score listings sequentially (NOT parallel -- avoid overwhelming backend). Call `onResult` callback after each score arrives for progressive rendering. Return Map<listingId, ScoreResponse>.

5. Create `extension/src/lib/flatfox.ts`:
   - `extractVisibleListingPKs(): number[]` -- query all `a[href*="/flat/"]` elements, extract PKs from href pattern `/flat/{slug}/{pk}/` using regex. Return deduplicated array of integer PKs.
   - `findListingCardElement(pk: number): HTMLElement | null` -- given a PK, find the listing card container by locating the anchor with matching href and walking up to the nearest card-like parent (use `.closest('a[href*="/flat/"]')` then `.closest('[class*="listing"]') || link.parentElement?.parentElement`). This is for badge injection positioning.

6. Update `extension/wxt.config.ts`:
   - Change manifest description: "AI-powered property match scoring for Flatfox.ch"
   - Bump version to "0.2.0"
   - Change host_permissions from `['*://*.homegate.ch/*']` to `['*://*.flatfox.ch/*', 'https://mlhtozdtiorkemamzjjc.supabase.co/*']`
   - Add `permissions: ['storage']` (already there, keep it)

7. Fill Wave 0 test stub `extension/src/__tests__/scoring-types.test.ts` with real assertions:
   - Import `TIER_COLORS` and type interfaces from `@/types/scoring`
   - Test TIER_COLORS has entries for all four tiers (excellent, good, fair, poor)
   - Test TIER_COLORS does NOT use traffic light hex values (#ff0000, #ffff00, #00ff00)
   - Test a sample ScoreResponse object conforms to expected shape (all fields present and typed correctly)
  </action>
  <verify>
    <automated>cd /Users/maximgusev/workspace/gen-ai-hackathon/extension && pnpm install && pnpm compile && pnpm vitest run src/__tests__/scoring-types.test.ts -x 2>&1 | tail -15</automated>
  </verify>
  <done>All 4 lib files compile without errors. @supabase/supabase-js in package.json. wxt.config.ts targets flatfox.ch. Types match backend ScoreResponse exactly. scoring-types tests pass (TIER_COLORS verified, type shape validated).</done>
</task>

<task type="auto">
  <name>Task 2: Background script auth + popup rewrite with Supabase login</name>
  <files>
    extension/src/entrypoints/background.ts,
    extension/src/entrypoints/popup/App.tsx,
    extension/src/components/popup/Dashboard.tsx,
    extension/src/components/popup/LoginForm.tsx,
    extension/src/__tests__/auth-flow.test.ts
  </files>
  <action>
1. Rewrite `extension/src/entrypoints/background.ts`:
   - Import `supabase` from `@/lib/supabase`
   - Keep the `handleInstalled` export (but change URL from onboarding to open the Next.js preferences website in a new tab instead -- `https://homematch-web.vercel.app/dashboard`... actually keep it as opening onboarding for now since it introduces the extension, but update the content)
   - Add message listener with `browser.runtime.onMessage.addListener`:
     - `action: 'signIn'` -- call `supabase.auth.signInWithPassword(msg.credentials)`, return `{ data, error }`
     - `action: 'signOut'` -- call `supabase.auth.signOut()`, return `{ error }`
     - `action: 'getSession'` -- call `supabase.auth.getSession()`, return `{ session: data.session }`
     - `action: 'getUser'` -- call `supabase.auth.getUser()`, return `{ user: data.user, error }`
   - Use `return true` from the listener to indicate async response
   - Keep `browser.runtime.onInstalled.addListener(handleInstalled)`

2. Create `extension/src/components/popup/LoginForm.tsx`:
   - Simple email + password form using existing shadcn/ui components (Input, Button, Label from extension's existing components)
   - On submit: `browser.runtime.sendMessage({ action: 'signIn', credentials: { email, password } })`
   - Show error message if login fails
   - Show loading spinner during sign-in
   - Minimal styling with Tailwind -- keep it clean and compact for popup width

3. Rewrite `extension/src/components/popup/Dashboard.tsx`:
   - Remove ALL references to `useProfile`, `profileStorage`, `useWeightSliders`, `ensureNormalizedWeights`, local storage profile -- this is the old Homegate flow
   - Remove theme toggle and extension toggle (simplify for now)
   - New state: `session` (from `browser.runtime.sendMessage({ action: 'getSession' })` on mount)
   - If no session: show `<LoginForm />` component
   - If session exists: show authenticated state:
     - Header: "HomeMatch" title
     - User email display
     - "Edit Preferences" button that opens `https://homematch-web.vercel.app/dashboard` in a new tab (NOT the onboarding wizard). Use `browser.tabs.create({ url: 'https://homematch-web.vercel.app/dashboard' })`
     - "View Analyses" button that opens `https://homematch-web.vercel.app/analyses` in a new tab (placeholder for future)
     - "Sign Out" button that calls `browser.runtime.sendMessage({ action: 'signOut' })` and resets session state
   - Keep the popup compact (min-w-[320px], max-w-[380px])

4. Update `extension/src/entrypoints/popup/App.tsx`:
   - Keep as-is (it renders `<Dashboard />`)

5. Fill Wave 0 test stub `extension/src/__tests__/auth-flow.test.ts` with real assertions:
   - Import `fakeBrowser` from `wxt/testing`
   - Mock `supabase.auth` methods via the background script's message handler
   - Test signIn message returns session data on success (mock signInWithPassword to return session)
   - Test signIn message returns error on invalid credentials (mock to return error)
   - Test getSession message returns current session
   - Test signOut message clears session
  </action>
  <verify>
    <automated>cd /Users/maximgusev/workspace/gen-ai-hackathon/extension && pnpm compile && pnpm vitest run src/__tests__/auth-flow.test.ts -x 2>&1 | tail -15</automated>
  </verify>
  <done>Popup compiles. Background script handles signIn/signOut/getSession/getUser messages. Dashboard shows LoginForm when no session, shows authenticated state with "Edit Preferences" linking to Next.js site when logged in. No references to old Homegate profile/onboarding flow. Auth flow tests pass.</done>
</task>

</tasks>

<verification>
1. `cd extension && pnpm compile` -- TypeScript compiles without errors
2. `cd extension && pnpm build` -- WXT builds extension successfully
3. `cd extension && pnpm vitest run src/__tests__/scoring-types.test.ts src/__tests__/auth-flow.test.ts -x` -- both test files pass
4. Verify wxt.config.ts has flatfox.ch in host_permissions (not homegate.ch)
5. Verify Dashboard.tsx has no imports from useProfile, useWeightSliders, or ensureNormalizedWeights
6. Verify background.ts handles all 4 message actions (signIn, signOut, getSession, getUser)
</verification>

<success_criteria>
- Extension compiles and builds cleanly with WXT
- All shared types/libs exist and are importable
- Popup shows login form for unauthenticated users
- Popup shows user email + "Edit Preferences" (opens Next.js site) for authenticated users
- Background script manages Supabase auth via chrome.storage.local adapter
- WXT config targets flatfox.ch with Supabase domain in host_permissions
- scoring-types.test.ts passes (TIER_COLORS validation, type shape checks)
- auth-flow.test.ts passes (signIn, signOut, getSession message handling)
</success_criteria>

<output>
After completion, create `.planning/phases/04-extension-ui-analysis-page/04-01-SUMMARY.md`
</output>
