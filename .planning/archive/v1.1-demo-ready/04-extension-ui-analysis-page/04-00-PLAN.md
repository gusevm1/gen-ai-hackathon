---
phase: 04-extension-ui-analysis-page
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - extension/src/__tests__/content-matches.test.ts
  - extension/src/__tests__/flatfox-parser.test.ts
  - extension/src/__tests__/scoring-types.test.ts
  - extension/src/__tests__/auth-flow.test.ts
  - extension/src/__tests__/loading-state.test.ts
  - web/vitest.config.ts
  - web/src/__tests__/analysis-page.test.ts
  - web/src/__tests__/category-breakdown.test.ts
autonomous: true
requirements: [EXT-01, EXT-03, EXT-04, EXT-07, EXT-08, WEB-01, WEB-02]
must_haves:
  truths:
    - "Extension test suite runs and all stubs fail with TODO markers (red phase)"
    - "Web test suite runs with vitest config and all stubs fail with TODO markers"
    - "Every automatable requirement has a test stub ready for implementation plans to fill"
  artifacts:
    - path: "extension/src/__tests__/content-matches.test.ts"
      provides: "Test stub for EXT-01 (content script matches flatfox.ch)"
    - path: "extension/src/__tests__/flatfox-parser.test.ts"
      provides: "Test stub for EXT-03 (listing PK extraction from DOM)"
    - path: "extension/src/__tests__/scoring-types.test.ts"
      provides: "Test stub for EXT-04 (ScoreResponse type validation)"
    - path: "extension/src/__tests__/auth-flow.test.ts"
      provides: "Test stub for EXT-07 (Supabase auth message flow)"
    - path: "extension/src/__tests__/loading-state.test.ts"
      provides: "Test stub for EXT-08 (loading state rendering)"
    - path: "web/vitest.config.ts"
      provides: "Vitest configuration for Next.js web project"
    - path: "web/src/__tests__/analysis-page.test.ts"
      provides: "Test stub for WEB-01 (analysis page renders data)"
    - path: "web/src/__tests__/category-breakdown.test.ts"
      provides: "Test stub for WEB-02 (category breakdown component)"
  key_links:
    - from: "extension/src/__tests__/flatfox-parser.test.ts"
      to: "extension/src/lib/flatfox.ts"
      via: "import extractVisibleListingPKs"
      pattern: "extractVisibleListingPKs"
    - from: "extension/src/__tests__/scoring-types.test.ts"
      to: "extension/src/types/scoring.ts"
      via: "import ScoreResponse, TIER_COLORS"
      pattern: "ScoreResponse"
---

<objective>
Wave 0 test scaffolding -- create all test stub files defined in VALIDATION.md so that implementation plans have failing tests to turn green.

Purpose: The Nyquist rule requires every plan verify block to reference an automated test. This plan creates the test infrastructure (web vitest config) and all test stub files with `it.todo()` or `it.skip()` markers. Implementation plans (01, 02, 03) will fill these stubs with real assertions as they build the production code.

Output: 8 test stub files across extension and web, plus web vitest config. All stubs runnable but marked as pending/todo.
</objective>

<execution_context>
@/Users/maximgusev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maximgusev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-extension-ui-analysis-page/04-CONTEXT.md
@.planning/phases/04-extension-ui-analysis-page/04-VALIDATION.md

@extension/vitest.config.ts
@extension/src/__tests__/background.test.ts
@web/package.json
@web/src/__tests__/preferences-schema.test.ts

<interfaces>
<!-- Backend ScoreResponse contract (types to validate in scoring-types.test.ts) -->
```typescript
interface CategoryScore {
  name: string;       // location, price, size, features, condition
  score: number;      // 0-100
  weight: number;     // 0-100
  reasoning: string[];
}

interface ChecklistItem {
  criterion: string;
  met: boolean | null;
  note: string;
}

interface ScoreResponse {
  overall_score: number;     // 0-100
  match_tier: 'excellent' | 'good' | 'fair' | 'poor';
  summary_bullets: string[];
  categories: CategoryScore[];
  checklist: ChecklistItem[];
  language: string;
}

const TIER_COLORS = {
  excellent: { bg: '#10b981', text: '#ffffff' },
  good:      { bg: '#3b82f6', text: '#ffffff' },
  fair:      { bg: '#f59e0b', text: '#1f2937' },
  poor:      { bg: '#6b7280', text: '#ffffff' },
};
```

<!-- Flatfox URL pattern for parser tests -->
```
/en/flat/{slug}/{pk}/
/de/flat/{slug}/{pk}/
```

<!-- Extension content script match pattern -->
```
matches: ['*://*.flatfox.ch/*']
```

<!-- Auth message actions -->
```typescript
browser.runtime.sendMessage({ action: 'signIn', credentials: { email, password } })
browser.runtime.sendMessage({ action: 'signOut' })
browser.runtime.sendMessage({ action: 'getSession' })
browser.runtime.sendMessage({ action: 'getUser' })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extension test stubs (5 files)</name>
  <files>
    extension/src/__tests__/content-matches.test.ts,
    extension/src/__tests__/flatfox-parser.test.ts,
    extension/src/__tests__/scoring-types.test.ts,
    extension/src/__tests__/auth-flow.test.ts,
    extension/src/__tests__/loading-state.test.ts
  </files>
  <action>
Create 5 test stub files in `extension/src/__tests__/`. Follow the existing test pattern from `background.test.ts` (uses vitest imports, `fakeBrowser` from `wxt/testing` where needed).

1. `content-matches.test.ts` (EXT-01):
   - `describe('content script configuration')` block
   - `it.todo('matches *://*.flatfox.ch/* URL pattern')` -- will verify the content script's matches config
   - `it.todo('does NOT match homegate.ch URLs')` -- regression guard
   - `it.todo('uses cssInjectionMode ui for Shadow DOM style isolation')` -- verify WXT config

2. `flatfox-parser.test.ts` (EXT-03):
   - `describe('extractVisibleListingPKs')` block
   - `it.todo('extracts PKs from /en/flat/{slug}/{pk}/ href patterns')` -- core extraction
   - `it.todo('extracts PKs from /de/flat/{slug}/{pk}/ href patterns')` -- locale variants
   - `it.todo('deduplicates PKs from multiple links to same listing')` -- dedup
   - `it.todo('returns empty array when no listing links present')` -- empty state
   - `it.todo('ignores non-flat URLs containing /flat/ substring')` -- false positive guard
   - `describe('findListingCardElement')` block
   - `it.todo('returns the closest card-like parent for a given PK')` -- DOM traversal

3. `scoring-types.test.ts` (EXT-04):
   - `describe('ScoreResponse types')` block
   - `it.todo('TIER_COLORS has entries for all four match tiers')` -- completeness
   - `it.todo('TIER_COLORS uses modern palette (not traffic light green/yellow/red)')` -- per user constraint
   - `it.todo('ScoreResponse interface matches backend contract')` -- type shape validation (use a sample object and verify all fields exist)

4. `auth-flow.test.ts` (EXT-07):
   - Import `fakeBrowser` from `wxt/testing`
   - `describe('extension auth message flow')` block
   - `it.todo('signIn message returns session data on success')` -- happy path
   - `it.todo('signIn message returns error on invalid credentials')` -- error path
   - `it.todo('getSession message returns current session')` -- session retrieval
   - `it.todo('signOut message clears session')` -- sign out
   - `it.todo('getUser message returns authenticated user')` -- user retrieval

5. `loading-state.test.ts` (EXT-08):
   - `describe('loading state behavior')` block
   - `it.todo('shows loading skeleton while score is being computed')` -- loading indicator
   - `it.todo('removes loading skeleton when score arrives')` -- cleanup
   - `it.todo('FAB shows spinner while scoring is in progress')` -- FAB loading state
  </action>
  <verify>
    <automated>cd /Users/maximgusev/workspace/gen-ai-hackathon/extension && pnpm vitest run src/__tests__/content-matches.test.ts src/__tests__/flatfox-parser.test.ts src/__tests__/scoring-types.test.ts src/__tests__/auth-flow.test.ts src/__tests__/loading-state.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All 5 extension test files exist and are recognized by vitest. `it.todo()` tests show as "todo" (not failures, not errors). Test runner completes successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Web vitest config and test stubs (3 files)</name>
  <files>
    web/vitest.config.ts,
    web/src/__tests__/analysis-page.test.ts,
    web/src/__tests__/category-breakdown.test.ts
  </files>
  <action>
1. Create `web/vitest.config.ts`:
   - Import `defineConfig` from `vitest/config`
   - Configure with `@vitejs/plugin-react` (already in web devDependencies)
   - Set test environment to `'jsdom'` (standard for React component testing with Next.js; NOTE: do NOT use happy-dom as it may have issues with Next.js imports)
   - Set `include: ['src/__tests__/**/*.test.ts', 'src/__tests__/**/*.test.tsx']`
   - Set `globals: true`
   - Set resolve alias: `'@' -> './src'` (matching Next.js tsconfig paths)
   - Reference the existing `web/src/__tests__/preferences-schema.test.ts` -- make sure it still works with the new config

2. Create `web/src/__tests__/analysis-page.test.ts` (WEB-01):
   - `describe('analysis page')` block
   - `it.todo('renders overall score and match tier for a valid analysis')` -- happy path
   - `it.todo('shows not-found message when no analysis exists for listing')` -- 404 state
   - `it.todo('renders summary bullets section')` -- bullet rendering
   - `it.todo('includes link back to dashboard')` -- navigation

3. Create `web/src/__tests__/category-breakdown.test.ts` (WEB-02):
   - `describe('CategoryBreakdown component')` block
   - `it.todo('renders all 5 categories with scores and weights')` -- completeness
   - `it.todo('shows reasoning bullets for each category')` -- per-category detail
   - `it.todo('colors score bars by tier threshold')` -- visual encoding
   - `describe('ChecklistSection component')` block
   - `it.todo('renders met items with check icon')` -- true state
   - `it.todo('renders unmet items with X icon')` -- false state
   - `it.todo('renders unknown items with question mark icon')` -- null state

4. Verify the existing `preferences-schema.test.ts` still passes with the new vitest config:
   - Run `cd web && npx vitest run src/__tests__/preferences-schema.test.ts` to confirm backward compatibility
  </action>
  <verify>
    <automated>cd /Users/maximgusev/workspace/gen-ai-hackathon/web && npx vitest run src/__tests__/ 2>&1 | tail -20</automated>
  </verify>
  <done>web/vitest.config.ts exists and works. All 3 web test files (including existing preferences-schema.test.ts) run successfully. New stubs show as "todo". No vitest configuration errors.</done>
</task>

</tasks>

<verification>
1. `cd extension && pnpm vitest run src/__tests__/ 2>&1 | tail -30` -- all extension tests run (existing + new stubs)
2. `cd web && npx vitest run src/__tests__/ 2>&1 | tail -30` -- all web tests run (existing + new stubs)
3. No test failures (only todo/skip markers on new stubs)
4. Existing extension tests (background, profile-schema, etc.) still pass
5. Existing web test (preferences-schema) still passes
</verification>

<success_criteria>
- 5 extension test stub files created with descriptive todo tests
- web/vitest.config.ts created and working
- 2 web test stub files created with descriptive todo tests
- All existing tests unaffected (no regressions)
- Test runners complete cleanly in both projects
</success_criteria>

<output>
After completion, create `.planning/phases/04-extension-ui-analysis-page/04-00-SUMMARY.md`
</output>
