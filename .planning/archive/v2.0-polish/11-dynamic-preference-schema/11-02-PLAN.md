---
phase: 11-dynamic-preference-schema
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - backend/app/prompts/scoring.py
  - backend/tests/test_prompts.py
  - web/src/components/preferences/dynamic-fields-section.tsx
  - web/src/components/preferences/preferences-form.tsx
  - web/src/components/preferences/soft-criteria-section.tsx
  - web/src/lib/profile-summary.ts
  - web/src/app/(dashboard)/profiles/[profileId]/page.tsx
  - web/src/app/(dashboard)/dashboard/actions.ts
autonomous: true
requirements: [SCHM-04, SCHM-01]

must_haves:
  truths:
    - "Claude scoring prompt renders dynamic fields grouped by importance level (CRITICAL/HIGH/MEDIUM/LOW) instead of flat soft criteria list"
    - "System prompt references 'custom criteria' instead of 'soft criteria'"
    - "User sees dynamic fields UI with name, value, and importance dropdown in the preferences form"
    - "User can add, edit, and remove dynamic fields in the preferences form"
    - "Profile page applies migratePreferences before parse so existing softCriteria data displays as dynamic fields"
    - "Profile summary includes dynamic fields information"
  artifacts:
    - path: "backend/app/prompts/scoring.py"
      provides: "_format_dynamic_fields_section() + updated build_user_prompt + updated build_system_prompt"
      contains: "_format_dynamic_fields_section"
    - path: "backend/tests/test_prompts.py"
      provides: "Tests for dynamic fields rendering in prompt with importance grouping"
      contains: "test_dynamic"
    - path: "web/src/components/preferences/dynamic-fields-section.tsx"
      provides: "DynamicFieldsSection component using useFieldArray"
      exports: ["DynamicFieldsSection"]
    - path: "web/src/components/preferences/preferences-form.tsx"
      provides: "Form using DynamicFieldsSection instead of SoftCriteriaSection"
      contains: "DynamicFieldsSection"
  key_links:
    - from: "backend/app/prompts/scoring.py"
      to: "backend/app/models/preferences.py"
      via: "prefs.dynamic_fields used in _format_dynamic_fields_section"
      pattern: "prefs\\.dynamic_fields"
    - from: "web/src/components/preferences/dynamic-fields-section.tsx"
      to: "web/src/lib/schemas/preferences.ts"
      via: "useFieldArray on dynamicFields + DynamicField type"
      pattern: "useFieldArray.*dynamicFields"
    - from: "web/src/app/(dashboard)/profiles/[profileId]/page.tsx"
      to: "web/src/lib/schemas/preferences.ts"
      via: "migratePreferences called before preferencesSchema.parse"
      pattern: "migratePreferences"
---

<objective>
Update the Claude scoring prompt to render dynamic fields with importance weighting, replace the soft criteria UI with a dynamic fields editor, and wire migration at all preferencesSchema.parse() call sites.

Purpose: Completes the user-facing and scoring-facing integration of dynamic fields. Users can edit dynamic fields with importance levels in the form, and Claude receives importance-weighted criteria for better scoring differentiation.

Output: Updated scoring prompt, new DynamicFieldsSection component, updated preferences form, all call sites migrated.
</objective>

<execution_context>
@/Users/maximgusev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maximgusev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-dynamic-preference-schema/11-RESEARCH.md
@.planning/phases/11-dynamic-preference-schema/11-01-SUMMARY.md

Source files being modified:
@backend/app/prompts/scoring.py
@backend/tests/test_prompts.py
@web/src/components/preferences/soft-criteria-section.tsx
@web/src/components/preferences/preferences-form.tsx
@web/src/lib/profile-summary.ts
@web/src/app/(dashboard)/profiles/[profileId]/page.tsx
@web/src/app/(dashboard)/dashboard/actions.ts

<interfaces>
<!-- From Plan 01 outputs (DynamicField types created in both schemas) -->

From backend/app/models/preferences.py:
```python
class DynamicField(BaseModel):
    model_config = ConfigDict(alias_generator=to_camel, populate_by_name=True)
    name: str
    value: str = ""
    importance: ImportanceLevel = ImportanceLevel.MEDIUM

class UserPreferences(BaseModel):
    # ... existing fields ...
    dynamic_fields: list[DynamicField] = Field(default_factory=list)
```

From web/src/lib/schemas/preferences.ts:
```typescript
export const dynamicFieldSchema = z.object({
  name: z.string().min(1),
  value: z.string().default(''),
  importance: importanceLevelSchema,
})
export type DynamicField = z.infer<typeof dynamicFieldSchema>
// preferencesSchema now includes: dynamicFields: z.array(dynamicFieldSchema).default([])
export function migratePreferences(raw: Record<string, unknown>): Record<string, unknown>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update scoring prompt to render dynamic fields with importance weighting</name>
  <files>backend/app/prompts/scoring.py, backend/tests/test_prompts.py</files>
  <behavior>
    - Test: Prompt with dynamic fields shows "Custom Criteria (by importance):" section
    - Test: Critical-importance field appears under "CRITICAL (must have):" label
    - Test: High-importance field appears under "HIGH (strongly preferred):" label
    - Test: Medium-importance field appears under "MEDIUM (nice to have):" label
    - Test: Low-importance field appears under "LOW (minor preference):" label
    - Test: Dynamic field with value renders as "name: value" (e.g., "Near train: within 500m")
    - Test: Dynamic field without value renders as just "name"
    - Test: Empty dynamic_fields produces no Custom Criteria section
    - Test: System prompt references "custom criterion" instead of "soft criterion"
    - Test: Prompt no longer contains "Soft criteria:" line when dynamic fields exist
    - Test: Existing prompt tests still pass (soft_criteria backward compat in prompts)
  </behavior>
  <action>
    1. In `backend/app/prompts/scoring.py`:
       - Add `_format_dynamic_fields_section(prefs: UserPreferences) -> str` function that:
         a. Returns empty string if `prefs.dynamic_fields` is empty
         b. Groups fields by importance level
         c. For each non-empty importance group, renders header (e.g., "CRITICAL (must have):") and bullet items
         d. Field with value renders as "- name: value", without value as "- name"
       - In `build_user_prompt()`:
         a. Replace the line `**Soft criteria:** {", ".join(prefs.soft_criteria) if prefs.soft_criteria else "None"}` with a conditional: if `prefs.dynamic_fields` is not empty, call `_format_dynamic_fields_section(prefs)`; else fall back to the old soft criteria line for backward compat during transition
         b. This ensures old profiles without dynamic_fields (and without softCriteria migration having run on the backend yet) still render correctly
       - In `build_system_prompt()`:
         a. Change "evaluate each soft criterion and desired feature individually" to "evaluate each custom criterion and desired feature individually"

    2. In `backend/tests/test_prompts.py`:
       - Add `TestDynamicFieldsPrompt` class with all behavior tests above
       - Use `UserPreferences` with `dynamic_fields=[DynamicField(name=..., value=..., importance=...), ...]` to test prompt rendering
       - Import `DynamicField` from `app.models.preferences`
       - Update `full_preferences` fixture: add `dynamic_fields` list (keeping backward compat with existing tests that check for "near Bahnhof" in the prompt -- those tests use `soft_criteria` which should still work since the fallback renders soft_criteria when dynamic_fields is empty)

    Run RED tests first, then implement, then verify GREEN.
  </action>
  <verify>
    <automated>cd /Users/maximgusev/workspace/gen-ai-hackathon/backend && python -m pytest tests/test_prompts.py -x -v</automated>
  </verify>
  <done>
    - _format_dynamic_fields_section renders importance-grouped custom criteria
    - build_user_prompt uses dynamic fields when present, falls back to soft criteria
    - build_system_prompt references "custom criterion" not "soft criterion"
    - All prompt tests pass (new + existing)
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace SoftCriteriaSection with DynamicFieldsSection + wire migration at parse sites</name>
  <files>web/src/components/preferences/dynamic-fields-section.tsx, web/src/components/preferences/preferences-form.tsx, web/src/lib/profile-summary.ts, web/src/app/(dashboard)/profiles/[profileId]/page.tsx, web/src/app/(dashboard)/dashboard/actions.ts</files>
  <action>
    1. Create `web/src/components/preferences/dynamic-fields-section.tsx`:
       - Import `useFieldArray` from `react-hook-form`, `type UseFormReturn` from `react-hook-form`, `type Preferences` from schema
       - Import `Input`, `Button`, `Select`, `SelectTrigger`, `SelectValue`, `SelectContent`, `SelectItem` from UI components, `X` from lucide-react
       - Use `useFieldArray({ control: form.control, name: 'dynamicFields' })` for proper array management
       - Render each field with: Input for name (placeholder "Criterion name"), Input for value (placeholder "Details (optional)"), importance Select dropdown with options critical/high/medium/low (display labels: "Critical", "High", "Medium", "Low"), remove button (X icon)
       - "Add Criterion" button that appends `{ name: '', value: '', importance: 'medium' }`
       - Section label: "Custom Criteria" with subtitle "Add any additional preferences with importance levels"

    2. Update `web/src/components/preferences/preferences-form.tsx`:
       - Replace import of `SoftCriteriaSection` with `DynamicFieldsSection` from `./dynamic-fields-section`
       - Replace `<SoftCriteriaSection form={form} />` with `<DynamicFieldsSection form={form} />`
       - Update AccordionItem value from "soft" to "dynamic" and AccordionTrigger text from "Soft Criteria" to "Custom Criteria"
       - Update defaultValue array: replace "soft" with "dynamic"

    3. Update `web/src/lib/profile-summary.ts`:
       - Add dynamic fields to summary: if `prefs.dynamicFields` has entries, show count and first 2 names (e.g., "3 custom criteria: near Bahnhof, quiet +1 more")
       - Show critical dynamic fields specifically if any exist

    4. Wire `migratePreferences` at all `preferencesSchema.parse()` call sites:
       - `web/src/app/(dashboard)/profiles/[profileId]/page.tsx` line 32: change `preferencesSchema.parse(profile.preferences ?? {})` to `preferencesSchema.parse(migratePreferences(profile.preferences ?? {}))`
       - `web/src/app/(dashboard)/dashboard/actions.ts` line 52: change `preferencesSchema.parse(data.preferences)` to `preferencesSchema.parse(migratePreferences(data.preferences as Record<string, unknown> ?? {}))`
       - `web/src/app/(dashboard)/profiles/actions.ts` line 168: NO migration needed here (this is the save path -- form already produces dynamicFields)
       - `web/src/app/(dashboard)/profiles/actions.ts` line 24: NO migration needed (creates empty defaults)
       - Import `migratePreferences` from `@/lib/schemas/preferences` at each modified call site

    5. Do NOT delete `soft-criteria-section.tsx` yet -- keep it for reference. It is not imported by the updated form.
  </action>
  <verify>
    <automated>cd /Users/maximgusev/workspace/gen-ai-hackathon/web && npx vitest run && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - DynamicFieldsSection component exists with name, value, importance inputs and add/remove controls
    - PreferencesForm uses DynamicFieldsSection instead of SoftCriteriaSection
    - migratePreferences wired at profile load call sites (profiles/[profileId]/page.tsx, dashboard/actions.ts)
    - Profile summary includes dynamic fields information
    - Web app builds without errors, all vitest tests pass
  </done>
</task>

</tasks>

<verification>
Backend prompt tests green:
- `cd backend && python -m pytest tests/test_prompts.py -x -v` -- all pass

Full backend suite:
- `cd backend && python -m pytest tests/ -x` -- all pass

Web tests and build:
- `cd web && npx vitest run` -- all pass
- `cd web && npx next build` -- builds without errors
</verification>

<success_criteria>
- Claude scoring prompt renders dynamic fields with importance-grouped sections
- System prompt says "custom criterion" not "soft criterion"
- DynamicFieldsSection component renders with useFieldArray for add/edit/remove
- PreferencesForm uses DynamicFieldsSection
- migratePreferences called before parse at profile load sites
- Profile summary shows dynamic fields info
- All tests pass, web app builds clean
</success_criteria>

<output>
After completion, create `.planning/phases/11-dynamic-preference-schema/11-02-SUMMARY.md`
</output>
