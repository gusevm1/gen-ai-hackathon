# Continue Here — HomeMatch v4.0 Landing Page & Design System

**Paused:** 2026-03-27
**Branch:** `redesign/v4-landing`
**Milestone:** v4.0 — Landing Page & Design System
**Progress:** 50% — Phases 18 + 19 complete, Phases 20 + 21 remaining

---

## Where We Are

Phases 18 and 19 are fully executed, TypeScript-clean, and UAT-verified.

**Last commit:** `04d5092` — fix(landing): fix Button render prop and translations type check

### What's been built

**Phase 18 — Design System** (`5b4dabe`)
- `motion` package installed (`motion@^12.38.0`, import from `"motion/react"`)
- `web/src/lib/motion.ts` — duration, ease, spring constants + Framer variants
- `web/src/components/motion/FadeIn.tsx` — scroll-triggered fade with `whileInView`
- `web/src/components/motion/StaggerGroup.tsx` + `StaggerItem`
- `web/src/components/motion/CountUp.tsx` — animated number counters
- `web/src/app/globals.css` — teal tokens (`--color-hero-teal`, `--color-teal`, etc.) + typography scale CSS variables (`--text-display-*`, `--text-headline-*`, etc.)

**Phase 19 — Landing Page** (`9ffb3d7` + `04d5092`)
- `web/src/app/page.tsx` — Server Component; shows landing page, redirects logged-in → `/dashboard`
  - Has env-var guard: works locally without Supabase configured
- `web/src/app/auth/page.tsx` — auth form extracted from old `/`
- `web/src/lib/translations.ts` — +31 `landing_*` keys (EN + DE), `translations` now exported
- `web/src/components/landing/` — 8 components:
  - `LandingPageContent.tsx` — `'use client'` boundary, language context
  - `LandingNavbar.tsx` — fixed sticky, scroll-aware opacity via `useScroll`/`useTransform`
  - `HeroSection.tsx` — dark hero, display headline, CTA
  - `HeroDemo.tsx` — `useAnimate` sequential timeline (cards → FAB → scores → panel)
  - `ProblemSection.tsx` — FadeIn, 3 pain bullets
  - `HowItWorksSection.tsx` — StaggerGroup 3-step grid
  - `FeaturesSection.tsx` — StaggerGroup 3-card grid
  - `CtaSection.tsx` — dark CTA section
  - `LandingFooter.tsx` — logo + copyright + privacy link
- Tests: 156 pass / 6 fail (6 are pre-existing `chat-page.test.tsx` — untouched)

### Key fixes applied (not obvious from plan)
- `Button` uses `@base-ui/react` — no `asChild` prop. Use `render={<Link href="..." />}` instead
- `_typeCheck` with `as const` translations: use key-extends check, not value-assign

---

## What's Next

### Phase 20 — Dashboard Alignment
Propagate the landing page design language to existing dashboard, profiles, and analysis pages:
- Sidebar / top navbar visual refresh to match new aesthetic
- Typography scale applied to dashboard headings
- Teal accent used consistently (replace any mismatched colors)
- Card styles aligned with landing page feature cards

### Phase 21 — Polish & QA
- Mobile responsiveness pass (hero, demo, sections)
- Performance: LCP/CLS audit
- Cross-browser check
- Final copy review
- PR from `redesign/v4-landing` → `main` + merge

---

## How to Resume

```bash
cd /Users/singhs/gen-ai-hackathon
git checkout redesign/v4-landing
```

Preview landing page:
```bash
cd web && npm run dev
# open http://localhost:3000
```

Then run:
```
/gsd:plan-phase 20
```

---

## Critical Decisions to Remember

- **Button link pattern**: `<Button render={<Link href="..." />}>text</Button>` — NOT `asChild`
- **Motion import**: `from "motion/react"` — NOT `"framer-motion"`
- **page.tsx must stay a Server Component** — no `'use client'`
- **Dashboard layout `redirect("/")` stays unchanged** — now correctly lands on the landing page
- **Supabase env guard in page.tsx** — intentional for local dev without credentials
- **Branch policy**: All work on `redesign/v4-landing`, merge to main only when milestone complete
